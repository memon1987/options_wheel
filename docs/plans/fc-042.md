# Plan: Backtest Engine Follow-on — Performance, Fidelity, and the Filter Studies

**FC entry:** `docs/FUTURE_CONSIDERATIONS.md` FC-042 (engine work); executes studies for FC-002, FC-034, FC-036
**Plan file:** `docs/plans/fc-042.md`
**Status:** Done — all tracks closed. **Two blocking findings surfaced (FC-048, FC-049); see Closeout.**
**Size:** L (across tracks; individual tracks are S/M)
**Author:** zeshan + Claude
**Last updated:** 2026-07-18

---

## Context

FC-032 shipped a working backtest engine (PRs #35, #45): it replays the live strategy code over historical Alpaca data, validated against 204 real fills (81% strike reproduction, 100% delta-band accuracy) and against live behavior (the gap filter's blocking of NVDA in Jun–Jul 2026 matches production, which sold zero NVDA puts in the same window).

**CORRECTION (2026-07-29, from the B1 study — FC-049):** that second validation was a **false confirmation**. The stage-2 gap filter is reached only via `WheelEngine.run_strategy_cycle()`, which no deployed endpoint calls — `/run` moved to the `OpportunityStore` + `ExecutionEngine` path in `842dcce` (2025-10-03), three days before this account's first fill. So the filter has never gated a live trade, and production's zero NVDA puts in Jun–Jul 2026 had some other cause. The backtest and production agreed by coincidence, not because the engine reproduced a live control. Treat every stage-2 block rate in this plan and in FC-002 as describing the **backtest engine only**.

Three things now stand between "working engine" and "engine that answers our money questions":

1. **Runs cost ~25 min/symbol cold.** `ChainStore` (the parquet cache) exists but is constructed nowhere, and contract discovery fetches every strike from $50 to $390 when only ±15% of spot can ever match the delta band (~70% waste, measured on NVDA 2025-10-06).
2. **Two fidelity gaps block demotion decisions on income names.** Dividends and ex-div early assignment are unmodeled. The dividend bias runs *both* ways: it flatters the wheel on the buy-and-hold comparison but **penalises it on the absolute gates that produce `unfit`** — on a 5–7% yielder the missing dividends alone can consume the entire 4% risk-free floor.
3. **The highest-value open question is now empirically answerable.** The June–July 2026 walkthrough (see `docs/investigations/`) showed NVDA blocked ~90% of the month by the gap-risk filter — specifically the **40% realized-vol cap**, not gap frequency: NVDA's 30-day vol sat at 40–42% against a hard 40% line, so whether it traded on a given day was threshold noise. NVDA is 155 of 241 lifetime put legs; AMD has been blocked on 47% of scans (FC-002). The filter double-counts risk that delta-based strike selection already prices, with a binary cliff and no graduated response. Whether the current calibration is right is a question the engine can now answer with data instead of opinion.

## Goals

- Track A: warm backtest runs in **minutes, not hours** (cache + strike-window).
- Track B: **published A/B studies** for the gap filter (FC-002) and the premium floor (FC-034) — evidence, not code changes.
- Track C: dividends and early assignment modeled; income names judgeable.
- Track E: the two known production bugs (FC-036, FC-035) fixed behind their own small PRs, with the FC-036 fix **quantified by the engine before it is enabled**.

## Non-goals

- **No live threshold changes in this FC.** Track B produces investigation documents. Any change to `gap_risk_controls` or `min_put_premium` is a separate live-behavior change gated on its own FC plan (FC-002 / FC-034) and the two-reviewer process.
- No demotion automation (FC-032 rollout rule stands: two observed screen cycles first).
- Track D (Cloud Run Job deploy, scheduler cleanup) is **owner infrastructure**, tracked in FC-032's plan — not agent work.

## The tracks

### Track A — Make experiments cheap *(prerequisite for B and E1's study)*

**A1. Wire `ChainStore`.** `ChainBuilder` gains an optional store; on `build()`, check cache → build → persist. `Simulator`/`evaluate` construct it by default (`cache/backtest/`). Include a cache-invalidation note (keyed by symbol/date; chains for a past date are immutable).
*Acceptance:* second run of a 1-month NVDA backtest completes with **zero** option-bar API calls (verify by instrumenting the provider); full suite green.

**A2. Strike-window filter in `get_contract_universe`.** Fetch only strikes within ±25% of the as-of close, **extended upward to `max(spot, cost_basis) × 1.25` when shares are held** — covered calls must clear cost basis, and a naive spot-window would starve the call side of exactly the underwater positions that need rolling. Alpaca's contracts endpoint accepts `strike_price_gte/lte`.
*Acceptance (as drafted):* NVDA single-day universe drops from ~148 contracts to <60 with **identical** chain output in the delta band (assert same selected candidate on 5 sampled days); parity spot-check unchanged.

*Acceptance (revised during execution — see Execution note A2):* the `<60`
target is **not achievable and was derived from one unrepresentative day**. It
is replaced by a percentage-reduction target plus the identity requirement,
which is the criterion that actually protects correctness:

- Delta-band chain output **identical** on 5 sampled days, and a full-month
  replay windowed vs unbounded produces an identical ledger and equity curve.
- Universe reduction reported honestly **through the shipped code path** (i.e.
  including the cost-basis/low anchors the simulator passes), not spot-only.
- Parity spot-check unchanged, measured rather than assumed.

### Track B — The filter studies *(after A; investigation docs, no live changes)*

**B1. Gap-filter A/B → feeds FC-002.** Per symbol (at minimum NVDA, AMD, GOOGL, IWM), 2024-02→present, run: (a) filter as-is, (b) filter off, (c) vol cap swept 40/50/60%, (d) vol-relative gate (block only above the symbol's own trailing 1-year vol percentile, e.g. 80th), (e) graduated response (over-threshold ⇒ half size or delta band shifted to [0.10, 0.15] instead of a ban). Compare **return on collateral, worst cycle, max drawdown, days-in-position**. Deliverable: `docs/investigations/fc-002-gap-filter-ab.md` with a recommendation. Remember the engine's own caveat: results are one vol regime (2024+).

**B2. Premium-floor A/B → feeds FC-034.** Same window, F/PFE/KMI/VZ plus two liquid controls: (a) `$0.50` flat (status quo), (b) %-of-strike (e.g. 0.4%), (c) annualized return-on-collateral floor (e.g. ≥8%). Deliverable: `docs/investigations/fc-034-premium-floor-ab.md` answering FC-034's open question (demote vs re-shape).

### Track C — Fidelity *(parallel with B)*

**C1. Dividends.** Fetch per-symbol dividend history (yfinance or Alpaca corporate-actions endpoint — the corporate-actions endpoint was already validated during `detect_split` verification), commit a static table like the earnings one, wire `broker.credit_dividend` into the day loop on ex-dates while holding shares, and add the dividend stream to the buy-and-hold benchmark. Update the `KNOWN_BIASES` footer — the both-directions caveat shrinks to residual (estimates vs actuals).
*Acceptance:* a KMI window shows non-zero dividends on both legs; attribution still reconciles to the cent; the bias footer text updated.

**C2. Ex-div early assignment.** Wire `broker.assign_call_early` into the day loop: day before ex-date, short ITM call with extrinsic < dividend ⇒ assigned. Uses C1's table. *Acceptance:* unit test with a scripted ex-div scenario; golden-path test extended.

**C3. Spread-model calibration (lower priority).** Run `SpreadModel.calibrate()` from live chain samples captured **during RTH** (the existing 2.1×-wide measurement was after-hours and is flagged unproven intraday); commit the calibrated params with provenance. *Acceptance:* `spread_model_check.py` re-run intraday shows modeled/real ratio documented; report footer updated from "not calibrated".

### Track E — Production bugs *(independent; own small PRs; live-behavior gated)*

**E1. FC-036 — Stage-4 gap check.** Two halves, in order: **(study, needs A1)** replay 2024→present with the fixed gate to quantify what it would have blocked — this is a live-behavior change and the evidence goes in the PR; **(fix)** date-based comparison per `_detect_current_gap`'s pattern. Two-reviewer process applies (live risk path).

**E2. FC-035 — SUPERSEDED by `docs/plans/fc-035.md`: the path was deleted, not fixed.** *(Original framing below.)* **`poll_order_statuses` NameError.** One-line import fix *after* checking whether the never-executed closed-orders path double-logs `order_filled` against the activities ingestor (FC-035's open question). Small PR, but it enables a path that has never run — treat as live change.

## Execution: parallel-agent plan

Designed for concurrent agents on separate branches. **File-conflict map is the constraint:**

| Agent | Scope | Branch | Files touched | Conflicts with |
|---|---|---|---|---|
| 1 | A1 + A2 (one PR — same area) | `fc-042-track-a` | `data/chain_store.py`, `data/chain_builder.py`, `data/alpaca_provider.py`, `engine/simulator.py`, `evaluate.py` | Agent 2 (simulator.py — minor) |
| 2 | C1 + C2 (one PR) | `fc-042-track-c` | `engine/broker.py` (call sites only), `engine/simulator.py`, new `data/dividends.py` + table, `reporting/report.py` footer | Agent 1 (simulator.py) |
| 3 | E2 + C3 (two small PRs) | `fc-035-fix`, `fc-042-spread-cal` | `strategy/wheel_engine.py`; `data/spread_model.py` + tool | none |
| 4 (wave 2) | B1 + B2 studies + E1 study | after Agent 1 merges | investigation docs only (+ a config-override hook in the study harness if needed) | none |

**Rules for the executing agents:**
1. **Wave 1** = Agents 1–3 in parallel, separate branches/worktrees. Agent 1 merges first; Agent 2 rebases (expected conflict: the day-loop block in `simulator.py` — small, mechanical).
2. **Wave 2** = studies, only after A1 is merged (they are unaffordable without the cache). B1 and B2 can run as parallel analysis agents; they write docs, not source.
3. Every code PR: full suite green (**504+ tests**), plus the track's acceptance criteria above. Peer review per CLAUDE.md — one reviewer for backtest-only tracks (A, C), **two reviewers for E1/E2** (live paths).
4. Session hygiene learned this FC (violate at your peril): stage files **deliberately, never `git add -A`** (swept in foreign work three times); never `git checkout --` a file with uncommitted changes (use `cp` backups for mutation tests); clear `__pycache__` before trusting any runtime observation after mutation testing; check `git status` after any subagent review returns.
5. **No agent touches Cloud Scheduler, Cloud Run services, or live config.** Track D is owner-only.

## Verification

- [ ] A1: warm re-run makes zero option-bar API calls; timed <2 min for a 1-month symbol.
- [ ] A2: identical delta-band chain on 5 sampled days pre/post filter.
- [ ] C1/C2: attribution reconciles; KMI shows dividends both legs; golden path extended.
- [ ] B1/B2: investigation docs with per-symbol tables, explicit single-regime caveat, and a recommendation section that separates *evidence* from *proposed live change*.
- [ ] E1/E2: two-reviewer sign-off before merge; E1 PR includes the would-have-blocked study.

## Rollout / rollback

All Track A/C changes are backtest-module-only — rollback is reverting the module. Track E PRs are individually revertable. No live thresholds move under this plan.


---

## Execution

### Track A — `fc-042-track-a` (A1 + A2, one PR)

- **PR:** https://github.com/memon1987/options_wheel/pull/49
- **Commits:** `322c75b` (implementation), `37fd2dc` (adversarial-review fixes)
- **Date:** 2026-07-18
- **Tests:** 504 → 542, full suite green.

**A1 — measured.** 1-month NVDA backtest (2025-10-01..10-31, 23 decision days),
counted by instrumenting the provider rather than timing:

| | option-bar calls | contract-discovery calls | wall clock |
|---|---|---|---|
| Cold | 23 | 23 | 21.0s |
| Warm | **0** | **0** | **0.2s** |

The warm run reproduced the cold run's equity curve and ledger exactly.

**A2 — measured, and where the plan was wrong.** Three corrections:

1. **`<60 contracts` is unachievable and was sampled from one day.** ±25% on
   the plan's own reference day (NVDA 2025-10-06) gives **64**, not <60. The
   universe is far larger on other dates — 932 contracts on 2026-01-12, 608 on
   2026-03-09 — so no fixed percentage window hits an absolute count of 60 in
   general (±15% on 2026-03-09 still leaves 176). The plan's two acceptance
   numbers (±25% *and* <60) are mutually inconsistent. ±25% was kept: the
   failure mode of a too-tight window is a silently different backtest, not a
   slow one.

2. **The reduction must be quoted through the shipped path.** The window the
   simulator actually requests is widened by the cost-basis ceiling and the low
   anchor. Spot-only figures overstate the saving:

   | Window | Spot-only cut | **Shipped cut** |
   |---|---|---|
   | 1 month | 57.5% | **49.1%** |
   | 6 months | 49.2% | **18.5%** |

   The saving **decays as the window lengthens**, because the anchors widen
   with it. For Track B's 2024-02→present studies the strike filter will
   contribute little; A1's cache is what makes those runs affordable.

3. **The `~25 min/symbol` figure does not describe a 1-month window** — that is
   ~21s cold. It is consistent with a full 2024-02→present history (~600
   decision days), doubled by the sensitivity pass.

**Identity, proved three ways** (all against a genuinely unbounded baseline):
delta-band quotes byte-identical on 5 sampled days; a full-month replay
windowed vs unbounded produced an identical ledger and equity curve; and
`parity_check.py --limit 40` returned identical verdicts both ways
(19 exact / 15 close / 6 diverged).

**Notes / deviations.**

- **`Simulator` does not construct a `ChainStore`.** It receives an
  already-built `ChainBuilder` and does not own it. The store is owned by
  `ChainBuilder` and wired at the application entry point (`evaluate`).
  Caching is opt-in rather than default-on so a unit test can never read a
  chain another test wrote.
- **The cache key needed more than `(symbol, date)`.** Chain content also
  depends on the DTE reach, the strike window, and — critically — the pricing
  model, since bid/ask/delta/iv are *computed*, not fetched. Each file records
  the request it answers plus a model fingerprint, and a read is a hit only
  when the file provably covers the request. **This matters for Track C:** C1
  (dividend yields) and C3 (spread recalibration) both change cached values,
  and without the fingerprint every study Track B ran would silently mix two
  pricing models. Bump the fingerprint's schema version if the *derivation*
  changes rather than its inputs.
- **Open follow-up — build chains lazily.** Every remaining wart descends from
  `_build_chains` materialising the whole window before the day loop: with no
  position in existence, cost basis has to be *bounded* rather than read, which
  is what widens the window and decays the A2 saving on long runs. Building
  chains per-day inside the loop, against the broker's real cost basis and
  real open-position strikes, would make the window tight again and remove the
  lookahead question entirely. Not attempted here — it changes the adapter
  contract and belongs in its own FC.
- Cache entries are keyed one file per `(symbol, date)`, so alternating between
  two pricing models re-fetches each time rather than keeping both. Not a
  concern for Track B (which sweeps *strategy* config, not chain model params).
- `tools/backtesting/coverage_report.py` and `tools/diagnostics/parity_check.py`
  were left unwired for the cache (out of scope); they inherit the strike
  window, and parity was measured unchanged as above.

---

## Track status (2026-07-29)

| Track | State | Where |
|---|---|---|
| **A1** ChainStore wired | **Done** | PR #49 `ac4d977` |
| **A2** strike-window filter | **Done** | PR #49 `ac4d977` |
| **B1** gap-filter A/B | **Done** | PR #56 — `docs/investigations/fc-002-gap-filter-ab.md` |
| **B2** premium-floor A/B | **Done** | PR #55 — `docs/investigations/fc-034-premium-floor-ab.md` |
| **C1** dividends | **Done** | PR #50 `18e71bb` |
| **C2** ex-div early assignment | **Done** | PR #50 `18e71bb` |
| **C3** spread-model calibration | **Partly done** | PR #48 `1e68a16` — estimator fixed; **RTH sample still pending** |
| **E1** FC-036 stage-4 gap gate | **Done** | PR #52 `44159d5` + `docs/plans/fc-036.md` |
| **E2** FC-035 poll | **Done (superseded)** | deleted, not fixed — PR #54 `ceaae16` |
| **D** owner infra | **Not started — owner only** | see `docs/plans/fc-032.md` |

### What the tracks actually found (short version)

- **A** delivered the intended speedup — warm re-run makes **zero** option-bar API
  calls (23 → 0; 21.0s → 0.2s). The strike window's benefit decays with window
  length (−49% at 1 month, −18.5% at 6 months) because chains are built eagerly
  before the day loop, so cost basis is *bounded* rather than read. **Lazy
  per-day building is DEFERRED**: a 2-year single-symbol cold run is 8m25s and
  warm is seconds, so the cache carries the studies; revisit only if a future
  study needs many cold long-window runs.
  A reviewer caught a BLOCKER the tests could not: `bid/ask/delta/iv` are
  *computed*, so the cache key needed a **model fingerprint** — without it C1
  and C3 would have silently served stale quotes to the B-track studies.
- **C** overturned this plan's own premise. Missing dividends do **not** bias
  income names toward `unfit`; the bias was *flattering* them. VZ collects $0
  even after C1, because it holds shares on ~0 of 481 days — its binding
  constraint is the premium floor (B2's question), not dividends. Crediting the
  benchmark too moved VZ's excess return +4.25% → −6.88%.
- **E1** shipped a corrected gap gate **unarmed**, plus evidence that arming it
  as designed would lose money. See `docs/plans/fc-036.md`; Phase B withdrawn.
- **E2** was superseded: the poll was deleted rather than fixed (`docs/plans/fc-035.md`).

### Outstanding

1. **B1/B2 studies** — in flight; they feed FC-002 and FC-034, which get their
   own plans + two reviewers before any live threshold moves.
2. **C3's RTH spread sample** — genuinely blocked on market hours, not on
   effort. The estimator defect (which would have had an operator commit
   `base_frac=0.0129` against a 0.05 default under a "calibrated" label) is
   fixed and merged; per-symbol fits span 19× (AAPL 0.0962, NVDA 0.0050) and
   pooled R²=0.04 is **not usable**, so C3 likely needs respecifying per-symbol
   regardless of when the RTH sample is taken.
3. **Track D** — owner infrastructure, unchanged.
4. Three FCs filed from review work along the way: **FC-045** (`/monitor` OCC
   misroute), **FC-046** (`trades_executed` view unqueryable), **FC-047**
   (`log_system_event` never sets `event_type`).

---

## Closeout (2026-07-29)

All tracks are closed. The engine is faster (A), more faithful on dividends and
early assignment (C), its spread estimator is honest (C3), and both filter
studies (B1/B2) are published. But **the two most consequential outputs of this
FC were not on its task list**, and both must be resolved before a screening
result drives any real decision.

### B2 → FC-034: **DEMOTE** (question answered)

The floor is not mis-shaped; the premium is not there. Median *richest* in-band
put per decision day over 275 days: **F $0.03, KMI $0.05, PFE $0.07, VZ $0.08**
vs AAPL $0.51 / AMZN $0.53. F's maximum across 173 days is $0.23 — it has never
offered a put clearing $0.50. F/PFE/KMI/VZ appear **zero times** in 330 real
fills over ten months. The proposed 0.4%-of-strike "re-shape" is in fact a
**tightening**: it would have blocked 47 of 330 real fills and destroyed $2,235
of realized P&L.

### B1 → FC-049: the question was unanswerable as posed

**The stage-2 gap-risk filter is not wired into the live trading path.**
`filter_stocks_by_gap_risk` is reachable only via `run_strategy_cycle()`, which
no deployed endpoint calls (`/run` moved to `OpportunityStore` +
`ExecutionEngine` in `842dcce`, three days before this account's first fill).
Every stage-2 block rate in FC-002 — and in this plan — describes the **backtest
engine**, not production. **Do not re-tune; decide first whether the control
should be wired in or deleted.**

Corollary already applied above: this plan's claim that the engine was validated
because its NVDA blocking "matches production" was a **false confirmation**.

### FC-048: every backtest to date models a **put-only** strategy

Covered-call opportunities from `evaluate_covered_call_opportunity` carry
`'strategy': 'sell_call'` but no `'type'` key, and `ExecutionEngine` routes on
`opp.get('type', 'put')` — so every covered call in replay is handed to
`put_seller` and rejected. Production is unaffected (it executes from the
scanner path, which types calls). **This is the single biggest threat to the
engine's credibility** and gates Track D: standing up the monthly screen while
backtests model half a wheel would automate demotion decisions on put-only
evidence.

### Verdict on this FC's own goal

Track A's speedup goal is met and the fidelity work landed. But **"engine that
answers our money questions" is not yet true** — it will be once FC-048 is fixed
and the affected verdicts are re-run. The studies' *real-fills* layers are
unaffected by FC-048 and stand on their own; their engine layers are put-only
and are labelled as such.
