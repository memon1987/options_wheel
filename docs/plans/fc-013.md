# Plan: Earnings Blackout Gate on the Live Sell Path (rev 2)

**FC entry:** `docs/FUTURE_CONSIDERATIONS.md` FC-013
**Plan file:** `docs/plans/fc-013.md`
**Status:** Rev 2.2 — window decision made (2026-08-03, operator: calls = true SPAN per-candidate, puts = N=2; step-0 audit EXECUTED; incident geometry corrected). **BUILD LAUNCHED.**
**Size:** M
**Author:** Claude (Fable)
**Last updated:** 2026-08-03 (rev 2.2 — **operator window decision, binding**: call leg = TRUE SPAN per-candidate (`expiration_date >= next_earnings_date`), put leg = N=2 days-until symbol-level. Informed by the step-0 audit and `docs/investigations/earnings-pop-callaway-2026-08-03.md` (`e13a144`). Incident geometry corrected — the AAPL call was a **day-of** sale, not the feared Monday N=3 shape; AMZN C262.5 joins the incident record ($2,308 surrendered, the live demonstration). DD-3 rewritten per-leg; call-side tests respecced for span; the rev 2.1 blocking open question is resolved.)

Rev 2.1 (2026-08-01) dispositioned two adversarial plan reviews (REQUEST_CHANGES × 2,
trader + reliability lenses; no reviewer-vs-reviewer disagreements; architecture
upheld): N/span acceptance criterion; enabled-but-no-key constructor crash fixed in
spec; fail-closed **and** alert (DD-6 rewritten — the "existing baseline" claim was
false); durable GCS earnings cache adopted; `EARNINGS_ENABLED` env kill switch;
replay honors `earnings.enabled`; audit SQL `side` predicate corrected;
earnings-table refresh + horizon report; operational numbers re-derived against the
real 6-scan/min-instances-0 topology.

---

## Rev 1 (superseded 2026-08-01)

Rev 1 (2026-04-29) responded to the GOOG incident by wiring the earnings gate into
`PutSeller.find_put_opportunity` and `CallSeller.evaluate_covered_call_opportunity`,
injected from `WheelEngine`. **Every one of those wiring targets is deleted by FC-068**
(`docs/plans/fc-068.md` §1–3): the engine builds no sellers, and both seller-side
opportunity producers are gone. Production trades via `/scan` → `OptionsScanner` →
GCS blob → `/run` → `ExecutionEngine` → the sellers' `execute_*` methods, and the
backtest now replays that exact pipeline.

**What survives from rev 1:** the `EarningsCalendarService` service-layer decisions
(Finnhub, caching, failure cache, `get_earnings_proximity` enrichment); the
fail-closed instinct (operator-reconfirmed at FC-069 sign-off); activating the
`earnings.blackout_days` knob; the test-matrix shape (gate fires / allows / blocks on
failure / no-op when disabled); the `docs/gates.md` deliverable.

**What rev 2 corrects in rev 1, beyond the deleted wiring targets:**

1. **Rev 1's fail-closed spec (§3) was buggy as written.** It flipped
   `is_earnings_within_n_days`'s error paths to return `True` — but that method's
   `days_until is None` branch conflates two different states: *"Finnhub fetch
   failed"* (`_fetch_earnings` returned `None`) and *"no earnings scheduled in the
   next 90 days"* (a successful fetch cached as `{"date": None}`). Flipping the
   `None` branch would have blocked every known-clear symbol — an account-wide
   brick on normal days. Rev 2 introduces a tri-state method instead (§Approach 2).
2. **Deleting `earnings_avoidance_days` moved to FC-069's sweep** (item 4 decision
   line, binding). This plan does not touch it.
3. **Rev 1's fail-closed flip changed the roller's behavior.** Operator decision
   (FC-069): the roller keeps its existing gate unchanged. Rev 2 confines the new
   semantics to the new gate; `is_earnings_within_n_days` is untouched.

---

## Context

**Three incidents on record, same root cause: no earnings gate has ever existed on
any path that trades.** Geometry corrected at rev 2.2 by the step-0 audit and the
earnings-pop investigation (`docs/investigations/earnings-pop-callaway-2026-08-03.md`):

- **GOOGL cash-secured put**, filled **2026-04-28**, earnings **2026-04-29** —
  `days_until = 1`.
- **AAPL covered call C347.5**, filled **Thursday 2026-07-30 15:15 ET**, hours
  before the AMC report that same day — `days_until = 0`, expiry 08-03 ≥ 07-30 so
  **spanning**. (Rev 2/2.1 assumed a Monday-of-that-week fill at `days_until = 3`;
  **that premise is falsified** — the only pre-earnings AAPL call of the week was
  the day-of sale. It happened to *profit* $165 because AAPL gapped **down** 8.5%
  — same trade shape as AMZN's, coin landed the other way.)
- **AMZN covered call C262.5** (added to the record at rev 2.2), filled
  **2026-07-30 10:15 ET**, morning of the AMC report — `days_until = 0`, expiry
  08-07 ≥ 07-30, **spanning**. AMZN gapped **+12.3% through the strike**;
  **$2,308 of upside currently surrendered** against $222 collected — the live
  demonstration of the gap-through-strike tail.

**The scorer is structurally attracted to the highest-risk day** (investigation §2):
earnings IV is what pushes marginal strikes over the premium floor — the AMZN
10.9%-OTM trade existed *at all* only because IV was 0.68. Cross-ref **FC-073**
(scorer/IV selection hazard).

Verified against the post-FC-068 tree (`origin/fc-068/engine-path-deletion`, tip
`c72127a` — the base this plan builds on): zero earnings references in
`src/data/options_scanner.py`, `src/strategy/put_seller.py`,
`src/strategy/call_seller.py`, `src/strategy/execution_engine.py`. The only earnings
consumers are the Friday roller (`CallRoller.should_roll` — the `earnings_blackout`
gate, structurally no-op'd by the quote-key bug, FC-066) and the backtest's roller
seam (`HistoricalEarningsCalendar`, threaded through `WheelEngine` in
`simulator.py`). The `earnings.*` knobs (`settings.yaml`: `enabled: true`,
`cache_ttl_hours: 24`, `blackout_days: 2`, `lookahead_days: 90`) read as live and
gate nothing that opens a position.

**Operator decisions (FC-069 item 4 sign-off 2026-08-01; window decision
2026-08-03 — binding, not relitigated here):**

- The earnings gate is a key part of the gating strategy. **REVIVE.**
- Gate **both** put opens and call opens. The roller keeps its existing gate.
- **Live and backtest coverage.** Post-FC-068 the simulator replays the scanner
  pipeline, so a scanner-layer gate applies in replays via the
  `HistoricalEarningsCalendar` seam — threaded explicitly (the FC-068-created
  obligation this plan discharges).
- Finnhub service, `FINNHUB_API_KEY`, and the `earnings.*` knobs stay.
- The dead `risk.gap_risk_controls.earnings_avoidance_days` knob dies in FC-069's
  sweep, not here.
- **Window semantics (2026-08-03): call leg = TRUE SPAN, per-candidate; put leg =
  N=2 days-until, symbol-level.** See DD-3.

**New findings from this re-plan (verified against `c72127a`; items 3–5 added at
rev 2.1 from the plan reviews):**

- **The 24h earnings cache is fictional at request scope — twice over.**
  `EarningsCalendarService` keeps `_cache` on the instance; `/scan` constructs a
  fresh `OptionsScanner` per request (`cloud_run_server.py`, `trigger_scan`). And
  the service runs with **min-instances=0** (`COST_OPTIMIZATION.md`; live-verify
  `gcloud run services describe` at build), so even a module-scope cache mostly
  dies between the hourly scans. §Approach 1 layers a durable GCS cache under a
  module-scope L1, which is what finally makes `earnings.cache_ttl_hours` real.
- **The FC-069 card's Finnhub-volume aside is backwards.** Card 4(c)(i) says
  scanner-side placement "spares Finnhub calls for symbols that produce no
  candidates" — in fact selection-side would need *fewer* lookups. The placement
  decision does not rest on call volume either way.
- **`EarningsCalendarService.__init__` crashes rather than degrades on a missing
  key** (rev 2.1, both reviewers): the early `return` on empty `finnhub_api_key`
  exits before `_enabled` / `_cache_ttl_hours` / `_lookahead_days` are set, so
  every subsequent method call raises `AttributeError` — which the scanner's broad
  `except` would misfile as `scan_failure`, producing zero opportunities *and zero
  unknown-events/rows*. §Approach 1 fixes the constructor; a named test pins the
  degraded path.
- **The committed earnings table goes horizon-dead if not refreshed** (rev 2.1,
  trader HIGH-3): the 2026-07-18 table had most reporters' last dates already in
  the past by 08-01, and horizon exhaustion is **silent** (a present symbol with
  only past dates answers `False` — allow; `symbols_without_data` fires only for
  *absent* symbols). §Approach 2 adds a horizon report. **Refresh executed
  2026-08-03** (step 0a, `fc-013/step0-audit` — generated via yfinance).
  (IWM/QQQ/SPY are present-with-empty by design — ETFs have no earnings.)
- **No alert policy matches any earnings event** (rev 2.1, reliability D2):
  `deploy/monitoring/` enumerated — `cost_basis_alert_policy.json` matches exactly
  two `call_scan_skipped_cost_basis_*` strings; `alert-policy.yaml` has empty
  `notificationChannels`. Rev 2's DD-6 claim that `earnings_fetch_failed` "flows
  through the existing FC-030 error-event alerting baseline" was **false**. DD-6
  is rewritten: fail-closed **and** alert.

## Goals

- One earnings gate, at the scanner, covering **both** legs: a put-side symbol
  within N=2 of earnings produces no put opportunities; a call candidate whose
  expiry spans earnings is never emitted, and a symbol whose whole candidate set
  spans is `blocked{earnings_blackout}`.
- The same gate object works in production (`EarningsCalendarService`, Finnhub) and
  in replays (`HistoricalEarningsCalendar`, committed point-in-time table) through
  one injected seam.
- **Fail closed on unknown, and loudly**: when the calendar cannot answer (Finnhub
  outage, missing key), the symbol is skipped on both legs — with events that
  distinguish "blocked by real earnings" from "blocked because we could not tell",
  **and a deployed alert policy on the unknown-state events** (DD-6).
- Every skip is legible: structured events on both legs, a P4 decision-record row on
  the call leg, replay-tally mappings on both legs, a criteria-stage rejection
  counter for span-rejected candidates.
- ~~The blackout window value is chosen by the operator on data~~ **Done
  (2026-08-03)** — and the chosen semantics demonstrably block all three incidents
  on record (DD-3).
- `docs/gates.md` ships: the live-path gate inventory FC-069 items 3/14 plan to
  point at.

## Non-goals

- Deleting `earnings_avoidance_days` (FC-069 sweep, binding).
- Unifying `rolling.earnings_blackout_days` with `earnings.blackout_days`, or any
  change to the roller's gate (§Design decision 5: FC-066's turf, item 13 HOLD).
- Per-symbol blackout windows (universe of 14; single global knob, as rev 1 decided).
- Closing existing positions ahead of earnings (conflicts with FC-010
  assignment-tolerance; the wheel takes assignment).
- Extending the P4 `decision_events` table to the put leg. That table is by design
  the per-held-symbol *covered-call* decision record; put-side legibility here is
  events + tally + the committed audit script (§Approach 6). A put-side decision
  table is its own FC if ever wanted.
- New alerting **beyond** the one earnings-gate policy DD-6 specs; a card-16
  detective check stays FC-069's turf.
- `/monitor` (closes reduce risk; never gated) and `/run` (gate is upstream, §DD-1).
- Roller revival or ITM-call management (FC-066; the investigation's §4/§5 roller
  findings are FC-066 input, not this FC's scope — FC-013 and FC-066 are
  complements: the gate prevents the unrescuable case, a revived roller would
  monetize the rescuable one).

---

## Design decisions

### DD-1 — Placement: scanner-side gate (both legs), not selection-side, not both

**Chosen:** the gate lives inside `OptionsScanner`. Rev 2.2 refines the call leg's
*granularity* — per-candidate span filtering in the criteria chain — but the
*placement* decision (scan stage, not selection, not both) is unchanged, and its
rationale carries over intact.

**Rejected — selection-side drop (`select_batch`, new `DROP_EARNINGS_BLACKOUT`):**

- `/run` has no earnings data and no natural place to get it: either
  `ExecutionEngine` grows its own Finnhub surface (a second copy of the outage
  problem, in the process that places orders), or the scan stamps proximity onto
  each opportunity and selection reads it — which is scan-time evaluation wearing a
  selection costume, plus blob plumbing.
- `DROP_REASONS` is FC-038's model for **batch-resource contention** — shares,
  buying power, duplicate-underlying — evaluated against *this batch*. Earnings is
  a policy fact known before ranking. Wedging it into the drop model misfiles it.
- Selection-side wastes the scan: chains fetched, opportunities built and ranked
  for candidates that cannot trade, plus (call leg) a BigQuery cost-basis
  cross-check spent on them.
- Observability is not better there: the call leg's drop would reach decision
  records via `/run`, but the scan-side gate reaches them too (§DD-4), and the put
  leg has no decision-record machinery on either side.

**Rejected — gate at both:** no incremental protection. Both predicates are
date-granular; between `/scan` and `/run` sit ≤30 minutes
(`opportunity_max_age_minutes`), inside market hours, never crossing midnight —
neither predicate can change value in that window. Cost: a second Finnhub
dependency surface and a second failure-semantics decision, for nothing.

**Finnhub volume / latency, derived against the real topology (rev 2.1 — rev 2's
numbers assumed ~13 scans/day and warm instances; production runs 6 scans/day,
hourly :00 from 10:00–15:00 ET, on min-instances=0, so instances are typically
reaped between hour-blocks and a module cache alone is cold nearly every scan):**

- The calendar is consulted once per put-side *suitable* stock (≤14) and once per
  call-side held symbol (≤7, overlapping) — ≤14 unique symbols per scan. The call
  leg's per-candidate span test reuses the **same single per-symbol date fetch**;
  candidate count adds zero Finnhub calls.
- **Without durable storage:** ~6 cold scans × 14 = **~84 Finnhub calls/day worst
  case**, each scan paying +2–6s of sequential fetches on the ~18.5s baseline —
  every scan, not once a day. "Cache hits, ~0ms" was a per-instance truth that is
  operationally false at min-instances=0.
- **With the durable GCS cache (§Approach 1, adopted):** ~14 fetches per 24h
  account-wide; the other five scans read one small GCS blob (~100–300ms). Finnhub
  free-tier limit is 60 calls/min; both shapes are well inside it.

**The NVDA-silent-exclusion lesson is honored, not waived:** a scanner skip here is
*never* silent — call leg writes a decision-record row when the gate terminates the
symbol (§DD-4), span-rejected candidates are counted in the stage-8 rejection
stats, both legs emit structured events, both events are tally-mapped for replays
(§Approach 5), and the unknown-state events are alerted (DD-6).

### DD-2 — Failure semantics: fail closed on unknown, via tri-state methods — plus the alert that bounds it

**Chosen: fail-closed, with a deployed alert on the unknown state (DD-6).** When
the calendar cannot answer for a symbol, that symbol is skipped for this scan, on
both legs — **the unknown skip is symbol-level on both legs, unchanged by the
rev 2.2 per-candidate call semantics** (a span test needs a date; no date means no
candidate can be cleared).

The options, weighed honestly:

- **Fail-closed + alert** (chosen — rev 2.1; rev 2 weighed only three options and
  under-stated the blast radius). A *transient* Finnhub outage at scan time means
  no opens account-wide for the affected scans — bounded and cheap: foregone
  7-DTE premium for part of a day, next scan retries after the 1h failure cache,
  and the durable cache (§Approach 1) means an outage only bites at all when the
  cached dates are >24h stale. But a **persistent** cause — key wiped by a
  `--set-env-vars` deploy, key revoked, Finnhub plan lapsed — blocks **all opens
  indefinitely, not for "one day"**. Nothing self-heals that; the alert is what
  bounds it. Hence the policy in DD-6 is a required deliverable of this FC, not an
  optional follow-up.
- **Fail-open-with-alert** (rejected): trades through outages ungated — the gate is
  silently absent exactly when its input is unverifiable. This codebase's signature
  disease is controls that read as live and fail open (`STATE_STORAGE_BUCKET`, the
  min-hold gate, the pre-FC-065 floor); FC-069 item 8's standing note exists
  because of it. An alert does not stop the trade.
- **Fail-closed-with-stale-cache-grace** (rejected as a *separate mechanism*, and
  rev 2's rejection rationale is corrected): rev 2 said a morning outage meets a
  cold cache "with nothing to be graceful with" — which is an argument **for
  durable storage, not against grace**. With the GCS cache adopted, the TTL itself
  *is* the grace: an entry fetched yesterday remains valid through today's outage.
  No second mechanism needed.

**Mechanism — do not flip the existing methods.** `is_earnings_within_n_days` stays
exactly as-is (the roller's gate, operator-decided unchanged; and its `None` branch
conflates "clear" with "unknown", rev 1's bug). New tri-state surface on both
calendar classes (rev 2.2: the date accessor is now primary, because the call leg
needs the *date*, not a within-N bool):

```
next_earnings_info(symbol) -> tuple[status, Optional[date]]
    ('known', date)   — earnings date within the lookahead
    ('clear', None)   — fetch succeeded, none within the lookahead (allow)
    ('unknown', None) — could not determine (fetch failed / no client / no key)
                        → gate skips the SYMBOL, both legs

earnings_within(symbol, n_days) -> Optional[bool]   # put leg; derived:
    'unknown' → None; 'clear' → False; 'known' → 0 <= (date − today) <= n_days
```

**Enabled-but-unusable degrades loudly — after the constructor fix (rev 2.1).**
As written today the no-key path does not degrade at all: it **crashes** (see
Context finding 3), and rev 2's claim that "every scan emits
`*_skipped_earnings_unknown`" described behavior the code could not produce. The
fixed contract (§Approach 1): construction with `earnings.enabled: true` and a
missing/unusable key initializes every attribute, logs one
`earnings_gate_unusable` error event, and leaves `_client = None`; thereafter
`next_earnings_info` returns `('unknown', None)` for every symbol, so every scan
skips every symbol with `*_skipped_earnings_unknown` events and
`(blocked, earnings_unknown)` rows — no exception, no `scan_failure` misfile. That
posture is FC-069 item 8's standing note: a configured-but-unresolvable dependency
must fail loudly, never silently no-op. `earnings.enabled: false` disables the
gate entirely — *disabled* and *broken* are different states with different
behavior, by design.

Fail-closed is decided, not a blocking operator question: the operator confirmed
the instinct survives (FC-069 card 4), and the blast radius — transient-bounded by
cache+retry, persistent-bounded by the alert — is restated in §Risks so sign-off
sees it.

### DD-3 — Window semantics: per-leg, decided (operator, 2026-08-03 — binding)

Informed by the step-0 exposure audit and the earnings-pop investigation
(`docs/investigations/earnings-pop-callaway-2026-08-03.md`, `e13a144`). The rev 2.1
single-knob-both-legs shape is superseded; the investigation's asymmetric-windows
analysis (§5) is the basis: the legs' exposures are mirror images but their costs
are not.

**Call leg — TRUE SPAN, per-candidate.** Block a call candidate when
`expiration_date >= next_earnings_date`, whenever earnings are known within the
lookahead. Candidates expiring **before** the event legitimately proceed.

- *Why span and not a numeric N:* any call whose expiry covers the report carries
  the full gap — AMZN C262.5 is the live demonstration ($2,308 surrendered on a
  +12.3% gap-through). A days-until N is only a proxy for span, and the operator
  **explicitly rejected hardcoding 7 or deriving N=DTE**: hardcoding manufactures
  a silent disconnect the day trading parameters extend beyond 7 DTE, and
  derive-from-DTE **over-blocks progressively at longer DTE** — a 30-DTE call
  sold a month out that *expires before the event* should be legal, and under
  N=DTE it isn't. Span is the risk predicate itself, DTE-invariant by
  construction.
- *Cost accepted, on the record:* span forfeits day-of/pre-event call premium —
  the juiciest of the cycle (~$100–250/symbol/week on this book) — **including
  the gap-down cushion**: AAPL's spanning C347.5 *profited* $165 on its −8.5%
  gap. The asymmetry is accepted because one gap-up pop costs ~$2,300 (AMZN,
  re-arm-adjusted ~$1,500–2,100) against a ~$100–200 cushion per gap-down —
  roughly 10:1 against the cushion. It also kills the adverse selection FC-073
  tracks: earnings IV is what makes the scorer *prefer* the day-of trade.
- *Lookahead invariant:* the span test is only meaningful for candidates expiring
  inside the calendar's lookahead (`earnings_lookahead_days: 90`). With call DTE
  ≤ 45 this is structurally covered; the build adds a config sanity assertion
  (`earnings_lookahead_days ≥ max call DTE + 7`) so a future DTE extension
  cannot silently outrun the calendar.

**Put leg — N=2 days-until, symbol-level.** Exactly as rev 2.1 specced: block
symbol S when `0 <= (next_earnings_date(S) − today) <= 2` in **calendar days**
(`blackout_days: 2`, now consumed by the put leg only).

- *Audit basis:* put-side realized-loss risk concentrates in **immediate
  pre-event entries** (the GOOGL 04-28 shape, `days_until = 1`), while
  wide-window pre-earnings put income was the book's **best bucket — 100% win
  rate, $301 average**. A wide put window would forfeit the best trades to block
  a risk that lives in the last two days.
- *Semantics carried from rev 2/2.1:* calendar days (trading-day semantics
  rejected — market-calendar dependency; weekend asymmetry stated and accepted);
  day-of (0) blocked; pre-earnings only — the service looks forward
  (`_from=today`), and post-earnings day-after is IV-crush territory the wheel
  should sell into, deliberately uncovered.
- *Why not span on puts too:* a 7-DTE put sold 5 days out spans the event, but
  the put leg's damage profile in ten months of fills is entry-proximity-shaped,
  not span-shaped (audit); and put-side "hold through the event" is the wheel's
  designed assignment-tolerance (FC-010). Divergent per-leg semantics are the
  deliberate choice here, not an oversight — the single-shared-knob simplicity
  argument was considered and rejected with the data on the table.

**Acceptance criterion — SATISFIED by the chosen configuration (geometry
corrected at rev 2.2).** The rev 2.1 criterion stands: the shipped gate must,
applied retroactively, block every incident on record. The audit corrected the
record: **GOOGL put `days_until = 1`; AAPL C347.5 `days_until = 0` AND spanning;
AMZN C262.5 `days_until = 0` AND spanning.** The old "AAPL Monday fill at
`days_until = 3`" premise — which drove rev 2.1's fear that N=2 fails the
flagship incident — **is falsified**; the only pre-earnings AAPL call of that
week was the day-of sale. Verification: puts-N=2 blocks the GOOGL incident
(1 ≤ 2); calls-SPAN blocks both call incidents (both expiries ≥ their event
dates). Test 7 encodes this corrected geometry, executable.

### DD-4 — Decision-record integration: two new reasons under `blocked`, schema untouched

- `REASON_EARNINGS_BLACKOUT = "earnings_blackout"` and
  `REASON_EARNINGS_UNKNOWN = "earnings_unknown"`, both added to
  `ALLOWED_REASONS[OUTCOME_BLOCKED]` in `src/data/decision_record.py`. Two reasons,
  not one: a row that says "earnings blackout" when Finnhub was down is a mislabel,
  and the closed vocabulary exists precisely so rows cannot misfile
  (`UnknownDecisionOutcome`'s reason for being).
- **Call-leg row shape under span (rev 2.2):** the `blocked{earnings_blackout}`
  row is written when the span predicate **empties the symbol's candidate set** —
  same row shape as the rev 2.1 symbol-level design, now meaning "every
  qualifying strike expired into the event". Candidates surviving the span filter
  proceed and the symbol terminates through the normal ladder. Partial span
  rejection (some candidates removed, set non-empty) is visible in the stage-8
  rejection stats (§Approach 3), not as a decision row — the row is the *symbol's*
  terminal state, and the symbol didn't terminate.
- **No new outcome** — `blocked` already means "a policy gate terminated this
  symbol at scan"; the floor reasons prove the shape.
- **No schema change.** `decision_events` columns are untouched; earnings detail
  (`next_earnings_date`, `days_until`, `earnings_hour`) rides the structured log
  event, not the record row. The `_LABEL_FIELDS` labels ride as usual. Rev 2.2
  note: the span-emptied row is written *after* basis resolution (the chain had
  to be fetched to know it emptied), so unlike the unknown-skip row it **does**
  carry `cost_basis_per_share`/`underwater` — matching the existing
  `no_candidates` row shape at that ladder position.
- Dedup key `(run_id, symbol, stage)` and the `sold`-wins ordering are unaffected.
- **Closed-enum tests:** `tests/test_decision_record.py`'s enum class gains
  `validate_outcome(OUTCOME_BLOCKED, REASON_EARNINGS_BLACKOUT/…UNKNOWN)` acceptance
  cases; the cross-pairing rejection test is extended to assert
  `validate_outcome(OUTCOME_DROPPED, REASON_EARNINGS_BLACKOUT)` still raises. No
  existing assertion weakens.
- **Cascading-impact check (docs/CLAUDE.md rule):** the dashboard reader is
  `uncovered_decisions_sql` (`dashboard/backend/services/bigquery.py:39`) —
  selects `reason` as a free string, additive-safe. The consuming card is
  `dashboard/frontend/src/components/v2/DrawdownPauseCard.tsx`, which renders
  `reason` free-form. Build step: confirm free-form rendering holds; add display
  labels for the two new reasons only if a mapping exists.

### DD-5 — Roller consistency: two knobs stay, unification is FC-066's

`rolling.earnings_blackout_days` (roller, N-based) and this gate's semantics
remain separate. Unifying now would reach into the roller's config surface while
FC-069 item 13 holds all `rolling.*` keys for FC-066's revival decision; this plan
honors that HOLD. FC-066's pre-revival checklist is the right home for the
unification question (and for adopting `next_earnings_info`/fail-closed — or span
semantics — on the roll path if FC-066 wants them). The investigation's roller
findings (§4: the as-built roller is structurally credit-only and cannot rescue
ITM calls; §5: a minimal defensive-roll scope) are **FC-066 input, cross-referenced
here, out of FC-013's scope.** Stated so the asymmetry is a recorded decision, not
an oversight.

### DD-6 — Alerting: one dedicated earnings-gate alert policy (rev 2.1 — rewritten; rev 2's premise was false)

Rev 2 leaned "no new alert" on the claim that `earnings_fetch_failed` error events
"flow through the existing FC-030 error-event alerting baseline." **Verified
false** (Context finding 5): no deployed policy matches any earnings event string,
and `alert-policy.yaml` carries empty `notificationChannels`. Without a policy, a
persistent unknown-state (key wiped/revoked) blocks all opens **indefinitely and
silently** — the exact "gate reads as protective, operator learns weeks later"
failure this plan exists to end, pointed the other direction.

**Deliverable:** `deploy/monitoring/earnings_gate_alert_policy.json`, modeled
verbatim on `cost_basis_alert_policy.json` (the FC-065 P1 precedent):

- **Match filter:** `jsonPayload.event_type` ∈
  {`put_scan_skipped_earnings_unknown`, `call_scan_skipped_earnings_unknown`,
  `earnings_gate_unusable`} OR the same three as `textPayload:` substrings —
  payload matched both ways, and **no severity clause** (Cloud Run captures stderr
  as plain text with EMPTY severity; a `severity>=WARNING` clause matches nothing —
  verified by the FC-030 fire drill 2026-07-18).
- **Channel:** the existing verified email channel the cost-basis policy uses.
- **Documentation content:** what the unknown state means (Finnhub outage vs key
  wiped vs plan lapsed), triage (`gcloud logging read` on the three event types;
  check `FINNHUB_API_KEY` on the live revision; check Finnhub status), and the
  reminder that fail-closed is correct behavior, not a bug to work around — plus
  the DD-7 env lever as the emergency response.
- **Not alerted:** `*_skipped_earnings_blackout` — a blackout skip is the gate
  working; normal operation, visible in decision records and events.

### DD-7 — Kill switch: env-var override, because baked-config rollback rides a pipeline that once sat red for 11 days (rev 2.1)

`earnings.enabled` lives in `settings.yaml`, baked into the image — "atomic
config rollback" in rev 2 actually meant commit → Cloud Build → deploy, behind the
same pipeline whose silent red build delayed FC-031 by 11 days. **Adopted:**
`config.earnings_enabled` honors an env override — `EARNINGS_ENABLED` (unset →
yaml value; set → parsed boolean wins). Disabling the gate in production becomes:

```
gcloud run services update options-wheel-strategy --update-env-vars EARNINGS_ENABLED=false
```

— one command, new revision in ~1 minute, no Cloud Build, no code commit.
(**`--update-env-vars`, never `--set-env-vars`** — the latter wipes the whole env
set, the documented gotcha that is itself one of this gate's persistent-failure
scenarios.) The yaml key remains the default and the documented home; the env var
is the emergency lever, documented in the alert policy's triage text.

### DD-8 — Replay semantics of `earnings.enabled`: policy follows config, injection supplies only data (rev 2.1)

Rev 2 left this ambiguous: as specced, an injected calendar always gated, so a
live rolloff (`enabled: false`) would leave replays gated — a live/replay
divergence on exactly the FC-048 fidelity axis. **Decided:** the scanner's gate
consults `config.earnings_enabled` **regardless of injection** — an injected
calendar substitutes the *data source* (Finnhub vs point-in-time table), never the
*policy*. `earnings.enabled: false` in the active config disables the gate in
production and in replays alike; a what-if replay with the gate forced on/off is a
config override, the same way every other knob is what-if'd. Pinned by test 16b.
**Noted, not changed:** the roller seam has the pre-existing inverse quirk
(`run_rolling_cycle` checks `earnings_enabled` only when constructing its own
service; an injected calendar bypasses the check) — recorded in `docs/gates.md` as
FC-066's inheritance, untouched here.

---

## Proposed approach

All file/line references against `origin/fc-068/engine-path-deletion` @ `c72127a`.
Symbol names are the contract; lines will drift at rebase.

### 1. `EarningsCalendarService` — tri-state methods, constructor fix, two-layer cache

`src/api/earnings_calendar.py`:

- **Constructor fix (rev 2.1 — required, both reviewers):** initialize **every**
  attribute (`_cache_ttl_hours`, `_lookahead_days`, `_enabled`, `_client = None`)
  *before* any early exit. The missing-key and failed-import paths no longer
  return a half-built object; they log one
  `log_error_event(error_type="earnings_gate_unusable", recoverable=True)` (when
  `earnings.enabled` is true) and leave `_client = None`. Every public method is
  callable on every construction path — the degraded service *answers*, it never
  raises.
- **Add `next_earnings_info(self, symbol) -> tuple[str, Optional[date]]`**
  (rev 2.2 — primary tri-state surface, DD-2):
  - `not self._client` → `('unknown', None)`.
  - Cached/fetched entry with `date is None` (successful fetch, nothing in the
    90d lookahead) → `('clear', None)`.
  - Entry with a date → `('known', date)`.
  - `_fetch_earnings` returned `None` (failure, incl. failure-cache window) →
    `('unknown', None)`.
- **Add `earnings_within(self, symbol, n_days) -> Optional[bool]`** — derived
  from `next_earnings_info` per DD-2's table (put leg's predicate).
- **Two-layer cache (rev 2.1 — durable layer adopted; see DD-1's topology
  numbers):**
  - **L1, module scope:** `_EARNINGS_CACHE: Dict[str, Dict]` and
    `_EARNINGS_FAILURE_CACHE: Dict[str, datetime]` at module level, plus
    `clear_earnings_cache()` for tests. Survives per-request scanner construction
    within an instance.
  - **L2, GCS blob:** one JSON object (`earnings_cache.json`, per-symbol entries
    `{date, hour, fetched_at}`) in the same bucket the opportunity store uses —
    read-through on L1 miss, write-through after any successful fetch batch.
    Entries respect the same `cache_ttl_hours` TTL. **GCS unavailability is a
    cache miss, never a block** — the ladder is L1 → L2 → Finnhub → (unknown);
    only Finnhub's answer (or its absence) determines gate state. Rationale for
    adopting: at min-instances=0 a memory-only cache is cold nearly every scan
    (≈84 calls/day, +2–6s per scan, and a morning outage meets an empty cache);
    the blob makes the 24h TTL real (~14 fetches/day) and shrinks the outage
    blast radius to "Finnhub down **and** cached dates >24h stale". Cost: one
    small GCS read per scan (~100–300ms) on an already-GCS-dependent path, and
    ~40 lines reusing the opportunity store's client/bucket plumbing.
  - **Concurrency posture, stated (reliability D12):** `/scan` runs under
    `strategy_lock`, so cache mutation is de facto serialized — the safety rests
    on that lock, recorded here so a future concurrent caller re-evaluates.
    Cheap hardening specced anyway (trader MEDIUM-7): eviction uses
    `.pop(symbol, None)`, never bare `del`, so a racing reader cannot KeyError.
- **Empty-200 ambiguity, stated and accepted with visibility (trader MEDIUM-6 /
  reliability D9):** a Finnhub 200 with an empty `earningsCalendar` is cached as
  known-clear (`{"date": None}`). Correct for ETFs (IWM/QQQ/SPY) and for genuine
  quiet windows — but a symbol-coverage regression on Finnhub's side would read
  as *permanently clear*: silent fail-open for that symbol. Accepted (the
  alternative — treating empty as unknown — bricks every ETF and quiet-window
  symbol). Visibility: the fetch's debug log gains `calendar_empty=True` on this
  path, and the audit script (§6) doubles as the retrospective detector — any
  fill inside a *table*-known window that Finnhub called clear shows up there.
  (The table is yfinance-sourced, the live service Finnhub — two independent
  providers, which is what makes the cross-check meaningful.)
- `is_earnings_within_n_days`, `get_next_earnings_date`, `get_earnings_proximity`:
  **unchanged behavior** (roller contract preserved verbatim).

### 2. `HistoricalEarningsCalendar` — same tri-state surface, plus the horizon report

`src/backtesting/engine/historical_earnings.py`. The table it serves is built by
`tools/backtesting/fetch_earnings_table.py` from **yfinance**
(`Ticker.get_earnings_dates`) — *not* Finnhub; the live service and the historical
table are deliberately independent sources. Refreshed 2026-08-03 (step 0a).

- **Add `next_earnings_info(self, symbol)`:** symbol present with an upcoming
  date → `('known', date)` (frozen-clock `_today()`); present with only past
  dates → `('clear', None)` **plus** `symbols_past_horizon.add(symbol)` (below);
  present-and-empty (ETFs) → `('clear', None)`; **absent** → `('clear', None)`
  **plus** `symbols_without_data.add(symbol)`. Add `earnings_within` derived the
  same way as the live service.
- **Horizon exhaustion becomes reportable (rev 2.1 — trader HIGH-3 / reliability
  D11):** a symbol *present* with only *past* dates otherwise answers exactly
  like known-clear — silent fail-open at the table's edge. Add
  `horizon_for(symbol) -> Optional[date]` (the symbol's last table date; `None`
  for absent/empty) and the `symbols_past_horizon` set. The simulator surfaces
  both sets in the run report: **a replay whose window end exceeds a traded
  symbol's horizon date is flagged like a data gap**, naming the symbol and its
  last covered date. ETF entries are excluded by the non-empty condition.
- **Replay fails open on table gaps, deliberately** — the one asymmetry with live,
  stated with its rationale: a replay "outage" is not a live risk, and returning
  `'unknown'` (block) for a symbol missing from the table would silently zero out
  that symbol's entire replay — a pessimistic measurement bias corrupting
  verdicts. The gap is *reported* (both sets above) so the operator refreshes the
  table instead of reading a biased verdict.
- Existing methods and the frozen-clock guard: unchanged.

### 3. `OptionsScanner` + `market_data.find_suitable_calls` — the gate, both legs

`src/data/options_scanner.py`:

- **Constructor:** add `earnings_calendar: Optional[object] = None` alongside
  `allow_bigquery` (same explicit-injection seam, same rationale block — a
  `clock.is_frozen()` keyed gate is rejected for the reasons already on the page
  at `__init__`). Resolution (DD-8 semantics):
  - `config.earnings_enabled` falsy → `self.earnings_calendar = None` (gate off),
    **even when a calendar was injected** — injection supplies data, config
    supplies policy;
  - enabled + param provided → use it (the simulator's path);
  - enabled + param `None` → `EarningsCalendarService(config)`.
- **Put leg (N=2 symbol-level, unchanged from rev 2.1)** — first check inside the
  per-suitable-stock loop of `scan_for_put_opportunities` (before
  `_has_existing_position`; both are network-or-cache checks, and gating first
  means the event count reads as the earnings exposure of the
  *post-`filter_suitable_stocks`* candidate set — the symbols that could
  otherwise have produced puts this scan, held or not; stage-1 rejects are
  upstream and never reach the gate). Predicate:
  `earnings_within(symbol, config.earnings_blackout_days)`; `None` → skip with
  `put_scan_skipped_earnings_unknown`; `True` → skip with
  `put_scan_skipped_earnings_blackout`; both events carry
  `blackout_days` + `get_earnings_proximity(symbol)` enrichment.
- **Call leg (rev 2.2 — per-candidate span):** in `scan_for_call_opportunities`,
  per held position:
  1. **Symbol-level unknown skip** stays where rev 2.1 put it — after the
     `shares < 100` not-eligible check, **before** cost-basis resolution (an
     unanswerable symbol should not spend a resolver call and a BigQuery
     cross-check): `status, e_date = next_earnings_info(symbol)`;
     `'unknown'` → `call_scan_skipped_earnings_unknown` event +
     `recorder.record(symbol, OUTCOME_BLOCKED, REASON_EARNINGS_UNKNOWN,
     **labels)` + continue. **There is no symbol-level blackout skip on the call
     leg any more** — a symbol reporting in 3 days may legally sell a call
     expiring in 2.
  2. **Span filter in the criteria chain:** thread the date into the chain the
     same way the floor rides —
     `market_data.find_suitable_calls(symbol, min_strike_price=basis,
     exclude_expiry_on_or_after=e_date if status == 'known' else None)`.
     `'clear'` symbols pass `None` and are unconstrained.
  3. **Emptied-set termination:** after the chain returns, if `calls` is empty
     **and** the span counter for this symbol is > 0 →
     `call_scan_skipped_earnings_blackout` event +
     `recorder.record(symbol, OUTCOME_BLOCKED, REASON_EARNINGS_BLACKOUT,
     **floor_labels)` (carries basis/underwater per DD-4) + continue. Empty with
     zero span rejections → the existing `no_candidates` path, untouched.

`src/api/market_data.py::find_suitable_calls`:

- New optional parameter `exclude_expiry_on_or_after: Optional[date] = None`
  (default `None` = today's behavior, byte-identical). In the per-contract
  criteria chain, a candidate with `expiration_date >= exclude_expiry_on_or_after`
  is rejected and counted under a new **`expires_into_earnings`** key in the
  published rejection stats (`last_call_rejection_stats` — the existing stage-8
  taxonomy the scanner already attaches to `no_candidates` rows via
  `reason_counts`, and that replay tallies read). This is the criteria-stage
  rejection counter the decision requires: partial span rejection is visible
  without a decision row, and an emptied set is attributable in `reason_counts`.
- **Behavior contract — what must NOT change:** opportunity dict shape (no new
  keys); `run_id` threading; `stamp_decision_labels` flow; the ordering and
  semantics of every existing ladder rung (`not_eligible` → [unknown skip] →
  divergent → unresolved → chain scan → [span-emptied termination] →
  candidates); `_has_existing_position` (FC-069 item 12's turf);
  `find_suitable_calls` with the param absent/`None` is byte-identical to today;
  scanner behavior when the gate is off (config-disabled) is byte-identical to
  today. `/scan`, CLI `--command scan`, and the simulator all construct through
  the same seams and get the gate consistently.

### 4. Wire the replay seam — `simulator.py`

`src/backtesting/engine/simulator.py`, the one-line obligation FC-068 created:

```python
scanner = OptionsScanner(client, market_data, self.config,
                         allow_bigquery=False,
                         earnings_calendar=self.earnings_calendar)
```

`self.earnings_calendar` is already the `HistoricalEarningsCalendar` (defaulted from
the committed table; constructor-injectable). The same instance keeps serving the
roller through `WheelEngine` — one point-in-time truth per replay. The run report
gains the §2 horizon/data-gap lines. Nothing else in the day loop changes.

### 5. Replay tally — `rejections.py`

Add to `_REASONS` (both legs; an entry with no live emitter is banned, and both
emitters are added in §3):

```python
"put_scan_skipped_earnings_blackout":  "earnings blackout (scan, put)",
"call_scan_skipped_earnings_blackout": "earnings span emptied the chain (scan, call)",
```

(Rev 2.2: the call-side label says what the event now means — the span predicate
removed every qualifying strike.) The `*_earnings_unknown` events are deliberately
**not** mapped: in a replay the historical calendar never returns unknown (§2 —
table-backed, fail-open on gaps), so a mapping would be an entry with no replay
emitter — the FC-068 §5 rule. Stated here so a reviewer doesn't "fix" it. Partial
span rejections surface through the `expires_into_earnings` key in
`reason_counts` on `no_candidates` rows, not through a tally event of their own.

### 6. The exposure audit — **EXECUTED** (step 0, 2026-08-03) — script stays a deliverable

The pre-build gate ran: table refreshed (yfinance, `fc-013/step0-audit`), audit
executed, findings distilled into
`docs/investigations/earnings-pop-callaway-2026-08-03.md` and fed the DD-3
decision. What remains for the build: commit the script as
`tools/diagnostics/fc013_earnings_exposure_audit.py` (reuse value: it is the
recurring detective query for "did anything trade inside a window", given the
log-sink to BQ is dead and events age out of Cloud Logging in 30 days), with the
incident-pinning and both predicates below preserved.

Mechanics: load the point-in-time calendar via
`HistoricalEarningsCalendar.from_table()` (the committed, **yfinance-sourced**
`src/backtesting/data/earnings_dates.json` — reproducible, no Finnhub
dependency), pull all option sell-to-open fills, join in pandas. **Environment
gotcha, learned in step 0:** BigQuery→dataframe reads require the `db-dtypes`
package — any environment running this script (or other BQ-dataframe tooling)
needs it installed or the read fails at result-materialization time.

The fills pull, verbatim (`side` predicate per `src/strategy/cost_basis.py:378` —
sell-to-open fills land as `sell_short`):

```sql
SELECT activity_id, symbol, underlying, option_type, side, qty, price,
       premium_total, strike_price, expiration,
       DATE(transaction_time, 'America/New_York') AS fill_date
FROM `options_wheel.trades_from_activities`
WHERE activity_type = 'FILL'
  AND option_type IS NOT NULL
  AND side IN ('sell_short', 'sell')
ORDER BY transaction_time
```

Per fill: `days_to_next_earnings = min({e − fill_date : e ≥ fill_date})` and
`spans_earnings = any(fill_date ≤ e ≤ expiration)` — the shipped predicates are
one column each (puts: `days ≤ 2`; calls: `spans_earnings`). Report per leg:
within-window counts for N ∈ {1, 2, 3, 5, 7}, span counts, realized P&L of
exposed positions vs book average (join `trades_with_outcomes`), and the pinned
incidents (GOOGL 04-28 put; AAPL C347.5 07-30; AMZN C262.5 07-30) — failing
loudly if any cannot be located.

### 7. `docs/gates.md`

The rev-1 deliverable, rebuilt for the post-FC-068 tree it will actually describe:
every gate on the live sell path (scan-stage: stock filter, position skip,
put-leg earnings window, call-leg earnings span, floor/divergence, quote
fail-closed; selection-stage: `DROP_REASONS` ladder, share ledger, buying power;
execution-stage: FC-050 floor, wrong-seller, naked-call), the roll path's gates —
including the pre-existing injected-calendar-bypasses-`enabled` quirk on the
roller seam (DD-8, FC-066's inheritance) — config keys and their env overrides
(DD-7), event names, decision-record outcomes, rejection-stat keys
(`expires_into_earnings`), alert coverage (DD-6), and test anchors — with the
FC-069 items 3/14 cross-references those cards expect (the emergent one-position
invariant with its open-order caveat verbatim, per item 3). One table per stage,
file:symbol references. FC-069 item 14's `docs/CLAUDE.md` rewrite will point here
rather than restating.

### 8. Config

No new yaml keys. `earnings.blackout_days: 2` becomes load-bearing as the **put
leg's** window (value decided 2026-08-03; the call leg's span semantics need no
knob — span is a predicate, not a tunable, and giving it a numeric knob would
reintroduce exactly the N=DTE disconnect the operator rejected).
`earnings.enabled` gates the scanner construction (§3, DD-8 semantics) and gains
the `EARNINGS_ENABLED` env override in `src/utils/config.py::earnings_enabled`
(DD-7). `earnings.cache_ttl_hours` becomes real (§1). `earnings.lookahead_days`
gains the DD-3 sanity assertion (≥ max call DTE + 7). `rolling.*` untouched.
`earnings_avoidance_days` untouched (FC-069's sweep).

### 9. Test hermeticity — `tests/conftest.py`

New autouse fixture `_no_finnhub`, mirroring `_no_production_bigquery`'s pattern
and placement:

- **Pin the env var first (rev 2.1 — reliability D3; without this the fixture is
  unbuildable as a hermeticity guarantee):**
  `monkeypatch.setenv("FINNHUB_API_KEY", "test_finnhub_key")` —
  **unconditionally**, exactly like `_deterministic_alpaca_credentials`.
  `Config.finnhub_api_key` reads `os.getenv` directly and `Config.__init__` calls
  `load_dotenv()`, so without the pin a developer's real key leaks into tests and
  CI diverges from dev — the ambient-environment defect this suite has been bitten
  by three separate times.
- `monkeypatch.setattr(EarningsCalendarService, "_fetch_earnings", lambda self,
  symbol: {"date": None, "hour": None, "fetched_at": datetime.now()})` —
  class-level chokepoint; every instance, including ones a test builds directly,
  answers "known clear" and never touches the network.
- **Patch the L2/GCS layer to a no-op** the same way (class-level; the blob
  read/write helpers) — no test touches GCS, same principle as the opportunity
  store's test handling.
- `clear_earnings_cache()` before **and** after each test — module-scope cache must
  never leak entries across tests (the `_reset_time_seam` precedent).
- Escape hatch marker `real_finnhub_fetch` for the service's own unit tests that
  exercise `_fetch_earnings` (they mock the finnhub client object, still no
  network — the marker exists so the chokepoint patch doesn't make them vacuous,
  the `real_bq_lookup` lesson).
- Tests that exercise *gate behavior* need no marker: they inject a fake calendar
  (or a `HistoricalEarningsCalendar` under a frozen clock) via the constructor
  param, which the class-level patch doesn't touch.

---

## Critical files

- `src/api/earnings_calendar.py` — constructor fix (all attributes on every path;
  loud `earnings_gate_unusable`); `next_earnings_info` + `earnings_within`
  tri-state; two-layer cache (module L1 + GCS L2) + `clear_earnings_cache()`;
  `.pop`-hardened eviction; `calendar_empty` fetch-log field. Existing
  roller-facing methods unchanged.
- `src/backtesting/engine/historical_earnings.py` — `next_earnings_info` +
  `earnings_within`; `horizon_for` + `symbols_past_horizon` (horizon report);
  fail-open-with-report on missing symbols.
- `src/backtesting/data/earnings_dates.json` — **refreshed 2026-08-03**
  (yfinance-sourced via `tools/backtesting/fetch_earnings_table.py`;
  `fc-013/step0-audit`); merge/rebase carries it.
- `src/data/options_scanner.py` — `earnings_calendar` constructor param (DD-8
  config-wins semantics); put-leg N=2 gate in `scan_for_put_opportunities`;
  call-leg unknown skip + span threading + emptied-set termination in
  `scan_for_call_opportunities`.
- `src/api/market_data.py` — `find_suitable_calls` gains
  `exclude_expiry_on_or_after` param + `expires_into_earnings` rejection-stat key
  (rev 2.2).
- `src/data/decision_record.py` — `REASON_EARNINGS_BLACKOUT`,
  `REASON_EARNINGS_UNKNOWN` under `OUTCOME_BLOCKED`.
- `src/utils/config.py` — `earnings_enabled` env override (`EARNINGS_ENABLED`);
  lookahead sanity assertion.
- `src/backtesting/engine/simulator.py` — pass `earnings_calendar` to the scanner;
  surface the horizon/data-gap report lines.
- `src/backtesting/engine/rejections.py` — two tally mappings.
- `deploy/monitoring/earnings_gate_alert_policy.json` — **new** (DD-6; cost-basis
  policy as the template, no-severity-clause pattern, existing email channel).
- `tests/conftest.py` — `_no_finnhub` autouse fixture (env pin + fetch chokepoint +
  GCS no-op + cache clear).
- `tests/test_earnings_calendar.py` — **new** (the still-missing FC-007
  deliverable).
- `tests/test_options_scanner.py`, `tests/test_market_data.py` (span criteria),
  `tests/test_decision_record.py`, `tests/test_backtest_earnings.py` or
  `tests/test_backtest_simulator.py`, `tests/test_backtest_rejections.py` — per
  §Tests.
- `tools/diagnostics/fc013_earnings_exposure_audit.py` — **new** (the step-0
  script, committed; recurring detective query; incident-pinning per §6).
- `docs/gates.md` — **new**.
- `deploy/cloud_run_server.py`, `main.py` — **no changes** (default-construction
  path covers both; the PR diff to both files must be empty — reviewer checklist
  item, FC-068's pattern).

## Reuse

- `EarningsCalendarService` fetch/failure-cache/proximity machinery — kept whole;
  the cache's scope, the tri-state surface, and the constructor's degraded path
  change.
- `HistoricalEarningsCalendar`'s frozen-clock guard, window arithmetic, and
  `symbols_without_data` reporting (extended, not replaced, by the horizon
  report).
- `find_suitable_calls`'s existing optional-criteria pattern (`min_strike_price`)
  and its published rejection-stats taxonomy — the span filter and its counter
  join both rather than inventing a parallel mechanism.
- The simulator's existing `earnings_calendar` constructor seam (built for the
  roller; now serves the scanner too).
- The `allow_bigquery` explicit-injection pattern and its documented rationale.
- The opportunity store's GCS client/bucket plumbing (L2 cache blob).
- `cost_basis_alert_policy.json` — the alert-policy template (filter shape,
  no-severity lesson, channel).
- `DecisionRecorder` / `OUTCOME_BLOCKED` machinery; `_no_production_bigquery`'s
  class-level chokepoint + marker-escape-hatch pattern;
  `_deterministic_alpaca_credentials`' unconditional env-pin pattern.
- `log_error_event` / structured-event conventions (`call_scan_skipped_*` naming
  family).

## Tests — each named with the regression it catches

**Put leg (N=2, symbol-level):**

1. **`test_put_scan_skips_symbol_in_earnings_blackout`** (`test_options_scanner.py`)
   — fake calendar: symbol A earnings tomorrow, symbol B clear; scan emits no put
   opportunity for A, does for B; `put_scan_skipped_earnings_blackout` emitted with
   `days_until`. **Must fail with the gate reverted** (mutation-check in the PR, per
   P1/P2 convention). *Regression: the GOOGL incident (`days_until = 1 ≤ 2`).*
2. **`test_put_window_boundary`** — parametrized `days_until` ∈ {0, 2, 3}:
   blocked / blocked / clear. *Regression: off-by-one on the inclusive window.*

**Call leg (TRUE SPAN, per-candidate — respecced at rev 2.2):**

3. **`test_call_candidate_spanning_earnings_is_rejected`**
   (`test_market_data.py` + scanner integration) — earnings at D+5; candidate
   expiring D+7 is rejected with the `expires_into_earnings` counter
   incremented; sibling candidate expiring D+3 survives the chain. **Mutation
   check: reverting span to a days-until window (e.g. `days_until ≤ 2`) lets the
   D+7 candidate through (days_until = 5) → test FAILS.** *Regression: span
   semantics silently degraded to a numeric window.*
4. **`test_call_candidate_expiring_before_earnings_is_allowed`** — the
   DTE-extension pin: earnings at D+5, candidate expiring D+3 → **allowed**,
   opportunity emitted. **Must FAIL under a hardcoded N=7 or derive-N=DTE
   implementation** (both would block a symbol at `days_until = 5`). *Regression:
   the over-blocking the operator explicitly rejected — a candidate that expires
   before the event must stay legal at any DTE.*
5. **`test_span_boundary_expiry_on_earnings_day_is_blocked`** — candidate
   `expiration_date == next_earnings_date` → rejected (`>=` is inclusive:
   assignment resolves after the report, so expiry-day-equals-event-day carries
   the gap). *Regression: `>` vs `>=` off-by-one reopening the day-of window.*
6. **`test_call_set_emptied_by_span_writes_blocked_row`** — all qualifying
   strikes expire into the event: decision row
   `(blocked, earnings_blackout)` with floor labels, event
   `call_scan_skipped_earnings_blackout`, **not** a `no_candidates` row;
   `expires_into_earnings` present in the symbol's rejection stats. *Regression:
   the span-emptied symbol going silent — the NVDA-silent-exclusion failure
   mode.*
7. **`test_the_incident_geometry_is_blocked`** (respecced — the corrected
   record) — fake calendar with earnings **today** (`days_until = 0`, the actual
   AAPL/AMZN geometry; the rev 2.1 "Monday `days_until = 3`" premise is
   falsified and no longer encoded): a 7-DTE candidate expiring after the event
   is rejected under span. **Must fail if span is reverted.** *Regression: the
   AMZN $2,308 / AAPL day-of incidents recurring.*
8. **`test_clear_symbol_candidates_unconstrained`** — `('clear', None)`:
   `find_suitable_calls` receives `exclude_expiry_on_or_after=None`; chain output
   byte-identical to pre-gate behavior. *Regression: the gate constraining
   symbols with no known earnings.*

**Shared / failure semantics:**

9. **`test_earnings_gate_fails_closed_on_unknown`** — calendar returns
   `('unknown', None)`: symbol skipped on **both** legs (the symbol-level
   contract, unchanged by per-candidate span), `*_skipped_earnings_unknown`
   events (not `_blackout`), call-side row `(blocked, earnings_unknown)`, and on
   the call side **the cost-basis resolver was never called** (unknown skip sits
   before the resolver). *Regression: outage silently failing open; ladder
   ordering.*
10. **`test_enabled_with_no_key_degrades_loudly_not_crash`** (rev 2.1 — both
    reviewers) — `earnings.enabled: true`, `FINNHUB_API_KEY` empty,
    **default-constructed** service (no injection): construction succeeds,
    `earnings_gate_unusable` error event logged, a scan completes without
    exception, every symbol skipped with `*_skipped_earnings_unknown` events and
    `(blocked, earnings_unknown)` rows — and **no `scan_failure` event**.
    *Regression: the half-built-constructor AttributeError being swallowed as
    `scan_failure` with zero gate telemetry.*
11. **`test_gate_absent_when_earnings_disabled`** — `earnings_enabled` falsy: no
    service constructed, scanner output byte-identical to pre-gate behavior.
    **11b. `test_env_override_disables_the_gate`** (DD-7) — yaml `enabled: true`
    + `EARNINGS_ENABLED=false` env: gate off; env unset: yaml wins. *Regressions:
    rollback lever not reverting behavior; the emergency lever losing to baked
    config.*
12. **`test_next_earnings_info_tristate`** (`test_earnings_calendar.py`, new
    file) — known/clear/unknown across: date present; empty-calendar fetch;
    fetch failure; failure-cache window; no client. `earnings_within` derived
    correctly (0/N/N+1 → True/True/False; clear → False; unknown → None). **The
    clear-vs-unknown distinction is the load-bearing case.** *Regression: rev 1's
    conflation bug — fail-closed blocking every clear symbol.*
    **12b. `test_cached_dates_answer_correctly_across_days`** (frozen clock) —
    entry fetched on day D with earnings at D+3, put window 2: queried on D →
    clear; on D+1 (same cached entry, within TTL) → blocked; and the *span* test
    against a candidate expiring D+4 blocks on both days. *Regression: caching a
    computed `days_until`/verdict instead of the date.*
13. **`test_earnings_cache_is_shared_across_instances`** — instance 1 fetches;
    instance 2 (fresh, same module) answers from L1 with zero fetch calls; TTL
    expiry refetches. **13b. `test_l2_blob_survives_a_cold_start`** — L1 cleared
    (simulated instance death), L2 populated: fresh instance answers from the
    blob with zero Finnhub calls; L2 read failure falls through to Finnhub
    (never blocks). *Regression: reverting to ~84 Finnhub calls/day; GCS trouble
    bricking the gate.*
14. **`test_roller_gate_behavior_unchanged`** (`test_call_roller.py`, additive) —
    `is_earnings_within_n_days` still fails open on proximity-`None`;
    `should_roll` earnings gate cases pass untouched. *Regression: the new
    semantics leaking into the roller against the operator's decision.*

**Replay:**

15. **`test_replay_uses_the_point_in_time_calendar_never_finnhub`**
    (`test_backtest_earnings.py`) — mutation-style: patch
    `EarningsCalendarService.__init__` to raise; run a short replay whose table
    places symbol X's earnings such that day D's put scan is inside N=2 **and**
    the call chain's expiries span the event; assert (i) no put opened on X on
    D, (ii) no spanning call emitted, (iii) X trades on a clear day, (iv) the
    live service was never constructed. **Fails two ways by design:** simulator
    stops passing the calendar (scanner default-constructs → raise) or the gate
    is reverted (trade opens on D). *Regression: the FC-068-created seam
    obligation, and replays asking Finnhub about the past.*
16. **`test_replay_reports_symbols_missing_from_the_table`** — symbol absent from
    a constructor-injected calendar: trades proceed (fail-open),
    `symbols_without_data` contains it. **16b.
    `test_replay_honors_earnings_enabled_false`** (DD-8) — config with
    `enabled: false` + injected calendar: gate off in the replay (mirrors a live
    rolloff). **16c. `test_replay_flags_horizon_exhaustion`** — replay window end
    past a present symbol's last table date: `symbols_past_horizon` contains it
    and the report surfaces it; an ETF (present-with-empty) does **not** appear.
    *Regressions: a table gap silently zeroing a symbol's replay; live/replay
    policy divergence; silent fail-open at the table's edge.*
17. **Enum acceptance/rejection** (`test_decision_record.py`) — new pairs
    accepted; `(dropped, earnings_blackout)` and `(no_candidates,
    earnings_unknown)` still raise. *Regression: the closed enum silently
    widening.*
18. **Tally mapping** (`test_backtest_rejections.py`) — a
    `put_scan_skipped_earnings_blackout` event becomes a named reason (must fail
    on today's mapping); `call_scan_skipped_earnings_blackout` maps to the span
    label. *Regression: the FC-057 invisible-constraint failure mode.*
19. **Hermeticity** — `_no_finnhub` fixture: `FINNHUB_API_KEY` pinned
    unconditionally (a real key in the developer's `.env` must be invisible to
    every test); a default-constructed service answers known-clear with zero
    network and zero GCS; cache cleared between tests. *Regression: the
    three-times-bitten ambient-environment defect.*

Validation: full `pytest tests/ -v` green; mutation checks for tests 1, 3, 4, 7
and 15 pasted into the PR description.

## Risks & mitigations

- **Fail-closed unknown-state: transient vs persistent, stated honestly
  (rev 2.1).** *Transient* Finnhub outage: bounded — with the L2 cache, only
  symbols whose cached dates are >24h stale block; 1h failure cache retries;
  foregone 7-DTE premium for part of a day. *Persistent* cause (key wiped/revoked,
  plan lapsed): **blocks all opens indefinitely** — bounded only by the DD-6
  alert, which is why the alert policy is a required deliverable, and by the DD-7
  env lever for the operator's response.
- **Span forfeits the gap-down premium cushion (rev 2.2 — residual accepted, on
  the record):** blocking pre-event spanning calls also blocks the trades that
  *profit* when the gap goes down — AAPL's spanning C347.5 made $165 on its
  −8.5% gap; GOOGL's two July spanners made $584 combined. Accepted because the
  asymmetry is ~10:1 the other way: one gap-up pop costs ~$2,300 gross
  (~$1,500–2,100 re-arm-adjusted) against a ~$100–200 cushion per gap-down, and
  the cushion trades are exactly the IV-bait the scorer over-selects (FC-073).
- **Deploying near an earnings date with a cold cache** (rev 1's GOOGL risk, still
  real): first scan after deploy pays up to ~14 Finnhub fetches; a transient
  hiccup then blocks symbols as unknown. Mitigation: deploy outside market hours;
  the post-deploy verification scan confirms the L2 blob is populated and events
  are the expected type before the next scheduled scan.
- **`/scan` latency**: worst case (cold L1+L2, first scan after deploy) +2–6s of
  sequential fetches on the ~18.5s baseline; steady state one L2 read
  (~100–300ms) per scan and ~14 fetches/day account-wide. The per-candidate span
  test adds zero network (one date per symbol, reused). Verified in the
  post-deploy scan. (Numbers derived against the real 6-scan/min-instances=0
  topology — DD-1.)
- **A blocked symbol every scan through an earnings week reads as "scanner broke"
  to a future reader.** Mitigation: decision rows + events name the reason;
  `expires_into_earnings` appears in rejection stats; `docs/gates.md` documents
  the gate; the dashboard reason string renders free-form
  (`DrawdownPauseCard.tsx` — build-step check, DD-4).
- **Base-branch drift**: this builds on `fc-068/engine-path-deletion` pre-merge. If
  FC-068's confirmation-pass fixes touch the scanner, rebase re-verifies §3's
  ladder placement (the gate sits between named rungs — symbol names, not lines,
  are the contract).
- **Finnhub data quality** (wrong/moved dates; empty-200 coverage gaps — §1's
  accepted fail-open): unchanged risk class from FC-007; 24h TTL bounds staleness;
  `calendar_empty` field + the audit script (independent yfinance table) are the
  retrospective detectors.
- **GCS cache blob as a new dependency**: read failure falls through to Finnhub
  (test 13b); the blob can never block a trade by itself — only Finnhub's answer
  or its absence can.

## Rollout

1. **Branch off `origin/fc-068/engine-path-deletion`** (this plan's base); when
   FC-068 merges, **rebase onto main**. Own PR — one PR per FC; FC-068's PR is not
   extended. Carry the refreshed `earnings_dates.json` from `fc-013/step0-audit`.
2. ~~Build step 0 (pre-wiring gate)~~ **DONE 2026-08-03**: table refreshed
   (yfinance), gcloud auth restored, exposure audit executed, window decision
   taken (DD-3 — calls SPAN, puts N=2) under the acceptance criterion, all three
   incidents verified blocked. Findings:
   `docs/investigations/earnings-pop-callaway-2026-08-03.md` (`e13a144`).
3. Implement §1–§9; full suite green; mutation checks for tests 1, 3, 4, 7, 15
   recorded.
4. Two adversarial reviews (Fable, fresh contexts, distinct personas — one with
   production-data access to verify the audit script's claims against live
   `trades_from_activities`), disposition, confirmation pass if fixes land — the
   standard gate.
5. **Deploy after FC-068's deploy, outside market hours.** Deploy the DD-6 alert
   policy in the same window (`gcloud alpha monitoring policies create` per the
   cost-basis policy's runbook); verify the policy is ACTIVE and its channel is
   the verified email.
6. Post-deploy verification, in order:
   - trigger one `/scan`; confirm duration within expectations, zero
     `*_earnings_unknown` events (Finnhub healthy), and the L2 blob populated;
   - **live-fire check, both legs** — the calendar cooperates: **NVDA reports
     2026-08-26**, so from **08-19** every ~7-DTE NVDA call candidate spans the
     event (the investigation flagged NVDA as "the next live test") and from
     08-24 the put leg is inside N=2. Verify: span-rejected NVDA call candidates
     appear in `expires_into_earnings` stats (or the
     `(blocked, earnings_blackout)` row if the chain empties); put-side blackout
     event within the window; the symbol absent from the opportunity blob. If
     deploying well before 08-19: any universe reporter within its window
     substitutes;
   - confirm a clear symbol still produces opportunities and that a
     known-earnings symbol still sells a call expiring **before** its event
     (span's allowed case — the gate must not over-block);
   - **alert fire drill**: emit one synthetic `earnings_gate_unusable`-matching
     log line (the FC-030 drill pattern) and confirm the email arrives.
7. Monitor one trading week: zero option sell-to-open fills violating either
   predicate (the audit script re-run is the check); `earnings_fetch_failed`
   events at baseline; no unknown-state alerts.
8. **Rollback, two levers (rev 2.1):** *emergency:*
   `gcloud run services update options-wheel-strategy --update-env-vars
   EARNINGS_ENABLED=false` — runtime-atomic (~1 min, no Cloud Build; never
   `--set-env-vars`). *Durable:* flip `earnings.enabled: false` in yaml through
   the normal pipeline — honest latency: commit → Cloud Build → deploy, and the
   pipeline has sat silently red before (FC-031's 11 days); the env lever exists
   because of exactly that. Test 11 proves config-off restores pre-gate behavior
   byte-identically. Full code rollback: single revert of the squash commit (no
   schema, no data migrations — the two enum reasons are additive strings; old
   rows unaffected; the L2 blob is inert to old code).

## Open questions

- ~~BLOCKING: the blackout window value~~ **RESOLVED 2026-08-03** — operator
  decision, DD-3: call leg TRUE SPAN per-candidate, put leg N=2. Audit +
  investigation on the record; acceptance criterion verified against the
  corrected incident geometry. No blocking open questions remain.
- Non-blocking: commit the audit script to `tools/diagnostics/` (recommended — it
  is the recurring detective query now that events age out of Cloud Logging in 30
  days) vs leave as a scratch probe. Plan assumes commit.
- Non-blocking: whether FC-066 adopts `next_earnings_info`/fail-closed (or span
  semantics) on the roll path and unifies the knobs — handed to FC-066's
  pre-revival checklist (DD-5), along with the roller seam's
  injected-calendar-bypasses-`enabled` quirk (DD-8) and the investigation's
  §4/§5 roller findings.

## Execution

_Filled in after implementation is complete._

- **PR:** TBD
- **Commit:** TBD
- **Date:** TBD
- **Notes:** TBD
