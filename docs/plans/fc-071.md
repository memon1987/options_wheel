# Plan: FC-071 — at-floor scoring bonus aligned to the gate (`>=`)

**FC entry:** `docs/FUTURE_CONSIDERATIONS.md` FC-071
**Plan file:** `docs/plans/fc-071.md`
**Status:** Approved by operator 2026-08-02 ("close out FC-071" — align per standing
recommendation). Built 2026-08-02 on `fc-071/at-floor-scoring`; dual-APPROVE reviews; merged
post-train main and applied the reviewers' cosmetic batch 2026-08-03. **PR opening
2026-08-03**, after the Monday train (see `## Execution`).
**Size:** S (one predicate + tests)
**Author:** Claude (Fable), 2026-08-02

## Context

FC-065 Phase 2 (PR #76) fixed the `assignment_above_cost_basis` *flag* to `>=`, matching all
three safety gates (`market_data.py:447`, seller execute-time check, `risk_manager.validate_roll`):
an at-floor call — strike exactly equal to Alpaca `avg_entry_price` — is allowed and genuinely
profitable (both premiums kept, no capital given up; GOOGL's 370C on a 370.00 floor was the live
example). The *scoring* input was deliberately left for an operator decision (PR #76 review,
INFO finding): `_create_call_opportunity` still passes strict `strike > cost_basis_per_share`
into `_calculate_call_attractiveness_score`'s `above_cost_basis` parameter
(`src/data/options_scanner.py:426` at time of filing), so an at-floor candidate is *flagged*
at-or-above but *scored* below-basis (5 pts instead of 15 — a 10-point swing in a 0–100 score).

**Operator decision (2026-08-02): align.** Gate, flag, and score should agree that at-floor is good.

## The change

- **One predicate:** the `above_cost_basis` argument passed into
  `_calculate_call_attractiveness_score` becomes `strike >= cost_basis_per_share`
  (same comparison the flag at ~`:451` already uses; reuse the already-computed flag value
  rather than writing the comparison twice, if the code shape allows — one source of truth).
- **Nothing else.** The scoring weights themselves are FC-073's territory. The gates are
  untouched. Put-side scoring has no basis component (nothing to symmetrize).

## Behavior contract

- At-floor call candidate (`strike == floor`): `above_cost_basis` scoring input True → +15 not +5.
  Net effect: at-floor candidates rank 10 points higher, consistent with their flagged status.
- Above-floor: unchanged (was True, stays True). Below-floor: unchanged (False → +5) — and still
  **blocked** at the gates regardless of score; scoring below-floor candidates is moot in practice
  but the parameter contract is preserved.
- Must NOT change: the floor gates, the flag, `attractiveness_score` for any non-at-floor
  candidate, opportunity dict shape, blob schema.
- Edge realism: with premium-netted floors (e.g. 368.34) on $2.50 grids, exact equality is
  near measure-zero — this is consistency hygiene, not a behavior shift on today's book.
  Verified consequence is confined to future exact-equality candidates.

## Tests

1. At-floor candidate receives the 15-pt bonus (scoring-level or opportunity-level assert).
   **Mutation-verify:** revert the predicate to strict `>` → test must fail; record in PR.
2. The flag and the scoring input agree at equality (pins the alignment itself, so the two
   predicates cannot drift apart again — assert both derive from one comparison if refactored
   to a single source).
3. Existing suite green (no other assertions move — a strict-`>` fixture candidate above floor
   scores identically).

## Rollout

Branch + PR per house rules (production behavior change → two adversarial Fable reviews, fresh
contexts, distinct personas; confirmation pass if fixes land). **Merge slot: after the Monday
2026-08-03 train (PR #76 → P4 → FC-068)** — the change targets scanner code that survives
FC-068; rebase onto post-train main before opening the PR to keep the diff exact. Deploy rides
the next Cloud Build; verification = next live scan shows unchanged strike sets (no at-floor
candidate expected on today's book) and the suite's mutation-backed pins. Rollback: single revert.

## Execution

**Status: built, merged with post-train main, pushed; PR opening 2026-08-03.**

- **Branch:** `fc-071/at-floor-scoring`
- **Built on:** `fc-068/engine-path-deletion` @ `c72127a` (post-FC-068-review tip), 2026-08-02.
- **Merged post-train `main`** (`3e5b0e8`) on 2026-08-03, once PR #76 (P2), #78 (P4), #79
  (FC-068) and FC-075 Phase 1 (#77) had all landed. Conflicts and their resolutions are
  recorded under *Merge record* below.
- **Build commit:** `46f7a28`. Merge + cosmetic-batch commit: see backfill at PR time.
- **Date built:** 2026-08-02

### What was built

Single source of truth, as the plan preferred — the code shape allowed it cleanly. One
comparison is now computed once, above the scoring call:

```python
assignment_above_cost_basis = strike >= cost_basis_per_share
```

It feeds **both** the `above_cost_basis` argument of `_calculate_call_attractiveness_score`
(was `strike > cost_basis_per_share`) and the `assignment_above_cost_basis` key on the
opportunity dict (was a second, separately-written `strike >= cost_basis_per_share`). Two
predicates became one, so they cannot drift apart again. `src/data/options_scanner.py`, one
functional line; the FC-065 P2 comment block moved up with the hoisted comparison and gained
the FC-071 rationale.

**Deliberately not touched:** the `strike > cost_basis_per_share` branch at `:595` that splits
capital gain from capital loss. At exact equality both arms compute
`total_return_if_assigned == premium_income` (gain and loss are each 0), so it is already
equality-correct and lies outside FC-071's one-predicate scope. Scoring weights (FC-073),
the gates, and put-side scoring are untouched.

### Tests

Three new tests (6 cases with parametrization) in
`tests/test_options_scanner.py::TestAtFloorStrikesAreScoredTheWayTheyAreFlagged`, using the
same GOOGL 368.34-floor fixture as the FC-065 P2 flag class directly above it:

1. `test_a_strike_exactly_at_the_floor_earns_the_above_basis_bonus` — the plan's test 1.
   Re-scores the emitted opportunity from its own published components with the basis input
   forced True and False, asserts the spread is 10 points (`pytest.approx`) and that the
   opportunity carries the with-bonus score. This is a **pin, not a derivation**: if FC-073
   retunes the basis weights the assert must be updated deliberately, which is the intended
   prompt to re-confirm that at-floor still ranks with the above-basis cohort.
2. `test_an_at_floor_strike_scores_the_same_as_one_cent_above` — the boundary is no longer a
   scoring cliff: 368.34 and 368.35 now score within 1 point (residual is the OTM/return
   effect of the extra cent), not 10 apart.
3. `test_the_flag_and_the_scoring_input_are_the_same_value` — the plan's test 2, the
   boundary-semantics pin. Spies on the scoring call, asserts the recorded `above_cost_basis`
   argument **is the same value** as the emitted `assignment_above_cost_basis` flag,
   parametrized across `[360.00, 368.34, 368.35, 375.00]` (below / at / just-above / above).
   Fails on any semantic divergence (`>` vs `>=`) at the boundary. It does **not** catch a
   semantics-preserving refactor that re-derives one side from an equivalent comparison —
   the single-source shape in the scanner is what guards against that.

Full suite on the merged tree: **1025 passed** (post-train baseline 1019 + 6).
No existing assertion moved.

### Mutation record

Reverted the scoring input to the pre-FC-071 strict `>` (recreating the exact drift: flag on
`>=`, score on `>`), cleared every `__pycache__`, re-ran with `-p no:cacheprovider`:

- **3 failed, 3 passed.** Failures: tests 1 and 2, plus test 3's `[368.34]` case only.
- Test 3's `360.00`, `368.35`, and `375.00` cases still passed — the failure is confined
  precisely to the equality boundary, which is the intended blast radius.
- Predicate restored; 6/6 pass, full suite green.

Re-run on the merged tree after the cosmetic batch (2026-08-03): identical outcome — same
3 failures, same 3 survivors, same boundary confinement; restored, 1025 green.

### Merge record (2026-08-03)

Merged `origin/main` @ `3e5b0e8` into the branch. Six conflicts, more than the two expected —
the extra ones are squash-merge artifacts (the branch was built on the *unsquashed* FC-068
branch, so FC-068's own content re-conflicted against its squashed form on main).

| File | Kind | Resolution |
| --- | --- | --- |
| `src/data/options_scanner.py` | flag line: my shared-variable form vs main's FC-065 P2 inline `>=` | **Took mine** — this is the FC-071 change. The hoisted `>=` and the scoring call auto-merged above the conflict and survived intact. |
| `tests/test_options_scanner.py` | my whole new class vs nothing on main | **Took mine.** |
| `docs/plans/fc-071.md` | add/add | **Took mine** — verified byte-identical to main's copy apart from my Status/Execution additions (`git diff c7478b8:… origin/main:…` was empty). |
| `docs/FUTURE_CONSIDERATIONS.md` (FC-071 entry) | Status line | **Took main's** ("Resolved 2026-08-02", cites `46f7a28`). |
| `docs/FUTURE_CONSIDERATIONS.md` (new entries) | mine empty vs main's additions | **Took main's** — unions in FC-072, FC-073, FC-074. |
| `docs/plans/fc-065.md` | Status line, both stale | **Took main's.** Another FC's bookkeeping; not FC-071's to curate. |
| `src/data/opportunity_store.py` | comment-only (FC-075 Seam 1 / FC-065 P4 doc) | **Took main's** — strictly more informative, no code difference. |

Post-merge verification: zero conflict markers repo-wide; `_create_call_opportunity` swept for
duplicated blocks (a prior merge on this repo produced git auto-merge duplication) — clean, with
`assignment_above_cost_basis` assigned exactly once, consumed once by the scoring call and once
by the opportunity dict.

### Reviewer cosmetic batch (2026-08-03)

Both reviewers APPROVEd with these as PR-opening conditions; all four applied:

- **(a)** Softened the anti-drift claim in the test docstrings and the PR-body draft from "fails
  the moment anyone re-derives either surface" to "fails on any semantic divergence (`>` vs
  `>=`) at the boundary" — the original overclaimed, since a semantics-preserving refactor
  would not trip it.
- **(b)** Test 1's comment claimed it "derives the delta" while the assert pins `== 10`. Now
  states honestly that it is a pin and that FC-073 must update it deliberately. Exact float
  comparisons switched to `pytest.approx`.
- **(c)** Date strings corrected — the Execution section had claimed a 2026-08-01 build date,
  a day *before* the plan was authored and approved (2026-08-02). Build is 2026-08-02; PR opens
  2026-08-03.
- **(d)** OCC fixture strike now uses `round(strike * 1000)` rather than `int(...)`, which
  truncates when the float product lands just under the integer. **Latent, not active:** none
  of the four fixture strikes in use (`360.00`, `368.34`, `368.35`, `375.00`) actually
  truncate — verified — so no test outcome changed. Applied only inside the FC-071 class; the
  identical fixture line in the FC-065 P2 class above was left alone to keep the diff inside
  this FC's scope.
