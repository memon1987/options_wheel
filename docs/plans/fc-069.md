# Plan: FC-069 — the decommission sweep. 15 decision cards for operator sign-off

**FC entry:** `docs/FUTURE_CONSIDERATIONS.md` FC-069
**Plan file:** `docs/plans/fc-069.md`
**Status:** **SIGNED OFF — 2026-08-01. All 17 cards decided by the operator (see §Operator sign-off record). Execution unblocked, sequenced after FC-068 lands (Monday 2026-08-03 train), per the S-partition as amended below. · S1 DONE (PR #83, `de64b51`) · S2 DONE (PR #84, `8726ab1`, table dropped) · S3 DONE (PR #85, `d1e3fc2`) · S5 DONE (PR #86, `7073f9f`) — all live-verified 2026-08-04.**
**Size:** L (many S items; the cost is decisions, not code)
**Author:** Claude (Fable)
**Last updated:** 2026-08-01 (rev 2 — dispositions both plan reviews: regression-monitor detective layer surfaced as card 16, emergency-stop sub-decision added to card 7, second knob stratum carded as 17, post-sweep control matrix + knob-census appendix added; no reviewer-vs-reviewer disagreements; every pre-tag directionally upheld)

---

## Operator sign-off record (2026-08-01, inline session — binding)

| Card | Pre-tag | **Operator decision** | Notes |
|---|---|---|---|
| 1 — `max_total_positions` | REVIVE | **FLIPPED → DELETE** | Rationale on record: breadth is bounded by capital (proportional, scales) and the ticker universe; a fixed count must not bind future growth. Accepted residual: BP remains the only preventive breadth limit; the burst scenario stays count-ungated. Card 16 check 1 dies in the same PR. |
| 4 — earnings gate | REVIVE via FC-013 | **CONFIRMED — upgraded scope** | "Key part of our gating strategy": both sellers, live + backtest. Finnhub kept. Vehicle: FC-013 rev 2.1 (`e744f70`, twice-reviewed, build-ready behind FC-068 + exposure audit + operator window choice under the block-both-incidents acceptance criterion). AAPL week-of-07-27 incident added to the record. |
| 7 — RiskManager shrink | DELETE | **CONFIRMED, sub-decision = option 2** | Emergency-stop concepts die deliberately; **FC-074 filed** (`a404e2d`) for the account-level kill-switch question. |
| 16 — `/regression` detective layer | SYNC per-card | **CONFIRMED (SYNC)** | Check 1 dies (item 1); 3/5/6 die (items 2/3/7); 4 re-sourced from Config; 7/8 kept, 8 re-specced onto `avg_entry_price`; parsers rewired under item 12. Each change rides the same PR as its policy. |
| 5 — gap module | DELETE shadow; module fate flagged | **CONFIRMED — DELETE module + knobs, SHA pointer** | FC-049 owns any evidence-based revival. Control matrix reads "gap risk: absent by decision" (ratifies FC-036's study). |
| 2, 3, 6, 8, 9, 10, 11, 12, 14, 15, A, 17 | as carded | **CONFIRMED as recommended** | Blanket confirm after per-card review; item 13 **HOLD** confirmed (FC-066's turf) — **subsequently RESOLVED 2026-08-04 by FC-078's knob disposition** (4 kept, 8 deleted, 4 added; FC-066 closed with lineage): item 13 is DONE, nothing left for this sweep. |

**Partition amendments from the decisions:** S4 (cap revival) is dropped — item 1's delete joins S1. Item 4 executes as FC-013's own PR (one-PR-per-FC), not inside this sweep. Everything else per §PR partitioning.

---

## Context

This is the **operator's decision document**, not a build spec. The 2026-07-31 three-agent investigation established that production runs a *stateless* wheel (`/scan` → `OptionsScanner` → GCS blob → `/run` → `rank/select_batch/execute_batch`) while the codebase maintains a parallel fictional stateful one, plus config knobs and risk controls that read as live but gate nothing on the path that trades. FC-065 and FC-068 remove what sits in their blast radius; the 15 items below survive them. Each is a **decision, not a deletion** — three are risk controls the operator may want revived: production currently has **no global position cap, no per-ticker exposure cap, and no earnings gate** on the live path.

**Base assumption (binding):** every card is written against the **post-FC-068 codebase as its plan (rev 2, `948abdf`) specifies** — the engine decision path (`run_strategy_cycle`, `_find_new_opportunities`, `evaluate_covered_call_opportunity`, `find_put_opportunity`, stage 2–6/9 machinery, `main.py --command run`) is deleted; the simulator replays the production pipeline; `max_stocks_evaluated_per_cycle` / `max_new_positions_per_cycle` are already gone. Line references are against today's `main` (`570f2c5`) and will drift a few lines after FC-068 and FC-065 P2/P4 merge; symbol names are the contract. FC-068 has **not built yet** — nothing in this sweep executes until it has.

**Verification discipline:** the FC-068 plan reviews found two "exhaustive" enumerations with holes. Every live/dead claim below was therefore re-verified by grep/read against `main` (and the two `fc-065/*` branches where relevant), not taken from the inventory. Where a claim could not be re-verified this session (BigQuery row counts, the scheduler job — auth unavailable non-interactively), the card says so and the build carries the audit query. **Falsified or corrected along the way:** the inventory's item-2 equivalence, item-12 line ref, and item-13 count (rev 1); rev 1's own "nothing counts positions," item 5's "only consumer," and the missed detective layer + second knob stratum (rev 2, from the reviews). All flagged inline and collected in §Open questions.

**Not relitigated here (operator decisions, binding):**

- The drawdown pause is dead and stays dead (FC-065 OQ-3). Item 9 is confirmed-tagged: the knob deletion is *when/how*, not *whether*.
- FC-033 stays **Deferred** (reopenable only by operator decision on `uncovered_days` evidence).
- The floor is Alpaca `avg_entry_price` (FC-065 Phase 1, live since `630c0bd`).
- Item 13 (`rolling.*`) is **HOLD** — its fate belongs to FC-066's revival decision, which now carries a three-item pre-revival checklist (added 2026-08-01 on `fc-065/phase-2-roller-floor`, merging Monday). This plan does not usurp it.

**How to read a card:** (a) what it is; (b) *verified* status — file:line evidence, not the inventory's claim; (c) recommendation with the strongest argument **for and against** (the counterargument is the point — decide, don't be sold); (d) cost/scope of each option; (e) blast radius of the delete; (f) the Decision line. Check ☐ confirm to accept the pre-tag, ☐ flip to reverse it. An unflagged ambiguity is a plan defect — flag it in review.

**Rev 2 — what both reviews found, in one line each:** (1) a deployed **detective control layer** the inventory missed entirely — `tools/testing/regression_monitor.py`, served at `/regression` (`cloud_run_server.py:1643`), invoked hourly by Cloud Scheduler, hardcode-mirroring five swept policies and returning **HTTP 500** on FAIL — now card 16, and cards 1/2/3/7 are corrected where its existence changed their premise; (2) card 7's widened delete was silently killing the only account-level loss-limit *concepts* in the repo — now an explicit sub-decision; (3) a **second stratum of dead knobs** the item list never covered — now card 17, and the knob-census appendix exists so a third inventory never has to be re-litigated.

---

## Post-sweep control matrix — the end state, before you decide

Both reviewers asked for this page: the decisions below interact, and the operator should see what stands between the account and a bad day **after S1–S5 land under the pre-tagged outcomes**. `[bracketed]` entries depend on a Decision line on this document.

| Layer | Control | State after the sweep |
|---|---|---|
| Symbol admission | `market_data.filter_suitable_stocks` — price band $10–400, ≥2M volume, metrics (`market_data.py:86–148`) | **Live** (unchanged) |
| Contract admission | delta bands, DTE targets, premium floors (`market_data.py:276–628`) | **Live** (unchanged) |
| Per-position size | `max_position_size 0.35` × portfolio + ≤10 contracts (`put_seller.py:146–159`) | **Live** (unchanged) — the *real* per-ticker bound, floats with equity |
| Per-underlying breadth | one option position per underlying — scanner skip + `duplicate_underlying` + share ledger | **Live** (item 12 makes the skip correct instead of accidentally-conservative) — **positions-based: blind to resting orders** (see cards 1/3) |
| Account breadth | global position cap | **[item 1]** — revived in `select_batch` (soft cap: blocks new *puts* beyond N) or nonexistent. Today: preventive nonexistent; detective check fails at >10 (card 16) |
| Capital exhaustion | buying-power ledger in `select_batch` | **Live** (unchanged) — the only hard limit on put deployment absent item 1 |
| Below-basis calls | execute-time floor = Alpaca `avg_entry_price`, fail-closed + divergence cross-check (FC-050/065) | **Live** (unchanged) — the strongest control in the system |
| Underwater positions | hold-uncovered-until-recovery + `uncovered_days` legibility (FC-065 P4) | **Live** post-P4. No pause, by decision |
| Exit discipline | DTE-banded profit-taking (`1c26b43`); put/call stop-losses **off** (FC-010) | **Live** (unchanged); min-hold gate deleted (item 6 — it never ran) |
| Earnings | none on the sell path | **[item 4]** — FC-013 rev 2, or permanently accepted absence |
| Gap/overnight risk | none anywhere post-FC-068 | **Absent by decision** (item 5; FC-036 study said don't arm; FC-049 owns any revival) |
| Account-level loss limit | none (dead emergency-stop concepts deleted with card 7) | **[card 7 sub-decision]** — delete undocumented, or delete + file the kill-switch FC |
| Detective layer | `/regression` hourly: position counts, sizing, exposure, naked-call, cost-basis mirrors | **[card 16]** — synced per-card outcome (recommended) or deleted |
| Duplicate close orders | `_closed_today` (amnesiac) | **Unchanged** — FC-009's open territory (item 15) |

Read the two `[absent]` rows and the two `[bracketed policy]` rows together — that is the risk posture being signed.

---

## Group A — policy revivals (decide these first)

### Item 1 — `max_total_positions: 10` · pre-tag: **REVIVE** (enforce in `select_batch`)

**(a) What it is.** A global cap on concurrent option positions. `config/settings.yaml:54` (`10`), property `src/utils/config.py:316–318`, validation `:161–163`.

**(b) Verified status: no *enforcement* exists on the live path — but a *detective* check counts positions hourly and fails at >10.** Rev-2 correction (both reviewers): rev 1 said "nothing counts positions" — false. `tools/testing/regression_monitor.py` `check_risk_parameters` (`:817–829`) counts option positions and returns FAIL above 10, served at `/regression` (`cloud_run_server.py:1643`, **HTTP 500 on any FAIL**), invoked hourly at :45 market hours by Cloud Scheduler (`regression-hourly`, per `docs/releases/RELEASE_2026-04-07.md`; live-verify the job at build). That is a materially different premise: the choice is not "cap vs nothing" but "**preventive** cap vs **detective-only** (alarm an hour after the eleventh position opens, with no blocking)." Preventive consumers: `wheel_engine._can_open_new_positions` (`:255–257` — note it counted **short options only**, `qty < 0`) and `get_strategy_status` (`:597`) — both deleted by FC-068; `risk_manager.py:52/:307/:354` — zero-caller methods (card 7); `portfolio_tracker.py:401/:440` — CLI-only; `/config` `max_positions` (`cloud_run_server.py:1069`); stale k8s copy (`deployment.yaml:120`); `scripts/testing/test_trading_workflow.py:35`, `test_issue_audit.py:81–93` (card 16/§CLI). Read of `select_batch` (`execution_engine.py:460–599`) confirms its only limits are per-underlying dedup, the calls share ledger, and put buying power.

**(c) Recommendation: REVIVE.**
- *Strongest FOR:* buying power is the only *preventive* breadth limit on puts today, and it is a *collateral* constraint, not a *count* constraint — a volatile morning, a margin-setting change at Alpaca, or a universe/config change could legally deploy the whole account across many puts in one batch, and the detective check would report it an hour later, after the fills. A count cap is the blunt, cheap, last-line preventive guard, and the cheapest control on this inventory to put on the real path.
- *Strongest AGAINST:* in ten months of live trading, BP exhaustion and the effective universe (6 of 14 symbols, FC-048) have kept concurrent positions well under 10. The arithmetic says the cap may **never bind**: at today's ≈$103k portfolio, `max_position_size 0.35` sizing against put collateral exhausts BP at roughly **4–8 concurrent puts** depending on strikes — BP hits zero before the count hits 10. Reviving at 10 may be pure ceremony; if so, DELETE (keeping the detective check) is the consistent choice.
- **Cap VALUE is its own sub-decision (rev 2 — rev 1 left it unflagged).** Confirming REVIVE at `10` ratifies a number nobody ever chose. Build-time audit query (marked, feeds the value decision): max concurrent short option positions ever held, from `trades_from_activities` fills minus expiries/closes per day — if the historical max is 6, a cap of 10 guards nothing and the operator should set the number deliberately (historical max + headroom, or an explicit "10 is fine as a catastrophe bound" on the record).
- **The delivered guarantee is a *soft* cap — stated so it is signed with eyes open:** with the recommended counting (calls count toward N, covered calls never blocked), total option positions **can exceed N via call writes** on held shares. The precise contract: "no *new put* is selected when short option positions ≥ N." That is the intended shape — calls on owned shares reduce risk — but it means the detective check (card 16) must use the same semantics or it will false-alarm.
- **Blind spot, stated:** the count is positions-snapshot-based and **cannot see resting unfilled orders** — the same open-order window FC-068 §1 documents for the deleted stage-6 guard and FC-009 owns. Two `/run` cycles racing a slow fill could each pass the count. Not solved here; not solvable from positions data; on the record.
- **Alternative weighed (rev 2 — the card previously never considered it):** a **per-batch bound** (`max_new_positions_per_batch` — the semantic of the just-deleted `max_new_positions_per_cycle`, revived in `select_batch`) targets the actual catastrophe scenario ("one volatile morning deploys the whole account") *more directly* than a total-count cap, is immune to the resting-order blind spot (it bounds this batch's additions, not a racy global count), and needs no positions snapshot. Decision offered both ways: total-count cap (matches the knob being revived, guards slow accumulation too) / per-batch bound (guards the burst, simpler) / both (belt and braces, two small checks in the same function). Recommended: **total-count, plus the per-batch bound if the audit query shows bursty fill days** — argue at sign-off if the audit says otherwise.

**(d) Cost.**
- *REVIVE (S):* in `select_batch`, count open short `us_option` positions from the positions snapshot (already fetched when calls are present; put-only batches gain one `get_positions` call — or reuse `/run`'s existing snapshot) plus batch-selected so far; when `count ≥ max_total_positions`, drop with a new `DROP_MAX_TOTAL_POSITIONS` reason (extends the closed `DROP_REASONS` enum, flows into FC-068's `selection_dropped` tally and P4 decision records for free). Counting semantics match the dead check (short options only, `qty < 0`) — rev-2 correction: rev 1 said the dead check counted *all* option positions; it did not, so the recommended counting is *the same as* the original and only the blocking scope (puts-only vs all) differs. Tests: cap fires at boundary; mutation-checked. Card 16's check 1 syncs to the same semantics in the same PR.
- *DELETE (S):* knob + property + validation + `/config` field + detective check 1 (card 16) + the dead readers + CLI/script edits (§CLI surface).

**(e) Blast radius if deleted:** `/config`'s `max_positions` field (verified: no dashboard consumer); regression monitor check 1 (card 16 — must be deleted with it or it 500s against a policy that no longer exists); `tests/test_config.py:34`; `portfolio_tracker.py:401/:440` + `scripts/testing/*` (§CLI surface); k8s yaml (OQ-8).

**(f) Decision:** ☐ confirm REVIVE ☐ flip to DELETE — operator. If REVIVE: **value** (10 / historical-max+headroom / other: ___) — informed by the marked audit query; **shape** ☐ total-count ☐ per-batch ☐ both; **call counting** ☐ calls count, never blocked (recommended) ☐ strict-original (short-only count, all types blocked).

---

### Item 4 — earnings blackout (`earnings.*`, `EarningsCalendarService`, FINNHUB key) · pre-tag: **REVIVE via FC-013**

**(a) What it is.** A Finnhub-backed earnings calendar that can block trade initiation within N days of earnings. Service `src/api/earnings_calendar.py` (206 lines, works — FC-007 verified); knobs `earnings.enabled: true / cache_ttl_hours / blackout_days: 2 / lookahead_days` (`settings.yaml:220–224`, `config.py:566–587`); a *dead* sibling knob `risk.gap_risk_controls.earnings_avoidance_days: 7` (`settings.yaml:144`, `config.py:451–453` — defined, never read, predates FC-007); `FINNHUB_API_KEY` on the live Cloud Run env (verified in FC-039's env dump) with secret mapping `config.py:85`.

**(b) Verified status: never on the live sell path.** Zero earnings references in `options_scanner.py`, `put_seller.py`, `call_seller.py`, `execution_engine.py` (grep). Sole construction: `wheel_engine.run_rolling_cycle` `:1223–1225`, consumed by `call_roller.py:78–81, 108–109` — the Friday roller, which is structurally dead (FC-066: zero rolls ever). The GOOG put sold 2026-04-28, the day before earnings, is the incident on record. Inventory claim confirmed. The backtest has its own point-in-time seam (`HistoricalEarningsCalendar`, `simulator.py:165–167`) — roller-path only today.

**(c) Recommendation: REVIVE — but via a re-planned FC-013, because FC-013's published plan is stale.**
- **FC-013 currency check (asked for explicitly): NOT current post-FC-038/065/068.** Its two wiring targets — `PutSeller.find_put_opportunity` and `CallSeller.evaluate_covered_call_opportunity` (fc-013.md §1) — are both **deleted by FC-068**, and its injection mechanism (WheelEngine constructs the sellers, §2) no longer exists: post-FC-068 the engine builds no sellers. Its line refs have drifted (`earnings_avoidance_days` now `config.py:451–453`, not 441–443). What *survives* of FC-013: the service works; the fail-closed decision; activating `earnings.blackout_days`; deleting `earnings_avoidance_days`; the `docs/gates.md` deliverable; the test matrix shape. **FC-013 needs a rev 2** that (i) moves the gate to the scanner (`scan_for_put_opportunities` / `scan_for_call_opportunities`) or to selection as a drop-with-reason — selection-side fits FC-038's drop model and P4's decision records, scanner-side spares Finnhub calls for symbols that produce no candidates; (ii) threads a replay seam (post-FC-068 the simulator replays the scanner, so the gate must accept `HistoricalEarningsCalendar` or be gated off in replays — an FC-068-created obligation FC-013 predates); (iii) accounts for scan latency (Finnhub call per universe symbol at scan time, 24h cache; `/scan` baseline is ~18.5s) and for what **fail-closed on a Finnhub outage now means: no puts written account-wide that day** — a bigger blast radius on the scanner path than on the old per-seller path.
- *Strongest FOR revival:* selling 7-DTE premium means earnings weeks are exactly the tail this strategy's delta band does not price (implied vol crush vs gap risk are different animals); the GOOG incident was real; the service exists and is already paid for (key, quota, cache) — the cost is wiring, not building. Symmetry: the roller has this gate; the paths that actually trade don't.
- *Strongest AGAINST:* production has traded through **every earnings season since 2025-10 ungated**, on a strategy explicitly designed to take assignment on quality stocks — the FC-010 philosophy already tolerates the downside case, and 88 call writes / 300+ put fills produced exactly one flagged incident. `blackout_days: 2` was never tuned; fail-closed adds a third-party availability dependency to the money path. If the operator's honest risk read is "assignment-tolerant means earnings-tolerant," DELETE is defensible — this FC exists to force that choice, not to presume it.
- **⚠ The AGAINST's denominator is unmeasured (rev 2 — risk reviewer).** "One flagged incident" counts incidents *noticed*, not exposures *taken* — nobody has counted how many of those fills sat inside an earnings window. **Marked build-time audit query, feeds this decision before sign-off executes:** join `trades_from_activities` sell-to-open fills against `HistoricalEarningsCalendar.from_table()` (the committed point-in-time table — makes this cheap and reproducible) and report (i) N put fills and N call writes within `blackout_days = 2` of an actual earnings date, (ii) their realized P&L vs the book's average. If the answer is "40 exposures, P&L indistinguishable," the AGAINST hardens into evidence; if it's "12 exposures, three of the book's worst cycles," the FOR does. The decision should not be signed on the anecdote in either direction.
- **DELETE option, scoped honestly:** a full delete (service + knobs + FINNHUB key) **breaks the live Friday roller's gate and the backtest earnings seam — it collides with item 13's HOLD.** There are no sell-path pieces to delete (nothing was ever wired). So "DELETE" today can only mean: delete `earnings_avoidance_days` (dead either way — do it regardless of the decision), mark the rest roller-owned, and let FC-066's decision dispose of service+knobs+key if the roller dies. i.e. **flipping this item to DELETE mostly defers it to FC-066** — the operator should know the flip buys little.

**(d) Cost.** *REVIVE:* FC-013 rev 2 (M — replan + own PR under FC-013, keeping one-PR-per-FC; FC-069 records the decision and hands off). *DELETE:* S now (`earnings_avoidance_days` only) + the rest rides FC-066.

**(e) Blast radius (delete path):** roller gate (`call_roller.py:78`), `tests/test_backtest_earnings.py`, `tests/test_call_roller.py` earnings cases, `rolling.earnings_blackout_days` (item 13's turf), Cloud Run env + secret. None of it deletable ahead of FC-066.

**(f) Decision:** ☐ confirm REVIVE (→ FC-013 rev 2) ☐ flip to DELETE-deferred-to-FC-066 — operator. Either way: `earnings_avoidance_days` is deleted in the sweep PR (dead knob, no decision needed beyond this line).

---

## Group B — deletes with money-path implications

### Item 5 — gap controls (`execution_gap_threshold: 999` shadow; stage-2 detector + knobs) · pre-tag: **DELETE shadow knob; stage-2 per FC-049**

**(a) What it is.** Two distinct controls sharing `GapDetector` (`src/risk/gap_detector.py`, 645 lines): the stage-4 *execution* gap gate (compare open vs prior close at execution time; knob `execution_gap_threshold`, set to `999` as a deliberate FC-036 "fixed but not armed" shadow, `settings.yaml:150`) and the stage-2 *symbol suitability* filter (gap frequency/volatility screening; knobs `enable_gap_detection`, `max_overnight_gap_percent`, `gap_lookback_days`, `max_gap_frequency`, `quality_gap_threshold`, `premarket_gap_threshold`, `market_open_delay_minutes`, `max_historical_vol`, `vol_lookback_days`, `execution_gap_lookback_hours` — `settings.yaml:139–155`).

**(b) Verified status.** The gap knobs live at `settings.yaml:139–158` (rev-2 range fix: the two volatility knobs sit at `:157–158`, past rev 1's `:155` cutoff). **Rev-2 falsification of rev 1's own consumer claim (both reviewers):** the knobs are *not* consumed only inside `gap_detector.py` — `src/backtesting/reporting/bq_writer.py:119–120` reads `gap_lookback_days`, `max_gap_frequency`, and `execution_gap_threshold` via `getattr(config, k, None)` into the **`config_hash`** that stamps every `backtest_runs` row. Deleting the properties would not crash anything (`getattr` defaults to `None`) — it would *silently change every future run's config hash*, making a knob deletion indistinguishable from a strategy-parameter change in the run record: the exact confusion that hash exists to prevent. **Disposition: the same PR that deletes the knobs drops the three keys from the `config_hash` list**, and the PR description notes the hash discontinuity beside FC-068's `ENGINE_VERSION` bump (one boundary, two markers, same non-comparability memo). `GapDetector`'s strategy consumers are exactly `wheel_engine.py:57` (construction), `:291` (stage 2), `:345` (stage 4) — **all three die with FC-068**, leaving the module orphaned from strategy code. Remaining references post-FC-068: the `bq_writer` hash reads above, `tests/test_gap_detector.py`, `tools/diagnostics/fc002_gap_filter_ab.py` (gets FC-068's stale-header), backtest adapter comments, and `/config`'s `gap_thresholds` block (`cloud_run_server.py:1070–1073`, exposing `quality` + `execution`). Verified no dashboard consumer of `gap_thresholds`. History that binds: FC-036 fixed stage 4 and the arming was **rejected on evidence**; FC-049 found stage 2 never ran in production and reconstructs it would have refused **123 of 327** real entries.

**(c) Recommendation: confirm the shadow-knob delete; but the pre-tag under-decides — the operator must also disposition the module.**
- *FOR deleting the shadow:* a knob at `999` gates nothing by construction, and post-FC-068 its only consumer (`can_execute_trade`) is unreachable from any entry point. Keeping it preserves the exact "reads as live, consumed by nothing" condition this FC exists to end.
- *AGAINST:* FC-036 Phase B contemplated arming with a studied threshold; deleting the knob deletes the shadow scaffolding. Weak — the FC-036 study already said don't arm, and re-adding a yaml key is a one-line change if evidence ever reverses.
- **The unfinished half:** the pre-tag says "resolve stage-2 per FC-049's study (revive only with evidence)" — but FC-049 is Status: Consideration with no study scheduled. That leaves `GapDetector` + 10 knobs + tests as a corpse *with a nominal owner and no timeline*. Options: **(i)** keep module + stage-2 knobs, annotate as FC-049-owned, delete only shadow + `/config.gap_thresholds.execution`; **(ii)** delete the whole module + all gap knobs + `/config.gap_thresholds` now — FC-049's study runs from git history if ever commissioned (the A/B tool pins the SHA); reviving with evidence means reviving code *with* the evidence in hand. Recommended: **(ii)** — "revive only with evidence" is better served by a git tag than by 645 orphaned lines that the next inventory has to re-litigate; FC-049's entry is updated to say the code lives at the pre-FC-069 SHA. Money-path implication: none today (nothing fires either way) — the implication is foreclosing a *future* control, which git does not foreclose.

**(d) Cost.** (i): S. (ii): S–M (module + knobs + properties + tests + `/config` block + FC-049 entry annotation).

**(e) Blast radius (option ii):** `tests/test_gap_detector.py` (deleted), `tools/diagnostics/fc002_gap_filter_ab.py` (already stale-headed by FC-068; add "code deleted at <SHA>" line), `/config.gap_thresholds` (no consumers), **`bq_writer.config_hash` key-list edit + hash-discontinuity note in the same PR (rev 2, see (b))**, `docs/logging` stage docs (already rewritten/stale-marked by FC-068), backtest adapter comment cleanup.

**(f) Decision:** ☐ confirm shadow delete — and module disposition: ☐ (i) keep, FC-049-annotated ☐ (ii) delete module + knobs (recommended) — operator.

---

### Item 7 — `RiskManager.validate_new_position` · pre-tag: **DELETE** (absorbs FC-014)

**(a) What it is.** The portfolio-level pre-trade validator (`src/risk/risk_manager.py:23–120`): allocation, max positions, per-stock cap, per-ticker exposure, cash reserve, option-specific checks.

**(b) Verified status: zero call sites, confirmed** (grep across `src/ deploy/ main.py`: only the definition). `RiskManager` is constructed exactly once — `wheel_engine.py:1222`, inside `run_rolling_cycle`, for the roller's `validate_roll` (`call_roller.py:163`), which survives. The "original substring bug" claim is confirmed **twice over**: `underlying_symbol in p.get('symbol', '')` (`:60` region) and `'PUT' in pos.get('symbol', '')` (`:67` region) — the ancestral instances of the OCC-substring family. **Wider than the inventory — five sibling methods, also zero external callers** (grep): `_validate_option_specific_risks` (`:174`), `calculate_portfolio_risk_metrics` (`:247`), `should_reduce_positions` (`:330`), `get_emergency_stop_conditions` (`:364`), `check_emergency_conditions` (`:377`). Six dead methods in total. Deleting only `validate_new_position` per the tag would mint five new unowned corpses — the exact condition FC-068 refused to leave behind.

**(c) Recommendation: confirm, widened — shrink `RiskManager` to `validate_roll` (+ its private helpers) — with one sub-decision rev 1 hid.**
- *FOR:* it has never run; its *pre-trade* checks are dispositioned one-by-one elsewhere on this inventory (exposure → item 2, per-stock → item 3, global cap → item 1, portfolio allocation → item 17, cash reserve → rider below), and it carries live bugs that would misbehave *if ever wired* — FC-014's "consolidate" fork means shipping substring bugs to the money path or rewriting the method, at which point item 1's `select_batch` port is the cheaper, already-recommended home for the one check worth keeping.
- *AGAINST (the FC-014 consolidate fork):* a single authoritative pre-trade validator is a legitimate architecture — one place to audit, one place to test. Retiring it commits the project permanently to distributed enforcement (scanner filters + selection ledgers + execute-time floor). That commitment is real; it is also **already the reality** — distributed enforcement is what has actually traded for ten months, and FC-065's principles point the same way.
- **⚠ Sub-decision the widened delete was hiding (rev 2 — risk reviewer, HIGH): three of the six methods are the only *account-level loss-limit concepts* in the repo, and no other inventory item dispositions them.** `should_reduce_positions` encodes a **10% portfolio unrealized-loss** reduction trigger (`:350`); `get_emergency_stop_conditions`/`check_emergency_conditions` encode a **15% portfolio-loss stop, 5% daily-loss limit, 80% margin ceiling, $1k min-BP floor** (`:364–404` — note the daily-loss and margin conditions are *declared* but not even checked by the dead checker; only portfolio-loss and BP are). None has ever run — but rev 1's "nothing silently disappears" was **false as written**: the *checks* were dispositioned, the *concepts* were not. And FC-065 OQ-3 is no cover — it killed the per-symbol call-side pause, **not** the idea of an account-level circuit breaker; nothing in the live system stops the strategy as a whole on a bad-enough day (the drawdown-pause R3 was per-symbol too). Deleting these undocumented would erase the only written record that the project ever contemplated one.
- **Rider, corrected (rev 2):** post-delete, `min_cash_reserve: 0.20` (`settings.yaml:68`) has zero *enforcing* consumers — but rev 1's "nothing enforces it" was imprecise: **regression monitor check 3 (card 16) watches it hourly** (warn-only — it does not 500). So the choice is: delete knob + detective check together (and record that cash discipline = buying-power-bounded batches); or keep both as a monitored-but-unenforced advisory; or wire a BP haircut into `select_batch` (behavior change → joins item 1's PR). Recommended: delete both, same PR.

**(d) Cost.** S–M: delete the six dead methods; keep `validate_roll` + its private helpers; sync/delete regression-monitor mirrors per card 16; delete/trim `tests/test_risk_manager.py`; CLI/script edits (§CLI surface); FC-014 closes (text in §Closing texts).

**(e) Blast radius:** `tests/test_risk_manager.py`; config knobs it referenced (each owned by items 1/2/3/17 or the rider); regression monitor checks 3/5 (card 16); `portfolio_tracker.py:390` + `scripts/testing/test_issue_audit.py:65–77` (§CLI surface); `docs/CLAUDE.md`'s "RiskManager validates all positions" claim (item 14). Roller untouched (`validate_roll` kept — survives harmlessly regardless of FC-066, until that decision).

**(f) Decision:** ☐ confirm (widened shrink) ☐ flip — operator. Rider `min_cash_reserve`: ☐ delete knob + detective check ☐ keep both as advisory ☐ wire haircut. **Emergency-stop concepts:** ☐ delete undocumented ☐ delete + file an S-sized FC for an account-level kill switch (portfolio-loss threshold halting `/scan`–`/run`, thresholds to be chosen with data, not inherited) — **recommended: the second**; the concept has never existed in running form, and whether it *should* is a real risk decision that deserves its own page, not silent burial in a cleanup PR.

---

### Item 8 — `WheelStateManager` fictional layer · pre-tag: **DELETE/SHRINK** (absorbs FC-039)

**(a) What it is.** The parallel stateful wheel (`src/strategy/wheel_state_manager.py`, 747 lines): phases, premium accumulators, roll counts, `can_sell_puts/calls`, GCS persistence. FC-039 proved four ways the persistence has **never worked** (`STATE_STORAGE_BUCKET` never set; no state object in any bucket) — state is rebuilt per request and discarded.

**(b) Verified status — the post-FC-068 consumer census:**
- `wheel_engine.reconcile_positions` (live, pre-`/run` housekeeping): heavy use — assignment/expiration bookkeeping via `handle_put_assignment`/`handle_call_assignment`, `symbol_states` sync to Alpaca, `get_position_summary`, a no-op `_save_state` (`:1194`), and the `wheel_cycles` writes (item 10).
- `CallRoller` (structurally dead, FC-066): `get_roll_count`, `get_active_call_details`, `remove_position`, `add_call_position`, `set_active_call_details`, `record_call_roll` (`call_roller.py:73,147,319–340,366` — rev-2 addition: the `:366` BTC-only-path `remove_position` site).
- `CallSeller.wheel_state` param + `set_active_call_details` (`call_seller.py:22,41,443–446`): **fully orphaned** — `/run` and `/monitor` construct `CallSeller(alpaca_client, market_data, config)` with no state (`cloud_run_server.py:375,674`), and FC-068's repointed replay mirrors that. Confirmed exactly as FC-068 §2 annotated.
- Pre-existing corpses in this orbit, per FC-068 §11: `call_seller.handle_call_assignment` (`:825`, zero callers), `call_seller._parse_option_symbol`, symmetric `put_seller` dead code.
- Inventory claim confirmed.

**Standing note for any future persistence target (recorded here; FC-039's closing text cites it):** a configured-but-unresolvable persistence target must **fail loudly at startup**, never silently no-op — the silent `storage_bucket=None` no-op is how this layer stayed fictional for a year while docs called it canonical. Whoever next adds a durable-state target (FC-009's dedup, FC-066's roller, anything) inherits this as a requirement, not a suggestion.

**(c) Recommendation: confirm SHRINK — with FC-066 sequencing as the gating question.**
- *FOR:* the layer is the root of the FC-029/038/045/048/050/062/065/066 defect family — controls installed on fiction. Post-FC-068 nothing that *trades* reads it; what reconcile actually produces of durable value is **log events** (assignment/expiration telemetry) and the item-10 writes (dying). The honest shrink: reconcile keeps a per-request, in-memory position diff against Alpaca (which is all it ever really had) and its logging; phases, accumulators, `can_sell_*`, GCS save/load, and the illusion of continuity are deleted. FC-039 closes as "delete the illusion" (text in §Closing texts) — its own entry already conceded `reconcile_positions` rebuilds from Alpaca each cycle, so persistence "may be genuinely unnecessary."
- *AGAINST:* FC-066's roller revival is queued and its current code consumes six state methods. Deleting them first means FC-066 rebuilds against a moving target; deleting *after* FC-066 (whose fix direction is explicitly **stateless-from-Alpaca** — `original_premium` from order history, eligibility in the monitor cycle) means the roller methods die naturally. There is also a modest counterargument for keeping a thin state layer as reconcile's internal bookkeeping structure — which the shrink preserves anyway; the argument for keeping *phases/persistence* has no live consumer to stand on.
- **Sequencing decision:** shrink in two stages — **now:** delete the orphaned `CallSeller` half (`wheel_state` param, `set_active_call_details` call site, `handle_call_assignment`, `_parse_option_symbol`, put-seller symmetric corpses) — zero live behavior change, verified; **after FC-066 decides:** the full `WheelStateManager` shrink (roller methods + phases + persistence + `can_sell_*`), coordinated with the roller's stateless rebuild. This respects FC-066's boundary without leaving the whole layer untouched.

**(d) Cost.** Stage 1: S. Stage 2: M (touches `reconcile_positions`, a live path — needs the full two-reviewer gate; tests `test_wheel_engine_injection.py` state tests, `test_wheel_state_manager` if present, roller tests).

**(e) Blast radius:** stage 2 touches `/run` housekeeping — a shrink bug breaks pre-trade reconciliation. Mitigation: reconcile's observable contract (events emitted per assignment/expiration) pinned by test before the shrink. Dashboard: verified the dashboard reads `trades_from_activities`-family views, not reconcile events; re-verify any log-sink view consuming `put_assignment_from_activity` etc. at build (bq unavailable this session).

**(f) Decision:** ☐ confirm two-stage SHRINK ☐ flip (delete nothing until FC-066) ☐ flip (full delete now, FC-066 rebuilds stateless from scratch) — operator.

---

### Item 10 — `wheel_cycles` writer in `reconcile_positions` · pre-tag: **STOP writing; drop table**

**(a) What it is.** BigQuery analytics writes recording "completed wheel cycles": `analytics_writer.write_wheel_cycle` (`:255–262`), called from two sites in `reconcile_positions` — `wheel_engine.py:844` (activity-driven) and `:1052` (position-diff-driven).

**(b) Verified status (code side — mechanism confirmed).** `capital_gain` derives from `wheel_state.symbol_states[...]['stock_cost_basis']` (`:842`, `:1050`), which never resolves (fictional state, FC-039) → `capital_gain = 0` on every row. The per-instance dedup (`_processed_activity_ids`, `:747–749`) does not survive cold starts, and the activity window is 7 days (`:739`) → every fresh instance re-detects and re-writes the same assignments → **duplicate zero-gain rows, one batch per cold `/run`**. Dashboard verified clean: `dashboard/backend/services/bigquery.py` reads **only** `wheel_cycles_from_activities` (the FC-012/018 view; grep shows zero reads of the raw table), and `analytics_writer.py:10–11`'s own docstring says so. **Not re-verified live:** the row counts — inventory says 36/31/7 dupes per assignment, FC-065 Context item 4 says 36/30/6. The discrepancy is immaterial to the mechanism but the build runs the audit query and records the real numbers. Also verify at build (FC-035's step-0 gate pattern): no view definition references the raw table, no scheduled queries.

**(c) Recommendation: confirm.**
- *FOR:* the table is 100% garbage by construction (every row zero-gain; most rows duplicates); nothing reads it; FC-035 set the exact precedent (`order_statuses`: stop writer, drop table). Leaving it means every future data audit re-discovers it — this is the third plan file to describe these rows.
- *AGAINST:* the rows are *evidence* — of assignment timestamps as reconcile saw them. But `trades_from_activities` carries the authoritative OPASN rows already (idempotent, deduped), so the evidentiary value is zero beyond what a pre-drop snapshot preserves. The only real risk is an unknown reader; the step-0 gate exists to catch exactly that.

**(d) Cost.** S: delete the two call sites + `write_wheel_cycle` + the schema entry; operator (or documented `bq` step) snapshots then drops the table.

**(e) Blast radius:** `reconcile_positions` (live path — but the writes are already wrapped in swallow-all `try/except`, so removal is behaviorally inert); backtest: the NoOp writer absorbs the method's absence trivially (catch-all `__getattr__`); `wheel_engine:604` reads `get_all_wheel_cycles` for the FC-068-deleted `get_strategy_status` — gone already.

**(f) Decision:** ☐ confirm ☐ flip — operator. Table drop is operator-executed (`bq cp` snapshot → `bq rm`), recipe in §Rollback.

---

## Group C — mechanical deletes

### Item 2 — `max_exposure_per_ticker: 40000` · pre-tag: **DELETE** — ⚠ inventory's equivalence claim FALSIFIED

**(a/b) What & verified status.** `settings.yaml:61`, property `config.py:321–323`, validation `:169–171`. Preventive consumer: `risk_manager.py:93` — inside the never-called `validate_new_position` (item 7). **Rev-2 correction: not the *sole* consumer — regression monitor check 6 (card 16) hardcode-mirrors the $40k limit hourly and returns FAIL (→ HTTP 500) above it** (`regression_monitor.py:899–923`), measuring **market value per underlying**, a different quantity from the dead knob's assignment-notional semantics. **This check is a live incident waiting on account growth:** a single `max_position_size`-sized stock lot crosses $40k market value at roughly a **$114k portfolio** (0.35 × $114k ≈ $40k; account ≈ $103k today) — at which point `/regression` begins returning 500 **hourly**, on a healthy account, against a policy nothing enforces. Deleting the knob without card 16's sync ships that alarm anyway. Stale divergent copy in `deploy/kubernetes/deployment.yaml:121` (`50000` — the two configs already disagree, itself evidence nothing enforces this). Also read by `scripts/testing/test_issue_audit.py:45` (§CLI surface).

**(c) Recommendation: confirm the DELETE — but the inventory's documented rationale is wrong and must not be written into the docs.** The inventory says the cap is "structurally implied: one put per underlying × `max_stock_price` 400 × 100 = exactly $40k." **Falsified:** the put sizer allows up to `min(max_position_size × portfolio ÷ collateral, BP ÷ collateral, 10)` **contracts** (`put_seller.py:159`) — `max_stock_price × 100` bounds one *contract*, not the ticker. A 3-contract $130-strike put is $39k; on a larger portfolio, more. The true structural bounds per ticker are: (i) `max_position_size: 0.35` × portfolio per position (`put_seller.py:146` — live, and it *floats with equity*: ≈$36k today, $70k at a $200k account); (ii) one option position per underlying at a time (item 3's emergent invariant); (iii) `max_stock_price: 400` capping per-contract collateral. The delete is still right — the knob enforces nothing and the real bounds live elsewhere — but the equivalence documented (item 14 / gates doc) must be the corrected one, and the operator should see that the implied cap **grows with the account** where the dead knob was absolute. If an absolute per-ticker dollar cap is actually wanted, that is a *revive* into `select_batch` alongside item 1 — say so and it joins that PR.

**(d/e) Cost/blast radius.** S. Knob + property + validation + `tests/test_config.py:35` + k8s line. No `/config` or dashboard exposure (verified).

**(f) Decision:** ☐ confirm DELETE (with corrected documentation) ☐ flip to REVIVE-in-`select_batch` (absolute cap) — operator.

---

### Item 3 — `max_positions_per_stock: 1` · pre-tag: **DELETE knob; document emergent invariant**

**(a/b) What & verified status.** `settings.yaml:53`, property `config.py:311–313`, validation `:165–167`; preventive consumer `risk_manager.py:60` (dead, item 7); **detective mirror: regression monitor check 2** (`:831–853`, card 16) flags duplicate-underlying option positions hourly — using a hand-rolled leading-alpha underlying parser (item 12's family). FC-068 §11 flagged that this item's original "Today" rationale cited the stage-6 block — deleted by FC-068 — so the invariant is restated here against the live path: **one option position per underlying** emerges from (i) the scanner's put-side skip of any symbol with existing positions (`options_scanner.py:60–62` — item 12's check, currently *over*-blocking, so the invariant holds conservatively), (ii) `select_batch`'s `duplicate_underlying` drop (one per batch, both pools), (iii) the calls share ledger (no double-covering). Note the invariant is per-*position*, not per-*contract* — a single position may hold up to 10 contracts (item 2's arithmetic).

**(c) For/against.** *FOR:* dead knob, invariant real and triple-enforced on the live path (plus the detective mirror). *AGAINST:* the invariant's put half currently rests partly on item 12's **buggy** check — if item 12's rewire were done wrong (skip removed rather than fixed), put-stacking becomes possible with no knob left to catch it. Mitigation: item 12's spec keeps the skip (fixes matching only) and adds a test pinning "held symbol ⇒ no put opportunity emitted." **Caveat that MUST ride the documented invariant (rev 2 — else item 14 ships a new false claim): all three enforcing legs are positions-based and blind to resting unfilled orders** — a submitted-but-unfilled put is invisible to the scanner skip, the batch dedup (across cycles), and the share ledger alike; the invariant is "one *position* per underlying, modulo the open-order window" (FC-009's standing territory, same window FC-068 §1 documents). Document it with the caveat attached, verbatim. Documentation home: `docs/gates.md` if item 4's FC-013 ships; else item 14's rewritten `docs/CLAUDE.md` (see OQ-6).

**(d/e)** S. Knob + property + validation + `tests/test_config.py:33` + k8s line.

**(f) Decision:** ☐ confirm ☐ flip — operator.

---

### Item 6 — `min_hold_hours: 4` + `_entry_times` · pre-tag: **DELETE** (absorbs FC-015)

**(a/b) What & verified status.** Hold-period gate for profit-taking closes: knob `risk.profit_taking.min_hold_hours` (`settings.yaml:89`, property `config.py:408–410`); in-memory dicts `put_seller.py:31` / `call_seller.py:45`, populated at execution (`:381`/`:456`), read in `should_close_*_early` (`:547–550`/`:738–741`). **Dead since inception, confirmed:** sellers are constructed per request (`cloud_run_server.py:373–375` for `/run`, `:673–674` for `/monitor`), so the dict populated during `/run` is gone by `/monitor` — the gate reads an empty dict and falls open. Empirical: 4 of 35 sub-4h closes observed (2026-05-12 validation); the raised DTE-band profit thresholds (`settings.yaml:91–116`) are what actually carry the hold discipline.

**(c) For/against.** *FOR:* never worked; the load-bearing control (DTE bands) is live and validated; FC-015's own open question ("persist to GCS or query Alpaca?") is answered by this sweep's principle — if a hold gate is ever wanted, derive entry time from the position's Alpaca order history (`submitted_at`/`filled_at`) statelessly; don't resurrect state. *AGAINST:* deleting removes the scaffolding a future same-day-churn fix would slot into, and monitor churn is real (52% of call positions closed early). But scaffolding that silently fails open is worse than none — it reads as protection and isn't; the stateless derivation is a fresh S-sized FC if churn data ever demands it.

**(d/e)** S. Knob + property + both dicts + population sites + gate blocks; tests in `test_put_seller.py`/`test_call_seller.py` that pin the gate (part of the 2026-05-12 additions) are deleted; tests pinning DTE-band behavior untouched. Rev-2 blast-radius additions: `tests/test_clock_seam.py:59–65` (`test_min_hold_gate_blocks_when_simulated_time_is_too_soon` — dies with the gate) and the `tests/conftest.py:70–80` credentials-fixture docstring, which cites `profit_taking_min_hold_hours` as its example (docstring touch-up, not behavior). No `/config`/dashboard exposure (verified).

**(f) Decision:** ☐ confirm ☐ flip — operator.

---

### Item 9 — `call_drawdown_pause_threshold` knob + `/config` exposure · **confirmed-tagged DELETE** (sequencing only)

**(a/b) What & verified status.** `settings.yaml:34`, property `config.py:276–283` (docstring fixed by FC-068), `/config` field `cloud_run_server.py:1079`. The pause itself is dead by operator decision (FC-065 OQ-3); FC-068 deletes its last consumer and binding-constraint-2 leaves the knob to this item. Dashboard: **verified on the P4 branch** — `fc-065/phase-4-decision-record` removes `_drawdown_pause_threshold` (the only dashboard read of this key) and its replacement comment explicitly bequeaths the key to FC-069. One cosmetic residue: `CycleStatsCard.tsx:73` tooltip text still mentions "drawdown pause" in the FC-029 deploy note.

**(c) Not relitigated.** Sequencing: **after FC-065 P4 merges** — rev-2 mechanism correction (hygiene reviewer): deleting the `/config` field pre-P4 would not *break* the fetch — `v2.py:64–66` falls back to the `DRAWDOWN_PAUSE_THRESHOLD` env default when the key is absent — it would silently flip the card's `threshold_source` label to "env fallback" while displaying a threshold the bot no longer holds anywhere. Wrong-but-rendering, not broken; the after-P4 conclusion stands either way. Scope: knob + property + `/config` field + the stale tooltip phrase.

**(d/e)** S. Verify post-deploy: `/config` JSON no longer carries the key; dashboard Bot Health renders (P4's uncovered card unaffected — it reads decision records, not `/config`).

**(f) Decision:** ☐ confirm (timing per §Sequencing) — operator. *(No flip line: the deletion itself is already decided; only sequencing is at issue.)*

---

### Item 11 — opportunity blob `expires_at` · pre-tag: **DELETE field**

**(a/b) What & verified status.** Written at `opportunity_store.py:66` (`scan_time + 20 minutes`, hardcoded), echoed into one log event (`:86`), **never read**: both retrieval (`:140–150`) and `mark_executed` (`:242`) gate on `scan_time` vs `opportunity_max_age_minutes: 30` (`settings.yaml:18`). Confirmed — and the tell that it was never consumed: the hardcoded 20 disagrees with the operative 30 and nobody ever noticed. One test fixture carries the field (`tests/fixtures/scan_2026-07-17_14-00.json:3` — readers ignore unknown keys; update for tidiness).

**(c) For/against.** *FOR:* a field that looks like a TTL and isn't is a trap for the next reader. *AGAINST:* none of substance — old blobs retain the field harmlessly (reader never touches it), new blobs simply lack it; blob consumers are internal only.

**(d/e)** S. Field + log kwarg + fixture line.

**(f) Decision:** ☐ confirm ☐ flip — operator.

---

## Group D — rewires / docs

### Item 12 — scanner put-side `_has_existing_position` substring check · pre-tag: **REWIRE to canonical parsers**

**(a/b) What & verified status.** `options_scanner.py:593` (def; **inventory said :575 — line drifted, substance confirmed**), the bug at `:611`: `if position['asset_class'] == 'us_option' and symbol in position['symbol']` — a substring test over the whole OCC symbol. Called from `:61`, the put-side scan skip (silent — FC-068 §5's accepted tally gap). **7th OCC-substring instance** (after FC-041, FC-043, FC-045, FC-048, FC-052, and the engine's dead `_has_existing_position` that FC-068 deletes) — **and rev 2 adds an 8th site: the regression monitor** (card 16), whose checks 2/6/7/8 hand-roll a leading-alpha underlying parser (`regression_monitor.py:837,904,939,965`) and whose short-call tests are `"C" in symbol` (`:622,934–935,961`); its rewire to the canonical parsers rides card 16's sync commit, under this item's family lineage. Effect: any held PFE option blocks F puts (`'F' ⊂ 'PFE…'`); with the current universe the F/PFE pair is the live collision (both structurally untradeable by premium floor per FC-032, so realized impact ≈ nil today — this is correctness and family-completion, not recovered P&L). The error path returns `True` (fail-closed) — correct, keep it.

**(c) Recommendation: confirm.** Fix: parse the underlying with `parse_option_symbol(...)['underlying'] == symbol` (`src/utils/option_symbols.py:253`; consumer pattern at `execution_engine.py:164–165`). Do not hand-roll — seven instances is the argument. **Ride-along, recommended:** give the skip a structured event (`put_scan_skipped_existing_position`) — FC-068 §5 documents the replay tally as blind to this exact skip "until FC-069 item 12 rewires that check"; this is the moment. Event name joins the rejections vocabulary + a tally mapping. *Against the ride-along:* it grows a "rewire" into a small behavior-visible change; but a silent gate on the money path is the pattern this whole FC family exists to end.
- *Strongest AGAINST the rewire itself:* the substring over-block is *conservative* — it can only suppress trades, never permit a bad one, and fixing it slightly *loosens* the scanner (F puts become possible while PFE options are held). That loosening is precisely correct behavior, but it is a live behavior change and gets tested + two-reviewed accordingly, not waved through as cleanup.

**(d/e)** S. Tests: held-stock skip still fires (exact match); held F option blocks F (parse match); held PFE option **no longer** blocks F (must FAIL pre-fix — mutation check); API-error path still fail-closed; event emitted with reason. Blast radius: none beyond the scanner + tally vocab.

**(f) Decision:** ☐ confirm (with event ride-along) ☐ confirm (rewire only, no event) ☐ flip — operator.

---

### Item 14 — `docs/CLAUDE.md` rewrite · pre-tag: **REWRITE**

**(a/b) What & verified status.** The repo's guidance file describes a system that does not run. **Every claim below verified false or stale against the post-FC-068 codebase** (this is the required fix-list for the rewrite):

1. **"Core Strategy Flow: 1. `WheelEngine` orchestrates the complete strategy"** — post-FC-068, `WheelEngine` = `reconcile_positions` + `run_rolling_cycle` only. The flow that trades is `/scan` → `OptionsScanner` → GCS blob → `/run` → `ExecutionEngine.rank/select_batch/execute_batch` → sellers' `execute_*`, + `/monitor`, + Friday `/roll`. Absent from the doc entirely.
2. **"5. `RiskManager` validates all positions against limits"** and **"Risk validation required before any trade execution"** — false since inception (item 7 evidence: zero call sites). Live validation = scanner filters + `market_data` criteria + selection ledgers + FC-050 execute-time floor.
3. **"6. `PortfolioTracker` monitors performance and generates reports"** — CLI-only (`main.py status/report`); not on the deployed path.
4. **Key Components: "WheelEngine — Core orchestrator"; "RiskManager — Position validation and risk controls"** — same two falsehoods restated; RiskManager's live surface is `validate_roll` only (roller-dependent, FC-066).
5. **"Conservative Deltas: 0.10–0.20 range"** — put-only since FC-029 R1; calls are `[0.15, 0.25]` (`settings.yaml:25,28`).
6. **"Position Sizing: Maximum 1 contract per new position, 10% portfolio allocation"** — false: sizer caps at **10 contracts** (`put_seller.py:159`) and `max_position_size` is **0.35** (`settings.yaml:67`).
7. **"Assignment Strategy: … protect calls"** + **"Risk Management Philosophy — Calls: Protected with stop losses… 75% loss threshold… Delta > 0.5 triggers immediate exit"** — false since FC-010: `use_call_stop_loss: false` (`settings.yaml:72`). Live call management = DTE-banded profit-taking (validated `1c26b43`) + the cost-basis floor (FC-050/065) + hold-until-recovery below the floor (FC-065 governing principle).
8. **Commands** — `--command run` examples already deleted by FC-068's PR; item 14 removes FC-068's interim annotation line ("full docs rewrite is FC-069 item 14") by *being* that rewrite.
9. **Rev-2 additions (hygiene reviewer):** **(i)** `docs/CLAUDE.md:164`'s "(dividends and early assignment are unmodeled)" is **false today** — both are modeled since FC-042 Track C (PR #50; `broker.py:299–316` `assign_call_early`/`credit_dividend`, `simulator.py:105,344`); the parenthetical predates the wave-1 merge. Fix it in the rewrite (and if the rewrite lands materially after this plan, fix it as a one-line docs commit sooner — a false caveat that *understates* the engine is still a false claim). **(ii)** The Data Analysis Policy figures — 81% / 55.2% / 0.676 and "8 of 14 configured symbols currently cannot trade" — go stale at FC-068's re-measurement (its §10 marks the parity figures stale pending re-run) and drift with any universe/config change: either de-exhaustify (point at `BACKTEST_ENGINE.md` as the single home of measured figures, quote none inline) or accept the maintenance duty explicitly. Recommended: de-exhaustify — one home per number.
10. **The list-header discipline (rev 2):** this claims list was audited against `main` on 2026-08-01; item 14's build step is to re-walk the full file against the *then-current* codebase, not to treat this list as exhaustive — the two reviews found claims rev 1 missed, which is the argument.
11. **Additions the rewrite owes:** the corrected per-ticker bound (item 2's falsification), the emergent one-position invariant **with its open-order caveat verbatim** (item 3), the accepted amnesiac state (item 15), the detective-layer description per card 16's outcome, and pointers to `docs/gates.md` if item 4 ships FC-013 (else the gate inventory lands here — OQ-6).

**(c) For/against.** *FOR:* this file is loaded into every Claude session on this repo — false architecture claims actively misdirect future work (the fictional layer got ten months of remediations partly because the docs said it was real). *AGAINST:* none for doing it; the only real question is **when** — it must be the sweep's *last* act, because its content depends on the outcome of every card above.

**(d/e)** S–M, docs-only, direct-to-main permitted. Also sweep `docs/README.md`/`INSTALL.md` for `--command run` echoes at build.

**(f) Decision:** ☐ confirm ☐ flip — operator.

---

## Group E — holds / notes

### Item 13 — `rolling.*` knobs · pre-tag: **HOLD** (FC-066's decision, not this plan's)

**(a/b) What & verified status.** **12 keys** under `rolling:` (`settings.yaml:205–218`) — *inventory said 11; verified count is 12 including `enabled`*. Consumed by `call_roller.py` (live Friday `/roll` path, structurally no-op'd by the quote-key bug — FC-066 cause 1). FC-066's entry **on `fc-065/phase-2-roller-floor`** (merging Monday) now carries a three-item pre-revival checklist (2026-08-01, from the P2 review): wire both roll-path cost-basis skip events into the deployed alert policy; give the pre-guard silent skips structured events; keep the replay-BQ-gate forwarding. That checklist plus FC-062's remaining `execute_roll` gate-routing are FC-066's build contract.

**(c) This plan's only obligations:** (i) do not delete anything the roller path consumes — enforced in items 4 (earnings service), 7 (`validate_roll` kept), 8 (roller state methods held to stage 2); (ii) hand FC-066 the cross-references above. Nothing else. If FC-066 decides against revival, the knobs, `validate_roll`, the roller's earnings gate, and item 8's stage-2 scope all become deletable — under FC-066's lineage, not this FC's.

**(f) Decision:** ☐ confirm HOLD — operator. *(A flip here would mean deciding the roller's fate inside FC-069 — explicitly out of bounds.)*

---

### Item 15 — process-local fragile state (`_closed_today`, `_failed_symbols`, `strategy_status`) · pre-tag: **NOTE only**

**(a/b) What & verified status — all three live and amnesiac, confirmed:** `_closed_today` (`cloud_run_server.py:67`; monitor dedup at `:721/:791`) — resets on cold start; duplicate close orders remain possible; **FC-009 owns it** (still open, scope reconfirmed post-FC-043). `_failed_symbols` (`execution_engine.py:62`, module-global) — non-retryable failure suppression; clears per cold start; FC-068 §4 already specs its replay handling (per-day clear + snapshot/restore). `strategy_status` (`cloud_run_server.py:54`; served by `/status` `:133`) — last-run bookkeeping, resets silently.

**(c) Recommendation: confirm NOTE.** These are *live* controls with a known, accepted weakness — not fiction. Fixing `_closed_today` is FC-009's M-sized job (check Alpaca for open buy-to-close orders — the cold-start-proof option); the other two are accepted operational amnesia. The sweep's obligation is honesty, not repair: item 14's rewrite documents all three as accepted per-instance state. *Against:* one could argue this sweep should just fix FC-009 while it's here — rejected: it's a behavior change on the monitor path with its own design question, and bundling it would break one-PR-per-FC for a real money-path change.

**(f) Decision:** ☐ confirm NOTE ☐ flip (pull FC-009 forward as its own plan) — operator.

---

### Addendum A — `src/risk/position_sizing.py` · bequeathed by FC-068 §11 · recommendation: **DELETE**

Not on the original 15 — FC-068's plan verified it **import-orphaned** (zero importers outside `tests/test_position_sizing.py`; re-verified this session; 414 lines) and directed it onto this inventory. Its `position_size_validation_failed` emitter is already dropped from the rejection taxonomy by FC-068. The live sizer is `PutSeller._calculate_position_size`. Delete module + its test file in the sweep PR. **Decision:** ☐ confirm ☐ flip — operator.

---

### Card 16 (rev 2 — both reviewers' top finding) — the `/regression` detective layer · recommendation: **SYNC per-card, in the same PRs**

**(a) What it is.** `tools/testing/regression_monitor.py` (1,207 lines) is **not a dev tool**: `deploy/cloud_run_server.py:1643` serves it at `/regression`, invoked **hourly at :45 during market hours** by Cloud Scheduler (`regression-hourly` — documented in `docs/releases/RELEASE_2026-04-07.md`; job existence live-verified at build, `gcloud` unavailable this session). Any check with status `fail` makes the endpoint return **HTTP 500** (`cloud_run_server.py:1687`). Its `check_risk_parameters` (`:777–1006`) is a **detective mirror of the policy layer this sweep decommissions** — rev 1 missed it entirely, which means rev 1's "zero live behavior change" claims for S1 were true of the *trading* path but false of the *alerting* path.

**(b) Verified check-by-check, against the cards that own each policy:**

| # | Check (lines) | Mirrors | Severity | Owning card → what the sync does |
|---|---|---|---|---|
| 1 | total option positions > 10 (`:817–829`) | `max_total_positions` | **fail → 500** | Item 1 — if revived: re-source from `Config` (it runs in-process, `Config()` is importable) and match the cap's semantics (short-only count, soft-cap shape) or it false-alarms on the first uncapped call write; if deleted: delete the check |
| 2 | duplicate underlyings (`:831–853`) | `max_positions_per_stock` | fail → 500 | Item 3 — invariant survives, check stays; parser rewired (item 12 family); inherits the open-order blindness already documented |
| 3 | cash < 20% (`:856–867`) | `min_cash_reserve` | warn only | Item 7 rider — dies or stays with the knob |
| 4 | position > 35% (`:870–882`) | `max_position_size` | fail → 500 | Live policy — **keep**, re-source from `Config` instead of the hardcoded 0.35 |
| 5 | allocation > 80% (`:884–897`) | `max_portfolio_allocation` | warn only | Card 17 — dies or stays with the knob |
| 6 | ticker exposure > $40k (`:899–923`) | `max_exposure_per_ticker` | **fail → 500** | Item 2 — delete with the knob, **before** the account crosses ≈$114k (card 2's arithmetic); note it measures market value, not the dead knob's assignment notional — the mirror never even mirrored faithfully |
| 7 | naked-call detection (`:924–956`) | FC-041's guard concept | fail → 500 | **Keep** — real protection; parser + `"C" in symbol` rewired to canonical (item 12 lineage) |
| 8 | call strike < cost basis (`:958–987`) | the FC-050/065 floor | fail → 500 | **Keep** — but re-spec: it derives basis from `cost_basis/qty` (`:978`), the exact field FC-029 proved returns 0 for assigned positions (the `> 0` guard then skips silently — the detective floor check is *blind on precisely the assigned lots it exists for*), and the decided floor is `avg_entry_price`. Align to the resolver's source |

**(c) Recommendation: SYNC (per-card outcome, in the same PR as each card), not delete, not leave.**
- *Why not leave as-is:* the sweep makes checks 1/3/5/6 mirrors of policies that no longer exist; check 6 will begin 500ing hourly on a healthy ≈$114k account regardless of the sweep. An alarm layer that cries wolf gets muted — that is how FC-031 sat behind a red build for 11 days.
- *Why not delete wholesale:* checks 4/7/8 are the only *continuously running* verification of live invariants (sizing, no naked calls, no below-basis calls), and item 1's revived cap gets its natural post-deploy verifier here for free. Detective coverage of preventive controls is exactly what a one-operator system wants more of, not less.
- *Cost:* each owning card's PR carries its sync lines (S per PR); checks 4/7/8's re-source/re-spec rides S1 (or a dedicated small commit within it). Tests: the monitor has none for `check_risk_parameters` — the sync adds shape tests per surviving check (fires-on-violation, mutation-checked, per the standing rule).

**(f) Decision:** ☐ confirm SYNC-per-card ☐ delete `check_risk_parameters` entirely (keep the endpoint's other checks) ☐ leave as-is (rejected above — signs up for hourly false 500s) — operator.

---

### Card 17 (rev 2) — the second knob stratum · recommendation: **DELETE all** (rides S1)

**(a) What it is.** Dead keys the original inventory never listed — found by the reviews and confirmed complete by the knob-census appendix:

| Key(s) | Verified status |
|---|---|
| `risk.max_portfolio_allocation: 0.80` (`settings.yaml:66`, property `config.py:337–339`, validation `:177–179`) | Zero preventive consumers; regression monitor check 5 hardcodes its own 0.80 (warn-only); `scripts/testing/test_issue_audit.py:77` prints it |
| `risk.max_iv_rank: 80` / `min_iv_rank: 20` (`settings.yaml:135–136`) | **No `Config` property even exists** — yaml-only corpses; no IV-rank computation anywhere in `src/` |
| `risk.profit_target_percent: 0.50` (`settings.yaml:132`, property `config.py:377–379`) | Self-labeled "legacy/deprecated" in both files; zero consumers; superseded by `profit_taking.*` since the DTE-bands work |
| `monitoring.*` — all 7 keys (`settings.yaml:190–202`) | `check_interval_minutes` has property+validation, zero consumers — **and reads as live cadence config while the real cadence is Cloud Scheduler**, a standing operator trap; the other six (`assignment_alert`, `profit_target_alert`, `loss_threshold_alert`, `enable_cloud_logging`, `max_execution_time_minutes`, `enable_performance_metrics`) have zero readers of any kind |
| `logging.*` — all 5 keys (census discovery, beyond the reviews) | `Config` never exposes the section; `setup_logging` takes CLI args (`main.py:46`) or hardcodes (`cloud_run_server.py:1708–1717`); the yaml block configures nothing |
| `earnings.blackout_days: 2` (property `config.py:580–582`) | Zero consumers — the roller uses `rolling.earnings_blackout_days` (`call_roller.py:80`); this key exists *for* FC-013. **Not deleted here:** it is item 4's revival knob; its fate follows that decision (REVIVE → becomes live in FC-013 rev 2; DELETE → dies with the FC-066-deferred remainder) |

**(c) For/against.** *FOR:* every entry verified consumer-less (or consumer-is-a-hardcoded-mirror); `check_interval_minutes` and the `logging.*` block actively mislead an operator reading settings.yaml as documentation of behavior. *AGAINST:* nothing of substance — none has semantics anything enforces or ever did; `profit_target_percent` deletion requires the property's removal to not break `Config` imports (mechanical). The k8s yaml carries `max_portfolio_allocation`, `profit_target_percent`, `max_iv_rank`, `min_iv_rank` copies (OQ-8's disposition covers them).

**(d/e)** S, inside S1: 15 yaml keys + 3 properties + 2 validation blocks + regression-monitor check 5 (card 16) + script prints (§CLI surface). FC-014's closing text names this card for its "portfolio allocation" check (§Closing texts — rev-2 completion).

**(f) Decision:** ☐ confirm DELETE-all ☐ flip (name survivors: ___) — operator.

---

## CLI surface & scripts (rev 2 — both reviewers)

Rev 1 called the CLI path "already-dead" — imprecise: **`main.py status/report` → `PortfolioTracker` is a live, working CLI path**, and rev 1's "zero behavior change" held only because its config reads sit inside swallow-all `try/except` (silent degradation is not "no change"). Spec for the delete forks:

- `portfolio_tracker.py:390` (`min_cash_reserve` recommendation): delete the recommendation branch if the item 7 rider deletes the knob.
- `portfolio_tracker.py:401` (`max_total_positions` 80% warning): if item 1 revives, keep (reads the surviving knob); if deleted, remove the branch.
- `portfolio_tracker.py:440` (config snapshot export): drop deleted keys from the export dict.
- `scripts/testing/test_trading_workflow.py:35` (prints `max_total_positions`): follows item 1 — edit or delete the line.
- `scripts/testing/test_issue_audit.py:45–93` (prints/audits `max_exposure_per_ticker`, `min_cash_reserve`, `max_portfolio_allocation`, `max_total_positions`): this script is an **audit of the fictional policy layer itself** — post-sweep its premise is gone; **delete the script** (its useful residue, account-vs-config arithmetic, is superseded by the regression monitor's live checks).
- `scripts/` **joins the grep gate** (§Verification) — rev 1's gate list omitted it, which is how these two scripts were missed.

---

## Closing texts (FC entries absorbed by this FC — applied at each item's merge, with lineage)

- **FC-014** → *"Closed 20XX-XX-XX by FC-069 item 7 (operator decision: retire). `validate_new_position` and its five sibling methods (six in all — `_validate_option_specific_risks`, `calculate_portfolio_risk_metrics`, `should_reduce_positions`, `get_emergency_stop_conditions`, `check_emergency_conditions`) were deleted with zero production call sites ever recorded; `validate_roll` survives as the roller's gate (FC-066's turf). All five checks this entry named are dispositioned: the global position cap by FC-069 item 1 [revived in `select_batch` / deleted], per-ticker exposure by item 2 (deleted; corrected structural bound documented), per-stock cap by item 3 (deleted; emergent invariant documented with its open-order caveat), cash reserve by the item 7 rider [deleted with its detective check / kept advisory / wired], and portfolio allocation by card 17 (knob deleted; its warn-only detective mirror handled per card 16). The account-level loss-limit concepts inside the emergency-stop methods were dispositioned by item 7's sub-decision [deleted undocumented / carried into FC-0NN, the account-level kill-switch entry]. The consolidate fork died with the method: enforcement on this system is distributed by design — scanner filters, selection ledgers, execute-time floor, plus the hourly `/regression` detective layer (card 16). Plan: `docs/plans/fc-069.md`, PR #NN."*
- **FC-015** → *"Closed 20XX-XX-XX by FC-069 item 6 (operator decision: delete). `_entry_times` never survived a request (sellers are per-request), so the 4h gate has been open since inception (2026-05-12 evidence: 4/35 sub-4h closes); the raised DTE-band thresholds carry the hold discipline. The knob, dicts, and gate code are deleted. If a hold gate is ever wanted, derive entry time statelessly from Alpaca order history (`filled_at`) — do not persist process state; that answer supersedes this entry's GCS-vs-Alpaca open question. Plan: `docs/plans/fc-069.md`, PR #NN."*
- **FC-039** → *"Closed 20XX-XX-XX by FC-069 item 8 (operator decision: delete the illusion, per the coherence principle — derive from durable truth). Persistence was never enabled and is not wanted: `reconcile_positions` rebuilds from Alpaca per request (this entry's own observation), the floor now resolves from Alpaca `avg_entry_price` (FC-065 P1) with no wheel_state source, and FC-066's roller direction is stateless-from-Alpaca. Stage 1 removed the orphaned CallSeller state plumbing; stage 2 [shrank `WheelStateManager` to reconcile's in-request bookkeeping / pending FC-066]. The startup-assertion question generalizes beyond this FC and is noted in FC-069's plan for any future persistence target. Plan: `docs/plans/fc-069.md`, PR #NN."*

FC-069's own entry updates per-item as decisions execute; FC-049's entry gains the item-5 disposition (incl. the code-lives-at-SHA note if option ii); FC-013's entry gains the rev-2 obligation if item 4 confirms.

---

## PR partitioning & sequencing

One-PR-per-FC yields to rollback granularity here, deliberately: bundling live behavior changes with dead-code deletion would destroy the clean-revert property that rule exists to protect. Proposed partition — **each PR two-reviewer gated** (all touch production paths or money-path decisions):

| PR | Contents | Nature | Sequenced after |
|---|---|---|---|
| **S1 — the knob sweep** | Items 2, 3, 6, 7 (+rider), 9, 11, Addendum A, **card 17**, item 4's `earnings_avoidance_days`, item 5's shadow knob (+module if option ii, **+`config_hash` key-list edit**), **card 16 sync: delete checks 3/5/6 (+1 if item 1 flips), re-source 4, re-spec 8 to `avg_entry_price`, canonical parsers (+shape tests)**, §CLI-surface edits | Dead code/config + detective-layer sync — **zero live *trading*-path behavior change**; `/regression` responses change by design (rev 2 honesty fix) | FC-068 merge + FC-065 P4 merge (item 9's dashboard dependency) |
| **S2 — `wheel_cycles` retirement** | Item 10 code half | Live-path file, behaviorally inert change | S1 (or parallel); step-0 BQ gate first; operator runs snapshot+drop after 7 clean days |
| **S3 — scanner rewire** | Item 12 (+skip event) | **Live behavior change** (un-over-blocking + new event) | FC-068 (its replay then reproduces the *fixed* check; tally mapping added here) |
| **S4 — cap revival** | Item 1 (if confirmed) **+ card 16 check-1 sync to the cap's chosen semantics — same PR, mandatory pairing** | **Live behavior change** on the money path | S1 (clean base); independent of S2/S3; value informed by the item-1 audit query |
| **S5 — state shrink stage 1** | Item 8 orphaned-CallSeller half | Zero live behavior change (verified orphan) | S1 |
| **S6 — state shrink stage 2** | Item 8 full shrink | Live `reconcile_positions` touch | **FC-066 decision** |
| **(FC-013 rev 2)** | Item 4 revival | Own FC, own plan rev, own PR — not an FC-069 PR | operator confirm of item 4 + the earnings-exposure audit query |
| **(kill-switch FC)** | Item 7 emergency-stop sub-decision, if the file-an-FC fork is chosen | New S-sized FC entry only (no code in this sweep) | operator sign-off |
| **Docs (direct-to-main)** | Item 14 rewrite + all closing texts not yet applied | Docs only | Last — after S1–S4 land and item 1/4 decisions are recorded |

Items 13, 15: no PR. Recommended order: S1 → S2/S3 (parallel) → S4 → S5 → docs; S6 and FC-013 rev 2 on their own clocks.

---

## Verification (standing rule: every claim shown, not asserted)

- **Every delete:** FC-068-style global grep gate — each deleted symbol/knob grepped across `src/ deploy/ config/ main.py tests/ tools/ scripts/ docs/ dashboard/` (**`scripts/` added in rev 2** — its omission is how rev 1 missed two breaking scripts); every remaining hit is an annotated historical doc or a defect; grep output pasted into the PR.
- **Deleted knobs with `/config` exposure (items 1-if-deleted, 5, 9):** post-deploy `curl /config` diff showing the field absent; dashboard smoke (Bot Health + Overview render; P4 uncovered card unaffected).
- **`/regression` post-deploy (rev 2, card 16):** after S1, one manual `/regression` invocation returns **200** with the synced check set — no check names a deleted policy; after S4, check 1 reflects the revived cap's semantics. The hourly scheduler job's existence and cadence verified via `gcloud scheduler jobs describe regression-hourly` at build (unverifiable this session).
- **Item 1 (if revived):** the cap **FIRES** — unit test at the boundary (mutation-checked: weaken the count, test fails); plus a staged verification against a positions snapshot ≥ cap (synthetic snapshot through `select_batch` directly — production won't naturally hit 10 on demand, and we do not manufacture live positions to prove a guard). **Marked audit query (feeds the cap-value decision, run before sign-off executes):** daily max concurrent short option positions reconstructed from `trades_from_activities` open/close/expiry events — reported as `MAX(concurrent)` and its date, plus the p95, in the S4 PR description.
- **Item 4 (marked audit query, feeds the decision):** sell-to-open fills within `blackout_days=2` of an actual earnings date (join against `HistoricalEarningsCalendar`'s committed table), with per-exposure realized P&L vs book average — in the sign-off record before item 4's box is ticked.
- **Item 10 — the two gates, verbatim (rev 2; the S2 PR description must carry these queries and their outputs):**
  - *Audit (resolves the 36/31/7 vs 36/30/6 discrepancy on the record):*
    ```sql
    SELECT symbol, assignment_date, COUNT(*) AS rows,
           COUNTIF(capital_gain = 0 OR capital_gain IS NULL) AS zero_gain_rows
    FROM `options_wheel.wheel_cycles`
    GROUP BY symbol, assignment_date
    ORDER BY rows DESC;
    ```
  - *Step-0 reader gate (FC-035 pattern — must return zero rows before the writer is deleted):*
    ```sql
    SELECT table_name FROM `options_wheel.INFORMATION_SCHEMA.VIEWS`
    WHERE LOWER(view_definition) LIKE '%wheel_cycles%'
      AND LOWER(view_definition) NOT LIKE '%wheel_cycles_from_activities%';
    ```
    plus the scheduled-queries listing (`bq ls --transfer_config`) showing no reference. Post-deploy 7-day check: zero new rows; then snapshot + drop (§Rollback).
- **Item 12:** the four tests in card (d), PFE/F case failing pre-fix (mutation check in PR); replay tally shows the new skip reason.
- **Items 7/8:** full suite green; reconcile's event contract pinned by test before any shrink commit; roller tests untouched and green (S5 must not touch roller behavior).
- **Every PR:** production no-op check where claimed — `/scan` + `/run` manual cycle post-deploy with unchanged opportunity counts/shapes (S1, S2, S5); changed-behavior memo where not (S3, S4).

## Rollback

- **S1–S5 each:** single `git revert` of the squash commit. Config knobs ship in the image with the code (settings.yaml is baked in) — no config/code skew window. No schema or data migrations in any sweep PR.
- **Item 10's table:** `bq cp options_wheel.wheel_cycles options_wheel.wheel_cycles_pre_fc069_snapshot` before `bq rm`; snapshot retained ≥ 30 days; restore = `bq cp` back + revert S2. Recipe in the PR description per the corrective-write discipline.
- **Item 1 (if revived):** revert S4 restores capless selection; no data dependency.
- **Docs commit:** revert restores prior text; closing texts re-opened by editing the FC entries (paper trail preserved either way).

## Risks & mitigations

- **Risk: this plan's "post-FC-068" baseline shifts if FC-068's build deviates from its plan.** Mitigation: S1 does not start until FC-068 merges; each card's symbol names (not line numbers) are the contract; any FC-068 build deviation touching a card is re-verified before that card's PR.
- **Risk: a "zero live behavior change" delete isn't (a hidden consumer).** Mitigation: the grep gate (now including `scripts/`) + the knob-census appendix + full suite + post-deploy no-op cycle and `/regression` check per PR. The tally of claims corrected across rev 1 and rev 2 — item 2's equivalence, item 5's "only consumer," rev 1's own "nothing counts positions," the missed detective layer, the missed second stratum — is the argument for keeping that gate mechanical, not judgment-based.
- **Risk: item 8 stage 2 breaks `/run` housekeeping.** Mitigation: event-contract test first; stage 2 held behind FC-066; two-reviewer gate with a reliability persona.
- **Risk: decision fatigue — 15 cards invite rubber-stamping.** Mitigation: the four cards where flipping is most live (1, 4, 5-module, 7-rider) are ordered first in their groups and carry explicit sub-decision lines; everything mechanically dead is grouped and summarized.

## Open questions — flagged for the plan reviewers

1. **(Blocking, item 5)** The pre-tag under-decides: module fate (keep-annotated vs delete-with-SHA-pointer) is a genuine operator choice this plan recommends (ii) on — reviewers should pressure-test the "git is the archive" argument.
2. **(Blocking, item 4)** Is REVIVE-via-FC-013-rev-2 the right vehicle, or should the earnings gate be re-planned inside FC-069 to avoid a third FC-013 deferral? Plan says FC-013 keeps it (one-PR-per-FC, existing plan capital); the counterargument is FC-013's two-year drift record.
3. **(Non-blocking, item 1)** The revived cap's three sub-decisions — value (informed by the marked audit query), shape (total-count / per-batch / both), call-counting (recommended: calls count toward, never blocked by) — all on the card's Decision line.
4. **(Non-blocking, item 2)** Confirm the corrected structural-bound wording; and does the operator want an absolute per-ticker cap revived instead (it would join S4)?
5. **(Non-blocking, item 7)** Rider: `min_cash_reserve` delete vs wire-as-BP-haircut.
6. **(Non-blocking, items 3/4/14)** Documentation home if item 4 flips: `docs/gates.md` never shipped; fallback is item 14's rewrite carrying the gate inventory.
7. **(Non-blocking, item 10)** Row-count discrepancy (36/31/7 vs 36/30/6) — resolved by the build-time audit query; flagged so reviewers don't treat either number as verified.
8. **(Non-blocking, global)** `deploy/kubernetes/deployment.yaml` carries stale copies of three swept knobs (and a divergent `50000`); it is not the deploy path (Cloud Run is). Delete the file / its strategy-config block in S1, or leave with a stale header? Plan recommends deleting the strategy-config block in S1.
9. **(Non-blocking, item 13)** Verified `rolling.*` count is 12 keys (inventory said 11) — carry the correction into the FC entry at merge bookkeeping.
10. **(Non-blocking, card 16 — rev 2)** The SYNC recommendation keeps checks 4/7/8 and adds shape tests; the alternative (delete `check_risk_parameters` wholesale) trades detective coverage for simplicity — reviewers upheld SYNC, operator confirms on the card.
11. **(Non-blocking, item 7 — rev 2)** If the file-an-FC fork is chosen for the emergency-stop concepts, the new entry needs thresholds chosen from the account's actual drawdown history, not the dead code's inherited 15%/5% — noted so the FC isn't filed as a copy-paste of the corpse.
12. **(Non-blocking, scheduler — rev 2)** `regression-hourly`'s cadence is documented in `RELEASE_2026-04-07.md` but was not re-verifiable this session (`gcloud` auth unavailable); the build re-verifies the job before relying on the sync's post-deploy checks.

## Appendix — the knob census (rev 2, both reviewers' structural ask)

Every leaf key in `config/settings.yaml` (**78 total**), against its *verified* consumer — grep-verified 2026-08-01 on `main` `570f2c5`. This is the mechanical sweep that catches what per-item inventories miss (it found the `logging.*` block neither review named), the authoritative source for item 14's rewrite, and the table that makes a third inventory unnecessary: **any future key must land with its consumer named here, or it is born a corpse.**

**Headline: 41 keys have a live consumer (23 trading-path + 3 dormant-behind-off-switch + 15 roller-only) · 37 are dead (2 die in FC-068, 35 are this sweep's).**

### Stratum 1 — live, trading path (23) — untouched by this sweep

| Key | Consumer |
|---|---|
| `alpaca.paper_trading` / `api_key_id` / `secret_key` | client auth (`config.py:237–247`) |
| `strategy.opportunity_max_age_minutes` | `opportunity_store.py:140,242` |
| `strategy.put_target_dte` / `call_target_dte` | `market_data.py` chain selection (+ scanner DTE score) |
| `strategy.put_delta_range` / `call_delta_range` | `market_data.py` filtering (+ `/config`) |
| `strategy.min_put_premium` / `min_call_premium` | `market_data.py:586,628` |
| `strategy.min_stock_price` / `max_stock_price` | `market_data.py:86,142` |
| `strategy.min_avg_volume` | `market_data.py:90,144` |
| `risk.max_position_size` | `put_seller.py:146,194` (+ detective check 4) |
| `risk.use_put_stop_loss` / `use_call_stop_loss` | `put_seller.py:570` / `call_seller.py:761` (live reads; both `false` → branches gated off) |
| `risk.profit_taking.use_dynamic_profit_target` / `static_profit_target` / `dte_bands` / `min_profit_target` / `max_profit_target` / `default_long_dte_target` | both sellers' `_get_profit_target_for_dte` (monitor path; properties renamed `profit_taking_*`, `config.py:385–405` — a grep for the yaml key alone misses them, which is why this census greps both names) |
| `stocks.symbols` | scanner/market_data universe (+ `/config`) |

### Stratum 2 — live-dormant (3) — read on the live path, inert behind an off-switch; not swept

`risk.put_stop_loss_percent`, `risk.call_stop_loss_percent`, `risk.stop_loss_multiplier` — consumed inside the stop-loss branches (`put_seller.py:601`, `call_seller.py:792`) that `use_*_stop_loss: false` gates off (FC-010). They become live the day a switch flips; deleting them would silently change what flipping the switch does. **Kept, documented as dormant in item 14's rewrite.**

### Stratum 3 — roller-only (15) — live Friday invocation, structurally no-op; item 13 HOLD, FC-066 owns

`rolling.*` (12 keys, `settings.yaml:205–218`) — `call_roller.py`; `earnings.enabled` (`wheel_engine.py:1224`), `earnings.cache_ttl_hours`, `earnings.lookahead_days` (`earnings_calendar.py:58–59`).

### Stratum 4 — dead (37): 2 × FC-068, 35 × this sweep

| Key(s) | Card | Note |
|---|---|---|
| `strategy.max_stocks_evaluated_per_cycle`, `max_new_positions_per_cycle` | **FC-068** | die with their sole consumers |
| `strategy.max_total_positions` | 1 | detective mirror hardcodes 10 (card 16) |
| `strategy.max_positions_per_stock` | 3 | detective mirror (card 16) |
| `strategy.max_exposure_per_ticker` | 2 | detective mirror hardcodes $40k — the ≈$114k time bomb |
| `strategy.call_drawdown_pause_threshold` | 9 | `/config` field; P4 removes the dashboard read |
| `risk.min_cash_reserve` | 7 rider | detective check 3 (warn-only); CLI recommendation branch |
| `risk.max_portfolio_allocation` | 17 | detective check 5 (warn-only) hardcodes 0.80 |
| `risk.profit_target_percent` | 17 | self-labeled deprecated |
| `risk.max_iv_rank`, `min_iv_rank` | 17 | yaml-only; no `Config` property exists |
| `risk.profit_taking.min_hold_hours` | 6 | gate dead since inception |
| `risk.gap_risk_controls.*` — 12 keys (incl. `earnings_avoidance_days`) | 5 (+4 for the earnings one) | 3 keys also in `bq_writer.config_hash` (item 5 disposition) |
| `monitoring.*` — 7 keys | 17 | `check_interval_minutes` reads as live cadence config; real cadence is Cloud Scheduler |
| `logging.*` — 5 keys | 17 | census discovery — `Config` never exposes the section |
| `earnings.blackout_days` | 4 | FC-013's revival knob; fate follows item 4 (roller uses `rolling.earnings_blackout_days`, not this) |

Count check: 23 + 3 + 15 + 37 = 78. ✓

**Standing rule this appendix creates (proposed for item 14's rewrite):** a config key with no consumer named in this table's successor doc is a defect; the census is re-run (one grep script, committable to `tools/diagnostics/` if wanted) whenever a settings key is added or removed.

---

## Execution

_Filled in after operator sign-off and per-PR implementation._

### S1 — the knob sweep · **DONE (merged `de64b51`, deployed rev 00473-gez, live-verified 2026-08-04)**

- **PR:** https://github.com/memon1987/options_wheel/pull/83
- **Branch:** `fc-069/s1-knob-sweep`
- **Commits:** `095a121` (delete the dead policy layer) → `7d1bc8b` (sync the
  `/regression` detective layer + test fallout) → `2bcbcba` (grep-gate cleanup)
  → `6e78d11` (review fixes) → `227f371` (bookkeeping; branch tip). Squash
  merge: **`de64b51`**. Post-merge hotfix on main: **`388470d`** — the S1
  tooltip annotation in `CycleStatsCard.tsx` used JSX-comment syntax in an
  expression position, breaking the dashboard image build (first Cloud Build
  for `de64b51` FAILED). Two-line fix; tsc clean; 77 frontend tests green.
  **Process lesson recorded: builder + both reviewers ran pytest only — every
  later sweep PR touching `dashboard/` must run `npx tsc --noEmit` before
  merge.**
- **Confirmation pass:** CONFIRMED-CLEAN (scoped, both fix commits; independent
  mutations on the warn-emission and the pre-pass handoff both caught).
- **Post-deploy verification (all green, 2026-08-04):** `/config` via dashboard
  proxy shows `max_positions`/`gap_thresholds`/`call_drawdown_pause_threshold`
  ABSENT; scheduled `regression-hourly` 12:45 ET firing returned **200** on rev
  00473-gez (any FAIL would 500 — the synced check set passes on the live
  book); `regression-hourly` job ENABLED at documented cadence; dashboard
  smoke 200s (`/api/v2/overview`, `/api/v2/bot-health`, `/api/live/status`);
  no-op cycle confirmed — 12:00 ET (pre-deploy) vs 13:00 ET (post-deploy)
  scans identical at **3 put / 10 call opportunities** with identical event
  sequences (PFE earnings block still firing), and the 13:15 ET `/run`
  executed the full selection pipeline normally.
- **Date:** 2026-08-04
- **Contents:** items 2, 3, 6, 7 (+`min_cash_reserve` rider), 9, 11, Addendum A,
  card 17, item 4's `earnings_avoidance_days`, item 5 (module + knobs, option ii,
  with SHA pointer), card 16's sync, §CLI-surface edits, k8s. **Item 1 joined S1
  as a DELETE** per the sign-off record's flip.
- **Tests:** 1237 → 1223 passing, 0 failing (−43 deleted with the code they
  pinned, +29 new in `tests/test_regression_monitor_risk.py`).
- **Reviews:** two adversarial reviews, both **REQUEST_CHANGES**. Union of
  required fixes addressed in code at `6e78d11`; disposition table in the PR body.

**Notes — drift found against the plan (written 2026-08-01; FC-013/065/068/071/075/078 have merged since):**

1. **`config/covered_call.yaml` did not exist** when the knob census was taken
   (FC-075 Phase 1 merged 08-03). It mirrored six swept knobs plus the
   `monitoring:` block. Deleted symmetrically — their properties and validation
   are gone, so leaving them would mint exactly the corpses this sweep ends.
   **The census's "78 leaf keys" headline is therefore now understated**; it was
   never re-run against the second profile.
2. **`monitoring` was in `Config._validate_config`'s `required_sections`** — not
   on any card, and *blocking*: deleting the block without removing it makes
   every config fail to load. Removed; both profiles verified loading.
3. **No min-hold tests exist in `test_put_seller.py` / `test_call_seller.py`.**
   Card 6's blast radius expected the 2026-05-12 additions there; they are not on
   `main`. The gate's only coverage was `test_clock_seam.py`, whose
   `TestClockDrivesLiveDecisions` was re-anchored onto the DTE profit bands —
   the property it tests (freezing the clock changes live decisions) is
   unchanged, only its vehicle moved to the control that carries the discipline.
4. **`tools/diagnostics/fc036_gap_gate_study.py` also imports `GapDetector`** —
   card 5 named only `fc002_gap_filter_ab.py`. Given the same SHA-pointer header.
5. **`tools/monitoring/monitoring.py`** required `execution_gap_threshold` as a
   Config attribute in its own config check; would have failed against every
   valid config. Not on any card. Fixed.

**Notes — deviation acknowledged:**

- **OQ-8 (k8s) executed as swept-keys-only + an archived-example warning**, not
  as the recommended delete-the-whole-strategy-config-block. The manifest has
  drifted from `settings.yaml` in several places the sweep does not own
  (universe, deltas, stop-loss switches), and deleting the whole block would
  have bundled unrelated changes into a cleanup PR. A header now states the file
  is not the deploy path and is an archived example. **Mild deviation from the
  plan's recommendation, acknowledged rather than silently taken.**

**Notes — review findings and how they landed:**

- The reviews' shared top finding was that `strict_option_type`'s refusal to
  classify was *right but silent*: an adjusted contract vanished from checks 7
  and 8 entirely. Fixed with a `risk_unclassifiable_option` warn finding — the
  same principle the PR applied to check 8's basis resolution, applied to symbol
  parsing. This is a genuine defect the build introduced and the reviews caught.
- Check 4's `Config()` ignored `STRATEGY_CONFIG`, so the monitor would have
  validated the wheel's policy against a covered-call service. Fixed.
- Check 9's premium floors were the last hardcoded mirror in the file; re-sourced.

**Remaining S-partition:** S2 (`wheel_cycles` retirement), S3 (scanner rewire),
S5 (state shrink stage 1), S6 (behind FC-066), and the item 14 docs rewrite —
which must be **last**, and now also owes the two-profile census correction in
note 1 above.

### S2 — `wheel_cycles` retirement · **DONE (merged `8726ab1`, deployed rev 00477-qov, table dropped 2026-08-04)**

- **PR:** https://github.com/memon1987/options_wheel/pull/84
- **Branch:** `fc-069/s2-wheel-cycles-retirement`, commits `92ae858` (retirement)
  + `50851cb` (review disposition; tip). Squash merge: **`8726ab1`**.
- **Date:** 2026-08-04
- **Contents:** item 10's code half — both `reconcile_positions` call sites +
  feed computations + module import; `write_wheel_cycle` + `_SCHEMAS` entry
  (audit logs showed 71 cold-start auto-creates in 30 days — the entry deletion
  is what makes the drop stick); the repo's only raw-table `INSERT`
  (`backfill_analytics.py` §4, unlisted on the card, FC-035 precedent);
  +7 reconcile tests (first behavioral coverage of `reconcile_positions`).
  Tests 1223 → 1230.
- **Reviews:** BigQuery-DBA persona (APPROVE w/ conditions; independently
  reproduced the audit + reader gates against live BQ, verified all six
  hand-backfilled cycles are reproduced by the view) + reconcile-systems
  persona (REQUEST_CHANGES, doc-level only; 3 own mutations, all caught).
  No code defects; confirmation pass waived by both reviewers. **FC-079 filed**
  from the review: OCC-substring classifier on the position-diff reconcile path
  (`'P' in option_symbol` — call assignments undetectable for AAPL/PFE/SPY/PG)
  + the monitor's reconcile sibling.
- **OQ-7 resolved on the record:** 78 rows / 74 zero-gain — historical clusters
  36/30/6 (matching FC-065's numbers) plus a newer MSFT 2026-07-02 ×36 cluster;
  2 of the 74 are legitimately-$0 hand-backfilled UNH cycles (72 writer
  artifacts).
- **Step-0 gate closed with audit-log census** (stronger than the plan's
  `bq ls --transfer_config`, which the SA cannot run): 30-day BQ audit logs
  show zero `bigquerydatatransfer` activity project-wide (no scheduled queries
  exist), only app cold-start creates + 1 operator query touched the table;
  `__TABLES__` last-modified 2026-07-10 (25 days quiet). 0 views, 0 code
  readers.
- **Drop executed** (the plan's 7-day zero-new-rows intent satisfied by the
  25-day quiet period + a post-deploy check): live 14:15 ET `/run` on rev
  00477-qov — `reconciliation_completed`, zero errors, row count still 78 →
  `bq cp` to `wheel_cycles_pre_fc069_snapshot` (78 rows verified) → `bq rm`.
  Post-drop: raw table absent from `INFORMATION_SCHEMA.TABLES`;
  `wheel_cycles_from_activities` healthy (16 cycles); dashboard smoke 200s
  incl. `/api/history/wheel-cycles` and the per-symbol cycle table. Snapshot
  retained ≥ 30 days; restore = `bq cp` back + revert `8726ab1`.
- **No-op cycle:** 14:00 ET scan on the new revision identical at 3 put /
  10 call opportunities.

### S3 — scanner rewire (item 12) · **DONE (merged `d1e3fc2`, deployed rev 00481-fop, live-verified 2026-08-04)**

- **PR:** https://github.com/memon1987/options_wheel/pull/85
- **Branch:** `fc-069/s3-scanner-rewire`, commits `a34d575` (rewire + event)
  + `2b5fe62` (review fixes; tip). Squash merge: **`d1e3fc2`**.
- **Date:** 2026-08-04
- **Contents:** `_has_existing_position` option branch rewired to
  `parse_option_symbol` underlying-equality (substring test deleted);
  fail-closed on unparseable symbols (stricter than before) and on API error
  (unchanged); operator-confirmed event ride-along
  `put_scan_skipped_existing_position` joins the rejections vocabulary +
  `docs/gates.md` row 3 + BACKTEST_ENGINE.md gap #7 closed. Tests 1230 → 1246.
- **Changed-behavior memo:** the complete collision set was **{F⊂MSFT, F⊂PFE}**
  (the card's "F/PFE pair" enumeration was incomplete — MSFT found at build,
  completeness proven by reviewer brute-force over all 14×13 pairs). Live blast
  radius at deploy: **exactly zero** (no MSFT/PFE option held; F has zero fills
  ever and cannot clear the premium floor).
- **Reviews:** trader persona APPROVE (live-data mandate: verified the current
  book, fills history, collision completeness) + replay-engineer persona
  REQUEST_CHANGES with a HIGH the plan itself missed: the new tally bucket
  fires on **every deployed day**, so it would have become
  `backtest_runs.binding_constraint` on healthy runs (deployment misreported
  as stand-down, FC-057 class). Fixed: `NOT_A_STAND_DOWN` filtering in
  `_binding()` + the report renders deployment separately; **ENGINE_VERSION →
  `fc-069-scanner-rewire`** (third comparability boundary, documented in
  BACKTEST_ENGINE.md #5); held-short-CALL fixture + prefix-shape tests kill the
  surviving mutations. Confirmation pass CONFIRMED-CLEAN (differential probe:
  no-holds runs byte-identical to pre-fix). Accepted in writing: nondeterministic
  `reason` when both legs held; one malformed position halts the put scan
  fail-closed (visible via events); `unparseable_position` reason value
  deferred to FC-079.
- **Live verification (manual scan on rev 00481-fop, 15:15 ET):**
  `put_scan_skipped_existing_position` emitted for exactly the four held
  symbols (NVDA, AAPL, AMZN, GOOGL) — the skip's first-ever visibility; PFE
  still earnings-blocked; counts unchanged at 3 put / 10 call.

### S5 — state shrink stage 1 (item 8) · **DONE (merged `7073f9f`, deployed rev 00485-nij, no-op verified 2026-08-04)**

- **PR:** https://github.com/memon1987/options_wheel/pull/86
- **Branch:** `fc-069/s5-state-orphan-deletion`, tip `6ba4c5e` (single commit).
  Squash merge: **`7073f9f`**.
- **Date:** 2026-08-04
- **Contents:** CallSeller's orphaned state half deleted — `wheel_state_manager`
  ctor param, `self.wheel_state`, the `set_active_call_details` block,
  `handle_call_assignment` (+ `wheel_state_transition` plumbing),
  `_parse_option_symbol`, the then-unused `log_position_update` import;
  PutSeller's `_parse_option_symbol`. **Census correction on the record:** the
  put-side corpse was smaller than FC-068 §11 implied — PutSeller never had a
  state channel or assignment handler. `_parse_dte_from_option_symbol` (near-
  name, live on `/monitor`) kept on both sellers and pinned by a survival test.
  +5 census tests; 1246 → 1251.
- **Reviews:** monitor-path engineer + dead-code skeptic, **APPROVE × 2** — no
  code changes; both independently swept directories the build's grep missed
  (root scripts, `examples/`, `research/` — all clean) and mutation-tested the
  census tripwire. No confirmation pass needed. LOW findings dispositioned in
  writing on the PR (guarded-revert census escape → moot at S6; DTE-guard
  pins existence not correctness → accepted; call-side DTE parser still
  hand-rolled → noted for item 14/FC-079's family page).
- **No-op verification (rev 00485-nij, manual triggers 15:41 ET):** `/run` 200
  with clean reconcile + aged-out-blob early exit (exercises the S5-touched
  seller-construction path); `/scan` full pipeline identical at 3 put /
  10 call / 13 stored, 4/4 cost-basis cross-checks ok; zero errors.
