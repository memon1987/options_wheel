# Plan: Delete the `poll_order_statuses` path and the `order_statuses` table

**FC entry:** `docs/FUTURE_CONSIDERATIONS.md` FC-035
**Plan file:** `docs/plans/fc-035.md`
**Status:** Done (merged)
**Size:** M (multi-file deletion in live production paths + a BigQuery table drop)
**Author:** zeshan + Claude (Fable)
**Last updated:** 2026-07-28
**Baseline:** `main` @ `d493d46`, suite 607 green. All line numbers below are at this commit.

---

## Context

FC-035 was filed as "fix a one-line `NameError` so `poll_order_statuses`'s
closed-orders path works." That framing is dead. The path should be **deleted,
not revived**: it runs but produces nothing (0 events / 0 rows in ~490 production invocations), nothing reads its output, the table it
feeds was deprecated by FC-012 (which scheduled the exact `bq rm` this plan
performs — `docs/plans/fc-012.md:321-326` — via a cleanup routine that never
ran, `docs/releases/RELEASE_2026-04-24.md:139`), and the authoritative
fill/expiration record already flows through the activities ingestor.

An open **PR #47** (branch `fc-035-fix`) takes the opposite approach and
revives the path. Both of its adversarial reviewers independently raised
deletion as the defensible alternative; its author explicitly deferred that
decision to the owner ("I did not take that decision unilaterally"). **This
plan is that decision.** PR #47's disposition is specified below.

### Verification record (2026-07-28, at `d493d46`)

Every claim re-verified for this plan, with method:

| Claim | Verified how | Result |
|---|---|---|
| No dashboard code reads `order_statuses` | `grep -rn order_statuses dashboard/` | **0 hits** |
| No committed SQL reads it | grep over `docs/bigquery/*.sql` (all view files) | **0 hits** |
| No live view reads it | PR #47 dumped all 7 live `options_wheel` view bodies via `bq show --view` (2026-07-18) | **0 references** (builder re-checks, see Step 0) |
| `write_order_status` has no other callers | grep repo-wide | Only the poll (wheel_engine 796/827/858) |
| `log_order_status_update` has no other callers | grep repo-wide | Only the poll + `tools/scripts/backfill_order_status.py` |
| The three `order_*` event types have no consumers | grep `order_filled\|order_expired\|order_canceled` over `dashboard/`, `docs/bigquery/` | **0 hits** (see the `trades_executed` nuance under Behavior contract) |
| The poll is called from exactly one place | grep | `deploy/cloud_run_server.py:316` (inside `/run`); no dedicated route, no scheduler job, not in `main.py` |
| The `closed_orders` fetch is dead twice over | Read + `flake8 --select=F821` | Result never iterated (only `all_orders` loops), **and** `alpaca` unimported → F821 at `wheel_engine.py:744` — the **only** F821 in all of `src/` + `deploy/` |
| No tests reference any deleted symbol | grep over `tests/` | **0 hits** (PR #47's 8 tests exist only on its branch) |
| Table has 0 rows | **Could not re-verify this session** (gcloud auth expired, non-interactive) | Three prior attestations: `RELEASE_2026-04-08.md:56` (0 at creation), PR #47 body (`SELECT COUNT(*)` = 0, live, 2026-07-18), FC-043 in-code comment (0 rows, 2026-07-24). **Builder MUST re-verify — Step 0.** |
| Activities feed supersedes the poll's data | Read `src/data/activities_ingestor.py` | Ingests `FILL,OPASN,OPEXP,OPTRD,JNLC,FEE` with fill price/qty/time, idempotent on `activity_id` — a superset of what the poll would have written |
| FC-044 (daily execution grid) has no designs on this table | Read its FC entry | No mention |

## Decision: delete (design rationale + rejected alternatives)

**Chosen: delete the entire path** — poll, writer method, schema, log helper,
backfill script, and the empty table.

1. **Zero readers.** Dashboard, live views, committed SQL, tools, and pending
   FCs — nothing consumes `order_statuses` or the `order_*` events.
2. **Zero rows in ~3.5 months of live trading.** The poll has been a no-op
   since it shipped (three independent defects; any one sufficient).
3. **FC-012 already decided this.** It moved the dashboard to
   `trades_with_outcomes`, declared the table inert, and scheduled `bq rm` +
   a cleanup PR that never ran. Deletion completes a recorded decision;
   revival reverses it with no consumer to justify it.
4. **A superior source already exists.** `trades_from_activities` is the
   authoritative, idempotent fill/expiration record. A revived poll is a
   second, weaker, non-idempotent writer of the same facts.
5. **Revival has documented costs.** PR #47's own analysis: ~30 re-emissions
   per order, mitigated but *not* eliminated (BQ streaming-dedup window is
   minutes; polls are ~4h apart, so duplicates persist across polls), plus the
   revived `order_*` events leak into the shared live view
   `options_wheel_logs.trades_executed` (keyed on `event_category='trade'`) —
   new pollution of an ad-hoc analysis surface, purchasing a table nobody reads.

**Rejected — revive (PR #47).** Well-engineered, wrong direction, per 3/4/5.
Its engineering (order normalization, `_ts()`, `_classify_order_strategy()`,
`row_id` dedup) exists to make a redundant feed safe; the safest redundant
feed is none.

**Rejected — leave dormant** (FC-043's pin as a permanent state). Keeps ~180
lines of dead code containing the repo's only F821, one dead live API fetch
per `/run` cycle, a schema entry whose auto-create (`_ensure_all_tables`)
resurrects the FC-012-condemned table on every deploy, and standing doc
invitations (`docs/CLAUDE.md` example, eval catalog) to "finish" the path.
Dormant-but-present is exactly how this survived April → July.

**Rejected — fix the NameError only** (FC-035's original framing). Falsified
by PR #47's analysis: the import fix alone adds a wasted API call (the fetch
result is never iterated) and changes nothing else; making the path actually
work requires all of PR #47 — see "revive."

## What happens to PR #47

- **Close unmerged**, with a comment linking this plan. Owner/main session
  performs the close once this plan is approved. It must never merge after
  the deletion (the delete makes its diff conflict, and the guard tests below
  catch partial resurrections).
- **Salvage assessment of its two real bug fixes — both moot here, one
  generalizes:**
  - `_ts()` (`str(None)` → literal `'None'` rejected by a BQ TIMESTAMP
    column): moot — its only producer (`write_order_status` via the poll) is
    deleted. No other code path str()-wraps a nullable datetime into a BQ
    TIMESTAMP (`write_wheel_cycle` uses `or None` guards; trades go through
    TradeJournal / the activities ingestor).
  - `_classify_order_strategy()` (replaces `'P' in symbol`, which labels
    every AAPL/SPY/PFE **call** as a put): moot in the poll path — but the
    same defect is **live** at `deploy/cloud_run_server.py:688-689`, where
    `/monitor` computes `is_put = 'P' in symbol` *first*, so a covered call
    on any underlying containing `P` (AAPL, SPY, PFE) is evaluated by
    `should_close_put_early`. That is a live behavior bug **out of scope
    here** (changes `/monitor` decisions; needs its own review).
    **Required follow-up: file a new FC for it** (next free number), fix
    pattern = `parse_option_symbol()['option_type']`, the same one PR #47
    used. Lower-stakes copies in `tools/testing/*` diagnostics ride along.
  - `row_id` streaming-dedup support in `_write`: not salvaged — no remaining
    writer has an idempotency requirement.
  - Its 8 tests: discarded with the branch. The F821 gate below covers the
    class its NameError tests guarded; the rest assert behavior of deleted code.

## Goals

- Remove the never-executed order-status polling path end to end: engine
  method, server call site, analytics writer method + schema, log helper,
  backfill script.
- Drop the empty `order_statuses` BigQuery table, completing FC-012's
  recorded cleanup.
- Add a durable guard against the bug class (undefined-name-in-dead-code) and
  against silent resurrection of the table.
- Correct the docs that would otherwise instruct a future session to rebuild
  the path.

## Non-goals

- **No change to any live trading decision.** See Behavior contract.
- **No change to the FC-043 work**: the `get_orders` wrapper, Stage-6
  duplicate guard (Check 2), and `_extract_underlying_from_option_symbol`
  (still used at `wheel_engine.py:506, 1096, 1206`) all stay.
- **No fix for `cloud_run_server.py:688-689`** (`/monitor` put/call misroute)
  — new FC, see above. Do not bundle it into this PR.
- **No lint clean-then-gate pass beyond F821** on `src/` + `deploy/`. Other
  pre-existing lint findings stay; widening the gate is separate work.
- **No edits to historical records**: `docs/plans/fc-012.md`,
  `docs/plans/fc-043.md`, `docs/releases/*` document what was true when
  written. Leave them.
- **No change to the Cloud Logging sink, `options_wheel_logs` views, or the
  activities ingestor.**

## Behavior contract (what must NOT change)

- **Live trading inputs:** `poll_stats` is consumed by exactly one thing —
  the `pre_trade_order_poll_completed` log line (`cloud_run_server.py:317-324`).
  No trading logic reads it. Removing the poll changes no decision input.
- **`/run` still reconciles:** `engine = WheelEngine(config)` +
  `reconcile_positions()` + its log event remain inside the same fail-open
  housekeeping `try/except` (`pre_trade_housekeeping_failed`).
- **Dashboard numbers:** unchanged. Dashboard reads `trades_with_outcomes` /
  `trades_from_activities` / `wheel_cycles_from_activities`; zero references
  to `order_statuses` or `order_*` events.
- **Other analytics tables:** `errors`, `executions`, `wheel_cycles` schemas
  and writes untouched. `trades_from_activities` untouched.
- **`options_wheel_logs.trades_executed`** (live, uncommitted view selecting
  `event_category='trade'`): receives exactly the same events as today. The
  three `order_*` types have never been emitted (the poll never worked), and
  after deletion still aren't. Status quo preserved — this is the coupling
  PR #47's correction identified; deletion keeps it inert.
- **`/monitor` behavior:** unchanged, including its inline fill-price capture
  (`:772-783`) — only the stale comment is corrected.
- **Alpaca API volume:** strictly decreases (one `get_orders('open')` call +
  one dead CLOSED fetch removed per `/run`). No new calls anywhere.

## Exact changes by file

### 1. `src/strategy/wheel_engine.py`
- **Delete the entire `poll_order_statuses` method, lines 712–890** (from
  `def poll_order_statuses` through `return {'error': str(e)}`). The FC-043
  pin comment (728–736) and the local `QueryOrderStatus` import (742) die
  with it. `reconcile_positions` (starts line 892) must remain intact and
  adjacent.
- **Line 13:** remove `log_order_status_update` from the import list; keep
  the other five names.
- **Keep** `get_analytics_writer` (line 16 — still used at 1021, 1229) and
  `_extract_underlying_from_option_symbol` (line 614 — still used at 506,
  1096, 1206).

### 2. `deploy/cloud_run_server.py`
- **Lines ~310–324** (inside `/run` pre-trade housekeeping): delete
  `poll_stats = engine.poll_order_statuses()` and the
  `log_system_event(event_type="pre_trade_order_poll_completed", ...)` call.
  **Keep** the `try/except`, `engine = WheelEngine(config)`,
  `reconcile_positions()` and its log event. Rewrite the block comment
  (310–312) to describe reconciliation only.
- **Line 783:** replace the stale comment
  `pass  # Fill price captured later by poll_order_statuses` with accurate
  text, e.g. `pass  # Best-effort; authoritative fills come from the
  activities ingestor (trades_from_activities)`. The `pass`/swallow behavior
  itself is unchanged.

### 3. `src/data/analytics_writer.py`
- Module docstring: remove the `order_statuses` bullet (lines 13–14); add a
  line to the "Post-FC-012 removals" block: `order_statuses` removed in
  FC-035 (never populated; poll deleted).
- Delete the `"order_statuses"` entry from `_SCHEMAS` (lines 99–113).
- Delete `write_order_status` (lines 289–311).
- Class docstring (~line 120): "7 analytics tables" is stale — the writer
  now manages **3** (`errors`, `executions`, `wheel_cycles`); fix the count.
- Nothing else: `_write`, `_write_batch`, `_ensure_all_tables` untouched; the
  init log's `tables=list(_SCHEMAS.keys())` self-adjusts.

### 4. `src/utils/logging_events.py`
- Delete `log_order_status_update` (lines 368–418). Both callers are being
  deleted (poll + backfill script); no tests reference it.

### 5. `tools/scripts/backfill_order_status.py`
- **Delete the file.** It reads `options_wheel_logs.trades_executed` and
  re-emits `log_order_status_update` events to backfill the pre-FC-012
  dashboard display — a purpose that no longer exists. It never touches the
  `order_statuses` BQ table. Referenced nowhere (no cron, no runbook,
  not in `main.py`).

### 6. `scripts/backfill_analytics.py`
- Line 157: update the closing print note to
  `"Note: order_statuses removed in FC-035; position_snapshots and scans removed in FC-012."`

### 7. `docs/CLAUDE.md`
- "Example: Order Status Fix" (~lines 205–216): step 1 instructs *adding*
  `poll_order_statuses()`. This file loads into every session; as written it
  teaches resurrecting the deleted path. Rewrite the example to trace the
  same five layers through the **current** architecture (fills/status flow:
  activities ingestor → `trades_from_activities` → `trades_with_outcomes` →
  backend → frontend). Constraint: no reference to `poll_order_statuses` as
  a thing to add. Wording is builder's latitude.

### 8. `docs/analysis/PERFORMANCE_EVAL_CATALOG.md`
- Line 51 (EVAL-002) and line 283 (slippage eval): both are "BLOCKED" on
  `poll_order_statuses` data. Re-point both to the activities data
  (`trades_from_activities`: FILL rows carry fill price/qty/time; OPEXP rows
  carry expirations — a superset of what the poll would have provided). Fix
  the stale data-source pointer only; whether each eval flips to unblocked is
  the eval owner's call.

### 9. Bookkeeping (direct-to-main allowed per workflow, or in the PR)
- `docs/FUTURE_CONSIDERATIONS.md` FC-035 entry: re-scope from "fix latent
  NameError" to "delete the dead path," link this plan, status per lifecycle.
- `docs/plans/fc-042.md:75` (Track E2): add a one-line supersession note —
  "E2 superseded by `docs/plans/fc-035.md`: path deleted rather than fixed."

## BigQuery table drop

- **Step 0 — pre-build gate (MUST run before writing code):**
  ```bash
  bq --project_id=gen-lang-client-0607444019 query --nouse_legacy_sql \
    'SELECT COUNT(*) AS n FROM `gen-lang-client-0607444019.options_wheel.order_statuses`'
  ```
  Expected: `0`. **If > 0, STOP the PR and surface to the owner** — the
  "never worked" premise is falsified and this plan must be re-evaluated.
  (This session could not run it: gcloud auth expired, non-interactive. Three
  prior attestations listed in the Verification record.)
  Also re-confirm zero live readers, since committed SQL can't prove this:
  ```bash
  bq --project_id=gen-lang-client-0607444019 query --nouse_legacy_sql \
    'SELECT table_name FROM `gen-lang-client-0607444019.options_wheel`.INFORMATION_SCHEMA.VIEWS
     WHERE LOWER(view_definition) LIKE "%order_statuses%"'
  bq ls --transfer_config --transfer_location=us-central1 --project_id=gen-lang-client-0607444019
  ```
  Expected: no views match; no scheduled query references the table.
- **Ordering constraint — drop AFTER deploy, never before.**
  `_ensure_all_tables()` auto-creates every `_SCHEMAS` key on AnalyticsWriter
  init, so dropping while the old code is deployed recreates the table on the
  next instance cold start. Sequence: merge → Cloud Build deploy → confirm
  new revision serving → re-run the count (still 0) → drop:
  ```bash
  bq rm -f -t gen-lang-client-0607444019:options_wheel.order_statuses
  ```
- **Rollback recipe:** the schema is code-defined and the table is empty —
  reverting the commit and redeploying auto-recreates the identical empty
  table via `_ensure_all_tables()`. No data restore exists or is needed.
  Belt-and-braces: BQ time travel (~7 days) can resurrect the dropped table
  (`bq cp options_wheel.order_statuses@<ms_epoch> ...`) if anything
  unexpected surfaces.

## Test requirements

- **Tests to delete: none.** Verified zero references in `tests/` to any
  deleted symbol. (PR #47's 8 tests live only on its branch and are
  discarded with it.)
- **Add: F821 lint gate — new file `tests/test_lint_gates.py`.** Run
  `python -m flake8 --select=F821 src deploy` from the repo root via
  `subprocess`; assert exit 0 / no findings. flake8 is already a dependency
  (`requirements.txt:53`). **Why this is the right guard:** verified this
  session that the only F821 in `src/` + `deploy/` is `wheel_engine.py:744`
  — the line being deleted — so the gate goes green with this PR and
  permanently blocks the FC-035 bug *class* (code that never executed
  because it references an undefined name; same family as FC-039/FC-015's
  "healthy-looking dead code"). This resolves FC-035's open question #3 for
  F821 without a repo-wide lint cleanup. There is no CI (`.github/` absent),
  so the pytest suite is the only enforcement vehicle that actually runs.
  Regression caught: any resurrection of the NameError pattern, including a
  bad merge of PR #47-era code. Implementation notes: use test-level
  `pytest.importorskip("flake8")` (module-level importorskip silently skips
  whole files — known 2026-07-18 gotcha); no network/creds needed, so
  conftest's ambient-environment blocks are unaffected.
- **Add: managed-table contract — new file `tests/test_analytics_writer.py`**
  (none exists today). Assert `"order_statuses" not in _SCHEMAS`
  unconditionally, and when `_HAS_BIGQUERY` is true, assert
  `set(_SCHEMAS) == {"errors", "executions", "wheel_cycles"}` (when false,
  `_SCHEMAS` is `{}` — handle both). **Why:** `_ensure_all_tables()`
  auto-creates every `_SCHEMAS` key on service start, so a re-added schema
  entry silently recreates the dropped table on the next deploy. This makes
  the writer's managed set an explicit contract; adding a genuinely new
  table means consciously editing the test. Regression caught: silent
  table resurrection via schema re-add.
- **Suite:** 607 green at baseline → expected 609 after (builder confirms
  exact count). No existing test should change.

## Reuse

- `flake8` (already in `requirements.txt`) for the F821 gate — do not add a
  new lint dependency.
- `parse_option_symbol` (`src/utils/option_symbols.py`) — cite it in the
  follow-up FC for `cloud_run_server.py:688`; do not re-implement OCC parsing
  anywhere.
- Existing conftest environment-isolation patterns — new tests must need no
  creds/network.

## Risks & mitigations

- **Rows exist in the table after all** (something wrote between 2026-07-24
  and build). Mitigation: Step 0 gate with a hard stop rule.
- **An uncommitted live view or scheduled query reads the table.** Committed
  SQL provably doesn't; PR #47 checked all 7 live views on 2026-07-18.
  Mitigation: Step 0 re-runs the live-view and transfer-config checks at
  build time; at least one reviewer independently verifies against BQ.
- **PR #47 merges later by accident.** Mitigation: close it on plan approval;
  the deletion makes its diff conflict; F821 + schema tests catch partial
  resurrections.
- **Table dropped before deploy → auto-recreated → confusion.** Mitigation:
  explicit ordering in Rollout; the drop is the final step.
- **`docs/CLAUDE.md` example rewrite drifts from reality.** Mitigation:
  confirmation pass verifies the rewritten example names only components that
  exist on `main`.

## Rollout

1. Branch + PR (production paths → PR mandatory). Commit message references
   `Plan: docs/plans/fc-035.md`.
2. Step 0 BQ verifications (before code).
3. Code changes + tests; full suite green.
4. Two adversarial reviews (below) → disposition → confirmation pass if fixes
   land in code → merge.
5. Cloud Build auto-deploy on merge. No config/env/scheduler changes; change
   is purely subtractive.
6. Post-deploy checks (next 2 trading days, 12 `/run` cycles): `/run` logs
   show `pre_trade_reconciliation_completed` and **no** `order_poll_started`;
   no new `errors` rows referencing order polling; dashboard endpoints
   unchanged.
7. Then the table drop (commands above).
8. Close PR #47 with a comment linking this plan (owner action; may happen at
   plan approval or at merge — but before any conceivable merge of #47).
9. File the follow-up FC for the `/monitor` `'P' in symbol` misroute.

## Rollback

- Single `git revert` of the implementation commit restores the poll exactly
  as it was: the FC-043-pinned no-op. Rollback risk is ~zero because the
  restored code *does nothing* by construction.
- If the table was already dropped, redeploying reverted code auto-recreates
  it empty via `_ensure_all_tables()`; BQ time travel is the backstop. No
  data can be lost (0 rows, re-verified at Step 0 and again pre-drop).

## Review

Per `~/CLAUDE.md`: **two adversarial reviewers, both Fable, fresh contexts,
different personas, run concurrently** — suggested lenses: (a) senior options
trader / Python developer who's seen production trading bots blow up; (b)
production data/reliability engineer who owns a BigQuery estate and has
written the post-mortem for a dropped-table incident. This PR touches the
live `/run` path and drops a table in the canonical dataset, so per the
stakes-calibration rule **at least one reviewer gets BQ access and must
verify the zero-row / zero-reader claims against live data**, not the diff.
Both must APPROVE; any REQUEST_CHANGES blocks per the standard disposition
rules; scoped confirmation pass (Fable, one reviewer, fresh context) if fixes
land in code. Hand reviewers this plan + the diff; do not pre-state
validation results beyond what this plan already records.

## Open questions

- **BLOCKING: none.** (Step 0 is a gating precondition with a defined stop
  rule, not an open question.)
- NON-BLOCKING: exact wording of the rewritten `docs/CLAUDE.md` example
  (constraint specified in change #7).
- NON-BLOCKING: whether EVAL-002 and the slippage eval flip from BLOCKED to
  unblocked once re-pointed at activities data — eval owner's call; this PR
  only corrects the stale pointer.
- NON-BLOCKING: FC number for the `/monitor` `'P' in symbol` misroute —
  assign the next free number at filing time.

## Execution

_Filled in after implementation is complete._

- **PR:**
- **Commit:**
- **Date:**
- **Notes:**

## Correction (post-review, 2026-07-29)

Both reviewers independently verified against production logs that the
"**never executed**" phrasing in this plan, the PR body and the in-code
comments was **wrong**. `order_poll_started` fired **490 times**
(2026-04-06 -> 2026-07-28) and `order_poll_completed` 486 times, with
`max(orders_checked)=3` and `filled_logged`/`expired_logged`/`canceled_logged`
never above 0.

The accurate claim — which the engineering conclusion rests on unchanged — is
that the method **ran every cycle and its effectful branches never fired**: it
only ever iterated open orders, and open orders are never in a final state, so
even a fixed import would have written nothing. Corrected everywhere, because
`docs/CLAUDE.md` loads into every session and a future auditor who finds those
490 events would rightly distrust this record.

**Post-deploy verification gotcha:** `log_system_event` passes `event_type` as
structlog's positional *message*, so `pre_trade_reconciliation_completed` lands
in `jsonPayload.event`, **not** `jsonPayload.event_type`. Query the former or
the check returns nothing and looks like a regression. (Filed as its own FC.)

## Execution

- **Status:** DONE — code merged and the table dropped 2026-07-30 by the owner
- **PR:** https://github.com/memon1987/options_wheel/pull/54
- **Merge commit:** `ceaae16`
- **Date:** 2026-07-29
- **Review:** two adversarial Fable reviewers (data-integrity + production
  reliability), worktree-isolated, both **APPROVE**. Both independently
  re-ran the Step 0 gate against live BigQuery and both mutation-tested the
  two guard tests (each confirmed to fail when the defect is reintroduced).
  Only finding was the LOW "never executed" phrasing, corrected in `82f4a41`.
- **Follow-ups filed:** FC-046 (`trades_executed` view unqueryable),
  FC-047 (`log_system_event` never sets `event_type`).

### REMAINING OWNER STEP — drop the table, in this order

`_ensure_all_tables()` recreates any `_SCHEMAS` key on AnalyticsWriter init,
so the drop MUST come after the new revision is serving:

1. Deploy main (Cloud Build) and confirm the new revision is serving traffic.
2. Re-verify the count is still 0:
   `bq query --nouse_legacy_sql 'SELECT COUNT(*) FROM `options_wheel.order_statuses`'`
3. Drop: `bq rm -f -t gen-lang-client-0607444019:options_wheel.order_statuses`
4. Spot-check within a week that it has not reappeared (a stale local checkout
   with BQ creds could recreate it — reviewer finding L3).

Rollback: revert the commit and redeploy; `_ensure_all_tables()` recreates the
identical empty table. BQ time travel (~7 days) is the backstop.

**Post-deploy verification gotcha:** query `jsonPayload.event`, NOT
`jsonPayload.event_type`, for `pre_trade_reconciliation_completed` — see FC-047.

### Drop completed 2026-07-30

`bq rm -f -t gen-lang-client-0607444019:options_wheel.order_statuses` — run by the owner.

Post-drop verification (all four checked):
- `bq show options_wheel.order_statuses` → **Not found**
- absent from `bq ls options_wheel`
- serving revision `53d927c` **contains** FC-035 (`ceaae16` is an ancestor)
- that image's `_SCHEMAS` has **0** `order_statuses` entries, so `_ensure_all_tables()`
  cannot recreate it on the next cold start — which was the ordering risk this plan called out

The `test_analytics_writer.py` managed-table contract guards against a future re-add.
