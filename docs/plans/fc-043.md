# Plan: Fix `AlpacaClient.get_orders` — the status filter has never worked

**FC entry:** `docs/FUTURE_CONSIDERATIONS.md` FC-043
**Plan file:** `docs/plans/fc-043.md`
**Status:** Published
**Size:** S (one wrapper function; live-behavior change)
**Author:** zeshan + Claude
**Last updated:** 2026-07-24

---

## Context

`AlpacaClient.get_orders(status=...)` (`src/api/alpaca_client.py:632`) has two
independent defects, and between them the `status` argument has **never
selected the orders the caller asked for** since the wrapper was written:

1. **It never passes a request filter.** It calls
   `self.trading_client.get_orders()` with no `GetOrdersRequest`, so Alpaca's
   REST default (`status=open`) always applies — closed orders are never even
   fetched, regardless of the `status` argument.
2. **It value-filters against the wrong vocabulary.** It then keeps orders where
   `order.status.value.lower() == status.lower()`. But callers pass Alpaca's
   *query* tokens (`'open'`), which are **not** order-status *values*. An open
   order's `status.value` is `new` / `accepted` / `pending_new`, never `'open'`
   (verified: `'open' in [s.value for s in OrderStatus]` is `False`). So the
   comparison can never match.

### Who this breaks (all three live callers)

| Caller | Arg | Intent | Actual result today |
|---|---|---|---|
| `wheel_engine.py:496` Stage-6 duplicate guard, Check 2 | `'open'` | block new entry if an unfilled order for this underlying exists | `[]` **always** — the guard's API check is dead |
| `wheel_engine.py:497` Stage-6 guard | `'pending_new'` | same | effectively never matches (transient bucket, and only the open set is fetched) |
| `portfolio_tracker.py:315` | `'filled'` | last 10 filled orders for reporting | `[]` **always** — `filled` lives in the CLOSED bucket, which is never fetched |

**Why the duplicate guard matters most.** Check 2 is the **only** duplicate
backstop that survives a Cloud Run cold start. Check 1 (`_pending_underlyings`)
is in-process and resets to empty on every new instance; Check 3 catches only
*filled* positions, not orders still resting at the broker. Check 2 was written
as the "race condition fix" (its own comment) to close exactly the FC-009
window: order placed → instance scales to zero → new instance re-evaluates with
an empty local set → **places a second order for the same underlying**. Because
Check 2 returns `[]` unconditionally, that window is currently unguarded. FC-009
is a *confirmed* duplicate-order bug; this is a direct contributor.

### Verified against live Alpaca (2026-07-24, paper)

- `OrderStatus` values do **not** include `open` or `closed` — those are
  `QueryOrderStatus` (query) tokens only.
- `GetOrdersRequest(status=CLOSED)` returns `{filled, expired}`;
  `GetOrdersRequest(status=OPEN)` returns the unfilled set. The wrapper fetches
  neither correctly.
- Current wrapper: `get_orders('open')`, `get_orders('pending_new')`,
  `get_orders('filled')`, `get_orders(None)` **all return 0** against an account
  with 500 closed orders.

## Goal

`get_orders(status=...)` returns the orders the caller asked for, fixing all
three callers at the root, with no change to the dict shape it already returns.

## Non-goals

- **Not activating the FC-035 poll path.** *(Corrected after review — the
  original plan claimed the poll "calls `trading_client` directly" and was
  untouched. That was false: `wheel_engine.py:716` uses the `get_orders`
  **wrapper** this fix changes; only the dead `closed_orders` fetch at line 722
  uses `trading_client`.)* Fixing the wrapper's `None → ALL` default would flip
  that dormant loop into ~500 non-idempotent BigQuery writes per cycle. To keep
  FC-043 scoped to the guard, the poll caller is **pinned to `get_orders('open')`**,
  preserving its exact current no-op. Properly activating the poll (fetch recent
  CLOSED, dedup, resolve the double-logging question) is PR #47 / FC-035.
- No change to `_pending_underlyings` (Check 1) or the cold-start persistence
  question — FC-009's in-memory-dedup root cause is broader and stays in FC-009.

*(Originally a non-goal, promoted into scope by review: Check 2's fail-open
`except`. Because this fix makes Check 2 load-bearing, leaving it fail-open was
judged too risky — see "The fix" below. Now fail-closed.)*

## The fix

Rewrite `get_orders` to map the `status` string to the correct
`QueryOrderStatus` bucket, fetch with an explicit `GetOrdersRequest`, then
value-filter only when the caller named a specific status value:

- `None` → `QueryOrderStatus.ALL`, no value filter
- `'open'` → `QueryOrderStatus.OPEN`, no value filter
- `'closed'` → `QueryOrderStatus.CLOSED`, no value filter
- a specific status value (`'filled'`, `'expired'`, `'pending_new'`, `'canceled'`,
  …) → fetch the bucket it lives in (OPEN for the unfilled/pending values,
  CLOSED otherwise), then keep rows whose `status.value` matches.

Pass an explicit `limit` (500), which raises Alpaca's default page of 50 to a
safe bound for the current callers. It is a **hard cap, not pagination** — the
OPEN bucket (guard, pending check) is always tiny, and the only CLOSED caller
(portfolio "last 10 filled") wants the newest 10, at the head of Alpaca's DESC
order. A future full-closed-history caller must paginate with `after=`/`until=`.
Preserve the returned dict keys exactly.

### Caller-side fixes pulled in by review (all in the same PR)

The wrapper fix reactivates two previously-dead callers; both needed fixing so
the fix doesn't trade "empty" for "wrong":

- **Poll path** (`wheel_engine.py:716`): pinned to `get_orders('open')` — see
  Non-goals. Preserves its no-op; poll activation is PR #47.
- **Check 2 substring match** (`wheel_engine.py`): `underlying in order['symbol']`
  false-matches (`'F'` in `'PFE…'`; `'C'`/`'P'` match the option-type char in
  every OCC symbol). Now that Check 2 is live it would over-block. Replaced with
  an exact OCC-root compare via `_extract_underlying_from_option_symbol`.
- **Check 2 fail-open → fail-closed**: the `except` fell through to Check 3
  (filled positions only), silently skipping the now-load-bearing guard on any
  API error. `get_orders` is `@api_retry`-wrapped, so reaching the `except`
  means retries were exhausted (sustained failure). We now **block** the entry
  and log `stage_6_order_check_failed_blocking`. A paused entry beats a
  duplicate order (FC-009 = real money); a sustained outage pausing entries is
  observable (no new trades) and self-heals next cycle.
- **Redundant fetch removed**: `get_orders('pending_new')` ⊆ `get_orders('open')`,
  so the second Stage-6 fetch is dropped.
- **`portfolio_tracker.py:315`**: `[-10:]` → `[:10]` — Alpaca returns DESC, so
  the newest 10 are the head; `[-10:]` returned the oldest 10.

### Behavior change and its risk

`get_orders('open')` flips from "always `[]`" to "the real open set", so the
Stage-6 guard begins blocking a new entry when an unfilled order for the same
underlying is resting. Intended. Only over-blocks if an order lingers open — but
the bot places **DAY** marketable orders on ≤7-DTE options, which resolve
same-session, so a stale open order is not a realistic standing state. Reviewers
should probe this.

## Tests

- Unit: mock `trading_client.get_orders`; assert the wrapper passes
  `GetOrdersRequest(status=OPEN)` for `'open'`, `CLOSED` for `'filled'`, `ALL`
  for `None`, and that value-filtering keeps/drops correctly. A regression test
  that fails on the current code (asserts `'open'` returns a resting order).
- Integration regression: `test_stage6_check2_real_wrapper_blocks` drives
  `WheelEngine._has_existing_option_position` through the **real** `get_orders`
  wrapper (only `trading_client` mocked at the SDK boundary) and asserts Check 2
  blocks. Verified to **fail on the pre-fix wrapper** (old value-filter drops the
  `accepted` order) and pass after — so it genuinely guards the regression, not
  just the guard's loop. Plus unit tests for the substring non-over-block (`'F'`
  vs a `PFE` order) and fail-closed-on-error.
- Full suite green (596 → 607).

## Review

Live trading logic → **two adversarial reviewers, different personas** (per
`~/CLAUDE.md` stakes calibration), both APPROVE, then the scoped confirmation
pass. Give them the diff and the FC-009 link; do not pre-state verification.

## Deferred (with justification)

- **Backtest adapter parity** (`src/backtesting/engine/alpaca_adapter.py:441`).
  The adapter keeps the old value-equality `get_orders`, so its Check 2 is still
  dead. This is harmless in backtest: replay is single-process, so Check 1
  (`_pending_underlyings`) never resets and always covers duplicates — there is
  no cold start for Check 2 to backstop. Widening this is not needed for
  correctness; noted for a future fidelity pass, not fixed here.

## Rollout / rollback

Wrapper change + three caller-side fixes + tests. Rollback = revert the commit.
No schema, no config, no deploy change. `order_statuses` write volume is
explicitly held at status quo (poll pinned to `open`), so nothing changes in
BigQuery under this PR.

## Execution

- **Status:** Done (merged)
- **PR:** https://github.com/memon1987/options_wheel/pull/51
- **Merge commit:** `7e71f69`
- **Commits:** `e37d11c` (initial fix), `57657b8` (two-reviewer fixes)
- **Review:** two adversarial reviewers (senior options trader + production
  reliability engineer), both **REQUEST_CHANGES**, converging independently on
  the poll-path HIGH. All required fixes landed in code (poll pinning, OCC-root
  match, fail-closed, portfolio `[:10]`, redundant-fetch removal, truncation
  doc, `calculated`→CLOSED); backtest-adapter parity deferred with written
  justification above. Scoped confirmation pass: **CONFIRMED-CLEAN** (all 7
  fixes verified landed; the integration test re-mutation-tested to fail on the
  old wrapper; no regressions; suite 607).
- **Date:** 2026-07-24
- **Notes / deviation:** the original plan's Non-goals wrongly claimed the poll
  path calls `trading_client` directly and was untouched. Review corrected this;
  the fix's scope grew from a one-function wrapper change to include four
  caller-side fixes (poll pin, OCC-root match, fail-closed, portfolio slice) so
  reactivating the wrapper's dormant callers didn't trade "empty" for "wrong."
  Suite 596 → 607.
- **Follow-up surfaced (not in scope):** the poll's `closed_orders` fetch
  (`wheel_engine.py`, `trading_client` CLOSED/limit=100) runs every cycle and its
  result is never iterated — dead code that predates this PR and writes nothing.
  Belongs to PR #47 / FC-035's poll-activation work.
