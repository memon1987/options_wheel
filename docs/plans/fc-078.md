# Plan: Minimal roller revival — credit-only defensive rolls, daily evaluation

**FC entry:** `docs/FUTURE_CONSIDERATIONS.md` FC-078
**Plan file:** `docs/plans/fc-078.md`
**Status:** **Built; both code reviews dispositioned — awaiting confirmation pass.** Rev 2 reviewed: two adversarial plan reviews (trader w/ live marks + order-execution lifecycle) REQUEST_CHANGES; all findings dispositioned in rev 2 (`834c2ea`, 2026-08-04). Build complete on `fc-078/roller-revival` (`af9c213`, 2026-08-04) — suite 1125 → 1223, 31/31 mutations killed. **No PR opened; the two-reviewer gate has NOT been run.** See §Execution. (Expedited — no log-only gate per operator; target live before 8/07.)
**Size:** M (money path: places orders; two-reviewer gate mandatory)
**Author:** Claude (Fable), operator decisions by zeshan 2026-08-04
**Last updated:** 2026-08-04 (**rev 2** — dispositions the two adversarial plan reviews, both REQUEST_CHANGES: expiry-relative roll horizon; order-lifecycle rigor — cancel-then-verify on both legs, partial-fill truth, monitor-order collision, infra timing budget; honest stranded-BTC residual. No reviewer-vs-reviewer disagreements; all operator decisions upheld.)
**EXPEDITED:** target live before the 8/07 expiries. Current holdings carry a mark-dependent GOOGL-class credit roll (§DD-8).

---

## Context

The call roller (`src/strategy/call_roller.py`, FC-006) has **never executed a roll in production** (FC-066: five Fridays of `rolls_evaluated>0, rolls_executed=0`; zero option orders in any Friday roll window across the full order history). Four stacked causes: a fatal quote-key mismatch that kills every evaluation silently, a Friday-only/DTE≤1 eligibility trap, a dead wheel-state premium that makes `debit_pct_of_premium = 999` (credit-only by accident), and zero observability on the fatal path.

The earnings-pop investigation (`docs/investigations/earnings-pop-callaway-2026-08-03.md`) established the design boundary empirically:

- **Deep-ITM rescue is not a roller job.** Every strike-improving AMZN roll is a $1.2–1.7k debit — a leveraged directional bet that the pop holds. The FC-013 earnings gate is the fix for that class; no roller should ever pay it.
- **Shallow-ITM credit rolls are free money the as-built roller makes illegal.** GOOGL C370 8/07 had a zero-cost roll to C380 8/21 (+$10 strike, up to +$1,001 upside preserved) — blocked by the entry delta band [0.15, 0.25], the DTE ≤ 7 cap, and the Friday cadence, *not* by the debit math.

**Scope (binding, from the FC-078 entry):** fix the quote keys; daily evaluation; credit-only execution (structurally cannot pay debits); defensive-roll exemption from the entry delta band; replacement horizon "~14 days" (rev 2 pins the frame: **old expiry + 14**, the frame the FC entry's investigation numbers used — DD-3); assignment-imminence override; FC-066's pre-revival checklist (alert-wire both roll-path cost-basis skip events; structured events for the silent pre-guard skips; keep the replay-BQ gate forwarding). FC-062's remaining half (execute-time floor at the roller's order site) is absorbed here. FC-066 closes into this on completion. FC-069 item 13 (rolling.* knob fates) resolves here.

**Out of scope (binding):** debit rolls of any kind; roll-up chasing; changes to the earnings gate itself; wheel-state persistence repair (FC-039 — this plan *deletes* the roller's dependency instead); the dashboard.

**Operator decisions (2026-08-04, binding — do not relitigate in review):**
1. Daily evaluation + credit-only execution + assignment-imminence override. Expiry-day-only was rejected: it forfeits transient mark-dependent windows and walks into early assignment on deep-ITM positions.
2. **No log-only launch gate.** Paper account; operator accepts first-execution risk. FC-078 ships straight to live execution on merge. `ROLLER_DRY_RUN` exists as an operational env flag (nearly free, useful for debugging) but is **not** a launch precondition.

---

## Goals

- The roller executes credit-only defensive rolls, evaluated daily, on the live book — capable of capturing the GOOGL-class roll and structurally incapable of the AMZN-class debit.
- Every evaluation emits exactly one terminal structured event per short call per cycle (the silent-skip fix).
- The operator is notified when a roll executes, without reading logs.
- One `git revert` + one scheduler flip restores today's world.

## Non-goals

- No multi-leg (combo) order support in `AlpacaClient` (DD-1).
- No decision_events rows for rolls in this FC (DD-5; follow-up FC filed on merge).
- No wheel-state repair; the roller's state dependency is deleted, not fixed (DD-6).
- No change to `/monitor` profit-taking, the FC-013 scanner gate, or entry-side selling.

---

## Design decisions

### DD-1 — "Credit-only", precisely

**Definition.** A roll is a BTC (buy-to-close the existing short call) + STO (sell-to-open the replacement). The **credit invariant** is enforced on the *limit prices actually placed*, not on marks:

```
STO_limit − BTC_limit ≥ rolling.min_net_credit_per_contract / 100   (per share; default knob 0.00 $/contract)
```

Because both legs are limit orders, actual fills can only improve on this (BTC fills at ≤ limit, STO at ≥ limit), so **a filled roll can never net a debit**. This invariant is checked at four sites: candidate screen, **immediately before BTC placement** (re-fetch the STO quote and recompute — evaluation quotes are minutes old by order time; this shrinks the staleness window to the BTC poll itself), order construction, and every fallback/retry repricing (see the fallback-path fix below). If the pre-BTC re-check fails, the roll is abandoned *before any order exists* with terminal skip `credit_gone_at_execution` — the cheap place to lose the race.

**Pricing modes.**
- **Base mode:** BTC limit = ask, STO limit = bid (conservative, near-certain fills; the credit is real at the worst case). The as-built `btc_limit_over_ask_pct` pad (limit = ask × 1.05) is deleted — a pad *above* ask makes the placed-limit worst case a debit, breaking the invariant. `stc_limit_under_bid_pct` is deleted for the mirror reason (an under-bid discount gives away credit the invariant needs).
- **Imminence mode** (DD-2 override active): BTC limit = mid + $0.05, STO limit = mid − $0.05, executed only if the invariant still holds at those limits. Rationale: bid/ask pricing double-counts the spread; when assignment is imminent, crossing half the spread to escape is worth the fill risk, and the invariant still guarantees no debit at the placed prices.

**Min-credit default = $0.00/contract.** Any non-negative roll at conservative prices executes the day it appears — this is what "transient zero-cost windows get caught the day they appear" means (FC-078 timing rationale). Marginal rolls defer *automatically* because their net is negative at conservative prices; the net improves toward expiry when the stock is still (the old call decays toward intrinsic faster than the 14-DTE replacement). The knob exists so the operator can later add a churn guard without a code change.
*Rejected:* a positive default (e.g. $5) — it would have deferred the GOOGL +$1 roll, the exact trade this FC is expedited to capture.

**Two sequential single-leg orders, BTC first.**
- *Rejected: single combo (mleg) order.* Alpaca supports multi-leg option orders, but (a) `AlpacaClient.place_option_order` is single-leg only — adding an order-class path to the shared client is new API surface on an expedited money path; (b) multi-leg requires a higher options approval level than the covered-call/CSP level this account is known to use — an unverified entitlement is a deploy-day discovery risk; (c) the two-leg exposure window (shares briefly uncovered) carries no naked-call risk. Revisit as a follow-up if leg risk ever bites.
- *BTC must be first, always.* STO-first would momentarily hold two short calls against 100 shares — a naked call Alpaca would either reject or margin. There is no acceptable STO-first sequence. (Mutation test T-9.)
- **Stranded-BTC recovery story** (BTC filled, all STO attempts failed): the shares are uncovered but *long-only* — no naked exposure. **The residual is NOT "a day's premium leak" — worst case is the full BTC debit paid in cash with nothing sold against it** (GOOGL-class: ≈ $839 BTC cash, of which ~$580 was extrinsic burned to close), followed by a re-cover that collects far less than what was closed, adversely selected: STO ladders fail on down-ticks, so the re-cover prices into a weaker tape. Recovery, stated as the composite it actually is: (1) the existing `call_roll_naked_exposure` error event fires (name kept for log continuity; message clarified to "shares uncovered — no naked position"), now **alert-wired** (DD-5); (2) no same-cycle re-entry heroics — the next `/scan` → `/run` cycle re-covers through the **entry path with all entry gates applied**: entry delta band [0.15, 0.25], `min_call_premium`, the FC-013 span gate, and the FC-029/FC-065 floor. Near a report this means the span gate can **legitimately leave the shares uncovered through the earnings event for days** — correct by doctrine (FC-013 exists to refuse exactly that new exposure), but it must be understood as a possible terminal outcome of a failed roll, not smoothed over as "self-healing"; (3) the next day's roll cycle sees no short call and skips. The alert exists so any repeated pattern gets investigated rather than absorbed.

### DD-2 — Eligibility: which short calls get evaluated

A short call is **evaluated** every cycle it exists; it is **roll-eligible** when:

1. **ITM ratio:** `stock_price / strike ≥ rolling.itm_trigger_ratio` (kept at **0.98**). OTM calls are the profit-taker's territory (`/monitor` DTE bands), not the roller's. 0.98 rather than 1.00 catches the knocking-on-the-door case where the credit window is widest.
2. **No current-DTE gate.** `rolling.max_current_dte` is **deleted**. The DTE ≤ 1 gate existed to bound *debit* escalation near expiry; under credit-only it has no protective job and is precisely what blinded the roller (a churned book's calls are 5–7 DTE on any given Friday — FC-066 cause 2). Self-timing is the credit sign, not the calendar.
   *Rejected:* raising it to 5 — excludes nothing real on a 7-DTE book but re-creates a calendar trap for any future longer-dated call.
3. **Max-rolls gate deleted.** `rolling.max_rolls_per_position` counted wheel-state rolls that were never persisted (always 0 — the gate has never bound). Its purpose was to stop debit-chasing escalation; credit-only removes the hazard, and the process terminates structurally: `validate_roll` requires strictly increasing strikes, so re-rolls are monotone and each is ≥ $0. At most one roll per position per day (one evaluation per cycle).
   *Rejected:* deriving roll counts statelessly from order history — real work, guarding against a hazard the credit invariant already removes.

**Assignment-imminence override.** When `extrinsic_per_share ≤ rolling.imminence_extrinsic_threshold` (new knob, default **$0.20**), the roller switches to imminence pricing mode (DD-1) for that position. `extrinsic = max(0, option_mid − max(0, spot − strike))`, computed from the same quotes the evaluation already fetches.
- *Threshold rationale:* early exercise of an American call is rational when remaining time value falls below the carry benefit — in practice, near-zero TV or TV below a pending dividend. $0.20 covers both the TV≈0 case and the typical quarterly dividend magnitude of this book's payers (AAPL/GOOGL/NVDA ~$0.20–0.26; AMZN pays none). At AMZN's 8/03 marks (TV ≈ $0.27 mid) the override would *not* yet fire — correct, since no credit roll exists there anyway.
- *What it overrides:* pricing aggression only. It does not relax the credit invariant (nothing does), the floor, or the span gate.
- *Best-effort, stated plainly:* the override improves the odds of escaping assignment; it does not guarantee escape. Its failure mode — mid-priced legs don't fill, position assigns — is the **pre-existing default outcome**, and assignment happens at/above the floor by construction, so the override can only add value, never subtract it. No heroic escalation beyond mid ± pad.
- *Quote quality gate:* imminence mode (and the extrinsic computation feeding it) requires a **two-sided option quote**; the stock quote must be two-sided everywhere (see the quote-guard spec in §1 — the "whichever side is positive" fallback is deleted).
- *Rejected:* having the override relax a positive min-credit — min-credit defaults to 0, there is nothing to relax; and relaxing *into debits* is out of scope by operator decision.

**Strike constraints (unchanged in direction, unified in mechanism):**
- New strike > old strike (`validate_roll`, kept).
- New strike ≥ cost-basis floor: `min_strike = max(cost_basis_per_share, current_strike + 0.01)` where `cost_basis_per_share` comes from the shared `CostBasisResolver` (FC-065 Phase 2, already in the roller), **fail-closed** — unresolved/divergent basis skips the roll with the existing `call_roll_skipped_cost_basis_unresolved`/`_divergent` events (now alert-wired, DD-5). At-floor strikes are allowed (≥, not >): floor = Alpaca `avg_entry_price`, so an at-floor call-away books ≥ $0 equity (FC-065 doctrine).
- FC-062 execute-time half: the floor must also bind at the **order site**, including the fallback-strike path — see "Fallback-path repair" below.

### DD-3 — Replacement constraints

- **Roll-out horizon — expiry-relative, not evaluation-relative (rev 2, trader BLOCKER B-1):** `replacement_expiry ≤ old_expiry + rolling.max_extension_days` (new knob, default **14** calendar days). The frame matters: roll economics extend the *existing contract's* tenor, so the horizon must be measured from the **old expiry**, not from the evaluation date. Rev 1's eval-relative `max_replacement_dte=14` would have excluded the flagship trade — GOOGL C370 8/07 rolled to 8/21 is 17–18 DTE from the 8/04 evaluation but exactly old-expiry + 14, the frame the investigation's "~14 DTE" numbers actually used. Live-verified consequence of the rev 1 drafting: first cycle would have logged GOOGL `no_credit_candidate` while C375 8/21 sat at ≈ +$248/contract credit. Two weeklies beyond the current expiry is long enough for where credits live, short enough that capital isn't parked under a month-long cap. No minimum: a same-week roll-up that nets a credit is legal (rare but harmless). Because the bound is per-position, it is passed per call as a date (`include_expiry_on_or_before`, §3), not read from config inside the shared filter.
- **Delta: entry band exempt; a generous upper rail applies.** The entry band's *lower* bound is the trap that made shallow-ITM rescue illegal (it forces far-OTM replacements exactly when the position needs near-money strikes). Deleted for rolls. An **upper** bound `rolling.max_replacement_delta` (new knob, default **0.60**) is kept: it admits every GOOGL-class target (0.35–0.48Δ) while blocking replace-ITM-with-deep-ITM churn — pinning shares under a near-certain-assignment cap for another 14 days in exchange for pennies delays the wheel's recycle for de minimis credit.
  *Rejected:* reusing the entry band (the documented trap); no bound at all (permits the pennies-for-14-days-of-cap case); 0.50 (would have excluded part of GOOGL's viable target list on 8/03 marks).
- **FC-013 span gate applies to the replacement — fail-closed.** The replacement is a *new short call*; it must not newly span a known earnings date. Mechanism: the roller calls `earnings_calendar.next_earnings_info(underlying)` (the tri-state surface) and passes `exclude_expiry_on_or_after=earnings_date` into `find_suitable_calls` — the parameter FC-013 already built. Tri-state posture:
  - `known` → candidates expiring on/after the date are span-rejected (inclusive `>=`, same semantics as the scanner);
  - `clear` → no span floor;
  - `unknown` → **the whole roll is skipped** with a structured event (`skip_reason=earnings_unknown`). A span test with no date cannot clear any candidate.
  This *replaces* the roller's old fail-open `is_earnings_within_n_days` blackout (`rolling.earnings_blackout_days` deleted): the span predicate subsumes it — any replacement sold within N days of a known report either expires before it (safe, allowed) or spans it (blocked).
  **The buyback/replacement distinction, stated cleanly:** the earnings gate exists to stop *opening* gap exposure. A defensive **buyback** (closing a short call) *reduces* exposure and is never earnings-gated — which is why `/monitor`'s early-close BTC path has no earnings check, correctly. This roller never places a buyback without a replacement plan, so the distinction manifests here as: the span gate filters **candidates**; when it empties the candidate set, the roller places **no order at all** (it does not close-and-stay-uncovered). The existing position riding to expiry is the pre-existing exposure FC-013 already accepted at sale time.
- **Premium floor (`min_call_premium`) does not apply to replacements.** The economics that matter are the *net credit*, not the standalone premium — and ITM replacements carry large premiums anyway, so the floor is inert where it isn't wrong. The liquidity check (volume == 0 and OI < 10) **is** kept.
- **Mechanism:** `find_suitable_calls` gains an optional `criteria_profile: str = 'entry'` parameter (`'entry'` = today's behavior, byte-identical; `'roll'` = no DTE check in the shared criteria — the expiry bound arrives as the `include_expiry_on_or_before` date (§3); delta ≤ `rolling.max_replacement_delta` with no lower bound; no premium floor; liquidity check kept). One filter site, no duplicated chain logic.
  *Rejected:* the roller building its own chain filter from `get_option_chain_with_analysis` — a second filter implementation is the FC-063 dict-boundary disease in new clothes.
- **Candidate selection order: maximum net credit among legal candidates (rev 2, decided).** The roller iterates the *legal* set (passed `validate_roll` + span + floor) and executes the candidate with the **largest net credit at the selected pricing mode's limits**, deterministically (ties → higher strike). At the 8/04 marks this picks GOOGL C375 8/21 (≈ +$248, +$5 strike) over C380 8/21 (≈ +$13, +$10 strike). Rationale: this is a *defensive* roll — bank the certain dollar. Choosing C380 spends $235 of certain credit to buy $500 of *contingent* strike value that only pays if the stock finishes above 380 — that is a delta bet, the roll-up-chasing instinct this FC explicitly excludes; every legal candidate already improves the strike strictly (validate_roll), so strike improvement is guaranteed and credit is the honest maximand. *Rejected:* rev 1's first-past-the-post on `return_score` order — that score (annualized premium yield × OTM preference) is entry-shaped; letting an ordering never designed for rolls silently pick the executed contract is exactly how tonight's book would have taken +$13 over +$248. *Rejected:* max strike gain subject to credit ≥ 0 — the contingent-upside preference argued against above.

### DD-4 — Cadence and wiring

- **New scheduler job `options-wheel-roll-daily`: `30 15 * * 1-5`, America/New_York**, same target URI/OIDC as the Friday job (verified live: `POST https://options-wheel-strategy-omnlacz6ia-uc.a.run.app/roll`, SA `799970961417-compute@developer.gserviceaccount.com`). 15:30 ET rationale: after the last `/monitor` (14:55 — profit-taking churn settled, the roller sees the surviving book) and after the last `/run` execute (15:15 — a freshly sold call is OTM by construction and simply skips `not_itm_enough`); 30 minutes of session left covers both legs' 120 s fill timeouts with margin; and it is the exact slot the Friday job already occupied, so roll marks stay comparable.
- **Created by:** the operator (or Claude with operator approval) via the exact `gcloud` commands in §Rollout — this repo has no Terraform; scheduler jobs are hand-created and documented here.
- **Friday job fate:** **paused** on deploy day (`gcloud scheduler jobs pause options-wheel-roll-friday`), **deleted** after the first verified week. Pause-then-delete keeps the rollback lever trivially reversible.
- **`/roll` endpoint:** the Friday-only weekday guard is **deleted**; the market-open guard and `strategy_lock` stay. `strategy_lock` already serializes `/roll` against `/monitor` and `/run`, so the two managers of the same positions cannot interleave.
- **Precedence with `/monitor` profit-taking, spec'd (rev 2 — corrected):** the two are near-disjoint by state — profit-taking fires on OTM winners (`unrealized_pl > 0` + DTE-band target), rolling on ITM positions (ratio ≥ 0.98) — and schedule order gives the profit-taker first claim (`/monitor` 14:55 before `/roll` 15:30). But schedule order alone is **not** sufficient, as rev 1 claimed: the monitor places **fire-and-forget DAY BTC limit orders with no cancel** (`deploy/cloud_run_server.py:979-1023`), so at 15:30 the roller can see a short call that still exists *with a live open buy order working against it*. If the roller then rolls it, both BTCs can fill → an unintended **long** call plus a sold replacement. **Fix — open-order guard:** before evaluating any position for rolling, the roller fetches open orders and **skips** any short call with an open order on its option symbol, terminal event `call_roll_skipped {open_order_conflict}` (test T-17). Skip, not cancel-first, deliberately: the working order belongs to the profit-taker, which has precedence by design — the roller canceling another subsystem's order inverts that precedence and races the fill anyway (a cancel can fail because it filled). Cost of skipping: one cycle's deferral at most — the DAY order dies at close and tomorrow's cycle sees a clean book. The guard also covers manual operator orders for free. No shared mutable state exists between the two paths (the `_closed_today` dedup set is `/monitor`-local and symbol-keyed; a rolled replacement is a different OCC symbol). **What must NOT change:** `/monitor` behavior is untouched by this FC.

### DD-5 — Observability

**Terminal-event contract:** every short call evaluated in a cycle emits **exactly one** terminal structured event. New/changed events (all via `log_trade_event`/`log_error_event`, flowing to BQ through the existing log sink):

| Event | When | Status |
|---|---|---|
| `call_roll_skipped` `{skip_reason}` | any pre-order gate fails: `not_itm_enough`, `invalid_strike`, `open_order_conflict`, `stock_quote_unavailable`, `stock_quote_unusable` (one-sided or spread-insane), `btc_quote_unavailable`, `earnings_unknown`, `no_suitable_replacement`, `no_credit_candidate`, `credit_gone_at_execution`, `cycle_budget_exhausted` | exists; new reasons added; the three silent `return None` paths (falsy/raising stock quote, zero price, zero BTC ask) now emit it — FC-066 checklist item 2 |
| `call_roll_skipped_cost_basis_unresolved` / `_divergent` | floor fail-closed | exists (FC-065 P2); **alert-wired now** — FC-066 checklist item 1 |
| `call_roll_evaluated` | viable candidate found (net credit fields, pricing mode, imminence flag) | exists; gains `pricing_mode`, `extrinsic_per_share`, `net_credit` fields; `net_debit` fields die with the debit machinery |
| `call_roll_btc_placed/filled`, `call_roll_stc_placed/filled`, `call_roll_partial_fill` | leg lifecycle | exist, kept |
| `call_roll_completed` | roll done (both legs) | exists, kept — **the alert trigger** |
| `call_roll_naked_exposure` | BTC filled (fully or partially), all STO attempts failed — shares uncovered | exists; **alert-wired now**; message clarified per DD-1 |
| `call_roll_btc_rejected` | BTC order rejected at placement — no fill, no exposure change | new (was an error log with no terminal event) |
| `call_roll_btc_timeout_canceled` | BTC unfilled at timeout, cancel confirmed, **post-cancel re-fetch shows zero fill** — position unchanged | new |
| `call_roll_dry_run` | `ROLLER_DRY_RUN=true` — full evaluation, would-be orders logged, nothing placed | new |
| `roll_cycle_completed` | cycle summary | exists, kept |

**Terminal taxonomy, exhaustive (rev 2, execution M-7) — so T-11 ("exactly N terminal events") is writable.** Every evaluated position ends in exactly one of: `call_roll_skipped{reason}` (no order was ever placed) · `call_roll_btc_rejected` (order refused; nothing live) · `call_roll_btc_timeout_canceled` (canceled with **verified** zero fill) · `call_roll_naked_exposure` (some BTC quantity filled — including a fill discovered *after* the timeout-cancel, and the partial-fill case — and the STO ladder exhausted) · `call_roll_completed` (both legs done; on a BTC partial, `contracts` = the filled quantity actually replaced). The cancel-vs-fill disposition rule (§1) decides between the last three: **events must tell the truth about what filled**, never report `btc_unfilled`/`btc_timeout_canceled` when contracts were actually bought.

**Executed-roll notification (operator must KNOW):** a new log-match alert policy `deploy/monitoring/roll_executed_alert_policy.json` on `call_roll_completed`, delivered to the FC-030 notification channel — the established pattern. Both `jsonPayload.event_type` **and** `textPayload` clauses (Cloud Run emits plain-text severity-DEFAULT logs; a jsonPayload-only or severity-filtered policy matches nothing — the FC-030 gotcha).

**FC-066 checklist item 1:** add `call_roll_skipped_cost_basis_unresolved` and `call_roll_skipped_cost_basis_divergent` to `deploy/monitoring/cost_basis_alert_policy.json`, in both payload clauses. `call_roll_naked_exposure` goes into the new roll policy as a second condition.

**decision_events: log + alert only for FC-078** (no `decision_events` rows). Rationale: `DecisionRecorder` is scan-run-shaped — minted `run_id`s, per-symbol dedup keys, stage vocabulary, flush semantics; rolls happen outside scan runs, and forcing them into that schema is schema risk on an expedited money path. Every roll event already lands in BQ via the log sink, which is the channel FC-066's own evidence used. *Deferred, not dismissed:* file a follow-up FC on merge for a `roll` stage in decision_events (noted in §Execution when opened).

**FC-066 checklist item 3 (replay-BQ gate):** the `allow_bigquery_cost_basis` forwarding (engine → roller → resolver) is **kept untouched** — it is pinned by mutation-verified tests, and the hazard it guards (replay querying production BQ) becomes live the moment this plan fixes the quote keys.

### DD-6 — State: delete the premium dependency, don't repair it

Credit-only makes `original_premium` irrelevant — the debit tolerance was the only consumer. **Deletions** (this resolves the roller's slice of FC-069 item 8, and is the "delete the fiction" direction):

- `CallRoller._check_debit_tolerance` — gone (replaced by the credit invariant).
- `_compute_net_roll_economics`: drops `original_premium` input and `debit_pct_of_premium` output; becomes a pure `net_credit` computation on the two limit prices.
- `wheel_state.get_active_call_details` / `set_active_call_details` / `record_call_roll` / `get_roll_count` / `remove_position` / `add_call_position` calls in the roller — **all gone**. The state was never persisted (`STATE_STORAGE_BUCKET` unset since project start); Alpaca positions are the truth and `reconcile_positions` already rebuilds from them. Half-maintaining fiction is worse than none.
- `CallRoller.__init__` loses the `wheel_state` parameter; `WheelEngine.run_rolling_cycle` stops passing it.
- `call_roll_blocked_debit` event — gone (no debit path exists to block).

*Rejected:* deriving `original_premium` statelessly from Alpaca order history (FC-066's old fix direction) — that repairs an input the new design doesn't consume.

**`rolling.*` knob disposition (FC-069 item 13's HOLD resolves here):**

| Knob | Fate | Why |
|---|---|---|
| `enabled` | **kept** + env override (DD-7) | master switch |
| `itm_trigger_ratio` (0.98) | **kept** | DD-2 eligibility |
| `btc_fill_timeout_seconds` (120) | **kept** | leg fill polling |
| `fallback_strike_attempts` (2) | **kept** | STO retry ladder width |
| `max_extension_days` (**new**, 14) | **added** | DD-3 — replacement expiry ≤ **old expiry** + N (expiry-relative, rev 2) |
| `max_replacement_delta` (**new**, 0.60) | **added** | DD-3 |
| `min_net_credit_per_contract` (**new**, 0.00) | **added** | DD-1 |
| `imminence_extrinsic_threshold` (**new**, 0.20) | **added** | DD-2 |
| `trigger_time_et` | **deleted** | never consumed by code; the scheduler owns timing |
| `max_current_dte` | **deleted** | DD-2 (the eligibility trap) |
| `max_debit_pct_of_premium`, `max_debit_pct_of_notional` | **deleted** | debit machinery gone (DD-1) |
| `max_rolls_per_position` | **deleted** | DD-2 (never bound; hazard removed by credit-only) |
| `earnings_blackout_days` | **deleted** | DD-3 (span predicate subsumes, fail-closed) |
| `btc_limit_over_ask_pct`, `stc_limit_under_bid_pct` | **deleted** | DD-1 (pads break/waste the credit invariant) |

Corresponding `Config` properties are deleted/added in `src/utils/config.py`; config validation gains bounds checks for the four new knobs (dte ≥ 1, 0 < delta ≤ 1, credit ≥ 0, extrinsic ≥ 0).

### DD-7 — Kill switch + rollback

- **Env lever:** `ROLLER_ENABLED` overrides `rolling.enabled` when set, exactly mirroring the `EARNINGS_ENABLED` pattern (`src/utils/config.py:712-738` — env wins over yaml, unparseable value logs and falls back). Kill without deploy: `gcloud run services update options-wheel-strategy --update-env-vars ROLLER_ENABLED=false` (**never** `--set-env-vars` — it wipes the whole env set). `run_rolling_cycle` already returns `{'skipped': 'rolling_disabled'}` when disabled.
- **Dry-run:** `ROLLER_DRY_RUN=true` → full evaluation pipeline, `call_roll_dry_run` events with the exact would-be orders, zero orders placed. Operational debugging flag only; **not** a launch gate (operator decision).
- **Single revert:** all code changes land in one PR. `git revert` of the squash commit restores the Friday-only guard in `/roll` — which makes the daily scheduler's Mon–Thu calls harmless no-ops (`skipped: not_friday`) and Fridays revert to the dead as-built roller. The revert alone is therefore safe even *before* touching the scheduler; deleting the daily job and resuming the Friday job completes the restoration.
- **Instant vs full rollback:** env flip (~1 min, new revision) for "stop rolling now"; revert + scheduler flip for "restore the old world".

### DD-8 — Launch + verification (no log-only gate, per operator)

Deploy-day sequence in §Rollout. Post-first-roll verification checklist in §Verification. **First-cycle expectation at the 2026-08-04 review marks (mark-dependent — re-derive at execution time, do not treat as a promise). The GOOGL window has *improved* since the 8/03 investigation:**

| Position | Expected outcome | Why |
|---|---|---|
| GOOGL C370 8/07 (ITM) | **the expected roll.** Legal candidates at 8/04 marks: C375 8/21 ≈ **+$248**/contract credit (+$5 strike); C380 8/21 ≈ +$13 (+$10 strike) — both ≤ old-expiry + 14 (8/21). Max-credit selection (DD-3) picks **C375 8/21**; expect `call_roll_completed` | the GOOGL-class window this FC is expedited for — and the exact trade rev 1's eval-relative DTE frame would have excluded (trader B-1) |
| AMZN C262.5 8/07 (deep ITM) | `call_roll_skipped {no_credit_candidate}` | every strike-improving roll is a four-figure debit; **this skip event is itself the launch verification that credit-only holds** |
| AAPL C312.5 8/05 (OTM) | `call_roll_skipped {not_itm_enough}` — or the position is already gone (expires 8/05) | profit-taker's territory |
| NVDA C220 8/10 (OTM) | `call_roll_skipped {not_itm_enough}` | same; note NVDA earnings 8/26 — any replacement expiring ≥ 8/26 would be span-blocked if it were ITM |

Finding **nothing** executable is also a pass — the pass criterion is 4 evaluated / 4 terminal events / 0 debits, not "a roll happened."

---

## Proposed approach — exact changes by file

### 1. `src/strategy/call_roller.py` (the bulk)

- **`__init__`**: drop `wheel_state` param. Keep `alpaca_client, market_data, config, risk_manager, earnings_calendar, allow_bigquery_cost_basis` (forwarding kept — FC-066 item 3).
- **`should_roll`**: keep ITM-ratio gate (+ `invalid_strike`). Add the **open-order guard** (DD-4): an open order on the position's option symbol → `(False, 'open_order_conflict')`. Delete DTE gate, max-rolls gate, `is_earnings_within_n_days` blackout. Returns `(bool, reason)` unchanged in shape. (The cycle fetches open option orders once per run and passes the symbol set in — one API call per cycle, not per position.)
- **`evaluate_roll_opportunity`**:
  - **Quote-key fix (FC-066 cause 1) + quote-quality guard (rev 2, trader M-5):** stock price = mid of `quote['bid']`/`quote['ask']`, **fail-closed on quality**: require both sides > 0 AND `ask/bid ≤ 1.05` (spread-sanity bound — live IEX example: AAPL bid 287.82 / ask 318.75 makes the ITM ratio 0.92 or 1.02 depending on the side you trust; a one-sided or spread-insane quote must not gate a money decision). Violation → skip event `stock_quote_unusable`. The rev 1 "whichever side is positive" fallback is **deleted**. Wrap `get_stock_quote` in try/except — **it raises on failure** (the `if not quote` branch is unreachable as written); exception → skip event `stock_quote_unavailable`.
  - BTC quote: `btc_quote.get('ask')` / `.get('bid')` (client returns `bid`/`ask`/`mid_price` — the `:219 ask_price` key bug dies). Zero/missing ask → skip event `btc_quote_unavailable`. Imminence mode and the extrinsic computation additionally require both option sides > 0 (a mid needs two sides); otherwise base pricing mode applies and extrinsic is treated as above-threshold (no override on junk data).
  - Extrinsic computation + imminence flag (DD-2); select pricing mode.
  - Span gate: `next_earnings_info(underlying)`; `unknown` → skip event `earnings_unknown`; `known` → pass `exclude_expiry_on_or_after=earnings_date` to `find_suitable_calls`; `clear` → no floor. Roller must be constructed with an earnings calendar whenever `earnings.enabled` (already true in `run_rolling_cycle`); **if `earnings_calendar is None` and earnings are enabled → skip `earnings_unknown`** (fail closed, never fail open on a missing service). If `earnings.enabled` is false, no span gate (same posture as the scanner).
  - Candidate search: `find_suitable_calls(underlying, min_strike_price=min_strike, exclude_expiry_on_or_after=…, include_expiry_on_or_before=old_expiry + rolling_max_extension_days, criteria_profile='roll')`.
  - Credit screen: evaluate **every** candidate against `validate_roll` + the credit invariant at the selected pricing mode's limits; execute the legal candidate with **maximum net credit** (DD-3; ties → higher strike). All candidates fail → terminal skip `no_credit_candidate` (replaces `no_candidate_passed_economics`; `call_roll_blocked_debit` deleted). The full legal candidate list (credit-ordered) is carried on the opportunity for the STO fallback ladder — the ladder **reuses this list and never re-queries** (M-8 resolution, below).
  - Opportunity dict gains `pricing_mode`, `btc_limit`, `stc_limit`, `net_credit_per_contract`, `extrinsic_per_share`, `fallback_candidates`; drops `original_premium`.
- **`execute_roll`** — full order-lifecycle spec (rev 2):
  - **Pre-BTC invariant re-check (trader M-3):** re-fetch the primary candidate's STO quote; recompute the invariant at current prices; failure → terminal skip `credit_gone_at_execution`, no order placed.
  - BTC at `opportunity['btc_limit']`; poll to `btc_fill_timeout_seconds`.
  - **BTC timeout → cancel, then verify (trader M-4 = execution H-2):** on timeout, cancel the order, then **re-fetch it** and dispatch on what actually happened: `filled` → proceed to the STO ladder as normal; `partially_filled` → cancel confirmed for the remainder, proceed to STO for the **filled** quantity; canceled with zero fill → terminal `call_roll_btc_timeout_canceled`, position unchanged. "Cancel rejected because already filled" is a *fill*, not an error. Events must report the true disposition — never `btc_unfilled` when contracts were bought (test T-10, which pins the post-cancel disposition, not merely that cancel was called).
  - **Partial-fill truth (execution H-4):** rev 1's "existing behavior kept" was wrong — as-built passes `contracts` (the requested qty) to the STO leg and `_poll_order_fill` returns on the first `partially_filled` with the remainder still working. New behavior: **STO qty = BTC filled qty**, and the BTC remainder is canceled before the STO is placed (the position keeps `contracts − filled` of the old call — still covered, still evaluated tomorrow). Test T-19.
  - All wheel-state calls deleted. `call_roll_completed` unchanged in name (alert contract), fields updated (`net_credit`, `pricing_mode`, `contracts` = quantity actually replaced; `roll_count` dropped).
- **`_attempt_stc` — fallback-path repair (as-built bugs, in scope because this FC makes the path live):**
  - **Cancel-then-verify between rungs (execution BLOCKER B-1):** as-built, `_place_and_poll_stc` returns `None` on timeout with the DAY limit order **still live**, and the ladder then places the next sell — two live sells against one covered lot; a late fill on rung 1 while rung 2 works = a **naked short call** (or Alpaca rejects rung 2 and the ladder is dead weight). New behavior: before placing any subsequent rung, **cancel the prior rung's order and verify its terminal state**. "Cancel failed because filled" = that rung **succeeded** — return it. A partial fill on a rung: cancel the remainder, treat the filled quantity as that rung's result (the uncovered remainder falls through to the naked-exposure terminal if no later rung covers it). At most one STO order live at any instant — an invariant, mutation-tested (T-18).
  - **Re-query ambiguity resolved: reuse, never re-query (execution M-8).** Fallback rungs draw exclusively from `opportunity['fallback_candidates']` — the evaluation's span/profile/floor-filtered, credit-ordered list. A fresh `find_suitable_calls` call at execute time could admit candidates filtered under a *different* earnings-cache state or drifted deltas — an unfiltered order is a money bug; price freshness is obtained per-rung by re-fetching the **candidate's quote** and recomputing the invariant against the *actual BTC fill price* (`STO_limit ≥ btc_filled_price + min_credit_per_share`), not by re-filtering the chain. (T-2's mutation extends to this call site: substituting an unfiltered chain query for the stored list must fail.)
  - Retry ladder, bounded: (1) primary candidate at `stc_limit`; (2) primary at the invariant-minimum price (`btc_filled_price + min_credit`) if that is below rung 1's price — never below it; (3) up to `fallback_strike_attempts` further candidates from the stored list, each re-passing `validate_roll` + the invariant. Every rung transition is cancel-then-verify. No fill after the ladder → `call_roll_naked_exposure` terminal (DD-1 recovery).
- **`_compute_net_roll_economics`** → renamed/reduced to credit computation (DD-6). **`_check_debit_tolerance`** deleted.
- Docstrings: "Friday EOW" language replaced everywhere.

### 2. `src/risk/risk_manager.py` — `validate_roll`

- Keep: strike > current; strike ≥ cost basis.
- Replace delta-band check: `abs(delta) ≤ config.rolling_max_replacement_delta` (no lower bound).
- Replace DTE check with the expiry-relative bound: `validate_roll` gains a `max_expiry: date` argument (computed by the roller as old expiry + `rolling_max_extension_days`); candidate `expiration_date ≤ max_expiry`, else rejected (was `dte ≤ call_target_dte`, the eval-relative frame trader B-1 killed).
- Delete the `min_call_premium` check (DD-3).
- **What must NOT change:** `validate_new_position` and everything else in the file.

### 3. `src/api/market_data.py` — `find_suitable_calls` + `_check_call_criteria_detailed`

- Add `criteria_profile: str = 'entry'` to `find_suitable_calls`; thread to `_check_call_criteria_detailed(call, profile)`. `'entry'` = byte-identical current behavior (existing callers untouched, param default inert). `'roll'` = no DTE check in the shared criteria (the bound is per-position, so it arrives as a date parameter — below); delta ≤ `rolling_max_replacement_delta` only; premium floor skipped; liquidity check kept.
- Add `include_expiry_on_or_before: Optional[date] = None` — the expiry-relative roll-out ceiling, symmetric with (and applied alongside) the FC-013 `exclude_expiry_on_or_after` span floor. `None` = inert, existing callers untouched.
- STAGE 8 log line gains `criteria_profile` field so scan-vs-roll chain queries are distinguishable in BQ.

### 4. `src/strategy/wheel_engine.py` — `run_rolling_cycle`

- Stop passing `wheel_state` to `CallRoller`.
- **Cycle time budget (rev 2, trader H-2 = execution M-5):** worst case per position ≈ **10 min** (BTC 120 s poll + cancel/verify + up to 4 STO rungs × 120 s); a 4-position ITM day would exceed any sane request deadline, and a mid-cycle kill **between BTC fill and STO placement is the worst seam** (shares uncovered, no event, no alert). Spec: the cycle tracks elapsed time against a budget constant (`_CYCLE_BUDGET_SECONDS = 1500`, i.e. the 1800 s scheduler attempt-deadline minus headroom) and **refuses to *start* a position unless remaining budget ≥ the per-position worst case (600 s)** — deferred positions emit terminal `call_roll_skipped {cycle_budget_exhausted}` and get tomorrow's cycle. This guarantees no position begins a BTC it cannot finish the ladder for within the deadline. Realistic load is 0–1 executable roll per day; the guard exists for the pathological day.
- Fetch open option orders once per cycle for the open-order guard (DD-4), via the existing `AlpacaClient.get_orders` / `cancel_order` (both exist — no new client surface). **FC-043 gotcha applies:** Alpaca's `status` parameter takes query tokens, not status values, and defaults to open/limit-50 — the builder must verify the call actually returns *open* orders (the FC-043 postmortem is the reference) rather than assuming a `status='open'` string works.
- Docstring: daily, not Friday.
- **What must NOT change:** `reconcile_positions`; the `allow_bigquery_cost_basis` forwarding; the injected-earnings-calendar seam (backtests depend on it); the `rolling_enabled` early-return.

### 5. `deploy/cloud_run_server.py` — `/roll`

- Delete the Friday-only weekday guard (lines ~1190-1198). Keep `_is_market_open` guard, `strategy_lock`, `@require_api_key`, `@require_account_match`, error handling. Docstring updated.

### 6. `src/utils/config.py`

- `EARNINGS_ENABLED`-pattern env override for `rolling.enabled` via `ROLLER_ENABLED` (mirror `config.py:712-738` exactly, including the unparseable-value warning).
- Delete properties: `rolling_trigger_time_et`, `rolling_max_current_dte`, `rolling_max_debit_pct_of_premium`, `rolling_max_debit_pct_of_notional`, `rolling_max_rolls_per_position`, `rolling_earnings_blackout_days`, `rolling_btc_limit_over_ask_pct`, `rolling_stc_limit_under_bid_pct`.
- Add properties + validation: `rolling_max_extension_days` (int ≥ 1), `rolling_max_replacement_delta` (0 < x ≤ 1), `rolling_min_net_credit_per_contract` (≥ 0), `rolling_imminence_extrinsic_threshold` (≥ 0).
- `ROLLER_DRY_RUN` accessor (env-only, default false).

### 7. `config/settings.yaml`

- `rolling:` block rewritten per the DD-6 table (comment header: "FC-078 — daily credit-only defensive rolls").

### 8. `deploy/monitoring/`

- New `roll_executed_alert_policy.json`: conditions for `call_roll_completed` (notify: a roll happened) and `call_roll_naked_exposure` (notify: stranded BTC). Both `jsonPayload.event_type` and `textPayload` clauses. FC-030 notification channel.
- Edit `cost_basis_alert_policy.json`: add `call_roll_skipped_cost_basis_unresolved` + `_divergent` to both clauses.
- Both applied via `gcloud alpha monitoring policies create/update` in §Rollout (mind the 4000-char documentation cap — FC-013 lesson).

### 9. `src/backtesting/engine/simulator.py` — replay cadence decision

**Decision: the replay mirrors production — `run_rolling_cycle()` runs every trading day** (the `if day.weekday() == self.roll_weekday` guard is removed; `roll_weekday` param deleted). Rationale: the simulator's stated purpose is replaying the production week; a Friday-only replay of a daily production roller would misstate roll frequency and credit capture in every future measurement. Consequences, accepted and flagged:
- The FC-068 tripwire test (golden replay asserts `rolls_executed == 0`) **flips by design** — exactly what its authors intended ("FC-066's fix flips a test rather than silently changing every measurement"). It becomes an assertion that roller *events* are present and every executed roll in replay satisfied the credit invariant.
- Backtest fidelity figures are already stale pending FC-068 re-measurement (`docs/BACKTEST_ENGINE.md`); the roller change folds into that same re-baseline. **Non-blocking follow-up, not this PR's problem** — but note it in `BACKTEST_ENGINE.md`'s staleness paragraph in this PR (one sentence).
- `HistoricalEarningsCalendar` already implements `next_earnings_info` with the same tri-state contract (`src/backtesting/engine/historical_earnings.py:108`), so the span gate works in replay. It never returns `unknown` (documented asymmetry) — acceptable; the replay's calendar is a complete table.
- Replay cost: one extra quote lookup per ITM short call per day, served from the local lake — negligible.

### 10. `tools/backtesting/fetch_earnings_table.py` — stale docstring (rev 2, one line)

- Line ~17 claims "the only consumer is the rolling blackout (`rolling_earnings_blackout_days`, currently 2) … a handful of Friday roll decisions per symbol-year." Stale twice over: FC-013 made the scanner gate a consumer, and this FC deletes the blackout and makes the **daily span-on-replacement check** the roller-side consumer. Correct the paragraph.

### 11. `docs/FUTURE_CONSIDERATIONS.md` (bookkeeping, same PR or direct-to-main after merge)

- FC-078 → Executing/Done with plan+PR links; FC-066 → Closed-superseded (lineage note); FC-062 → Closed (floor half was FC-065 P2, gate-routing half here); FC-069 item 13 HOLD → resolved per DD-6 table.

## Reuse

- `CostBasisResolver` / `resolve_detailed` + the two skip events (FC-065 P2) — already in the roller; untouched.
- `find_suitable_calls`'s `exclude_expiry_on_or_after` span parameter (FC-013) — the roller becomes its second caller.
- `next_earnings_info` tri-state surface (FC-013) — no new earnings plumbing.
- `EARNINGS_ENABLED` env-override pattern (`config.py`) for `ROLLER_ENABLED`.
- Alert-policy JSON pattern (`cost_basis_alert_policy.json`) incl. the dual payload clauses.
- `parse_option_symbol` / `strict_option_type` — never hand-roll OCC parsing (six prior instances).

## Behavior contract (summary)

**Inputs:** live Alpaca positions (short calls + underlying stock), IEX stock quotes (`bid`/`ask`), OPRA option quotes (`bid`/`ask`/`mid_price`), `CostBasisResolver` floor, `next_earnings_info` tri-state, `rolling.*` config.

**Outputs:** at most one BTC+STO limit-order pair per ITM short call per cycle, satisfying ALL of: new strike > old; new strike ≥ floor (≥, at-floor legal); replacement expiry ≤ **old expiry + 14 days**; replacement Δ ≤ 0.60; replacement does not span a known earnings date (unknown → no order); `STO_limit − BTC_limit ≥ min_credit ≥ 0` at every placement and repricing; at most one live STO order at any instant; no position started without 600 s of cycle budget. Exactly one terminal event per evaluated position. `call_roll_completed` triggers an operator notification.

**Edge cases:** quote fetch raises or fails the quality guard → skip event, next position continues (per-position try/except in the cycle loop — one bad symbol must not kill the cycle); open order on the option symbol → `open_order_conflict` skip (DD-4); BTC unfilled at timeout → **cancel, then verify the true disposition** — filled/partial fills proceed into the ladder for the filled qty, verified-zero-fill terminates `call_roll_btc_timeout_canceled` (§1; as-built left the DAY order live and would have reported a fiction); BTC partial fill → STO qty = **BTC filled qty**, remainder canceled (as-built wrongly passed the requested qty and returned on first `partially_filled`); STO rung transitions → cancel-then-verify, at most one live STO order ever (§1); STO ladder exhausted → uncovered-shares recovery with the honest composite residual (DD-1); cycle budget insufficient to start a position → `cycle_budget_exhausted` deferral (§4); market closed → endpoint guard; `ROLLER_ENABLED=false` → cycle no-op with `rolling_disabled`; `ROLLER_DRY_RUN=true` → evaluation only.

**What must NOT change:** `/monitor` and `/run` behavior; entry-side `find_suitable_calls` results (profile default inert — pinned by test); scanner FC-013 gate; `reconcile_positions`; `allow_bigquery_cost_basis` forwarding; `require_account_match` on `/roll`.

## Tests (each names the regression it catches)

Existing suite: `tests/test_call_roller.py` (rewrite alongside), `test_wheel_engine.py`, `test_wheel_engine_injection.py`, `test_backtest_simulator.py`, `test_backtest_earnings.py`, `test_risk_manager.py`, `test_market_data.py`, `test_config.py`.

**Mutation-check list (reviewer: verify each test fails under the stated mutation):**

- **T-1 credit-only:** candidate with `STO_limit < BTC_limit` → no order placed, terminal `no_credit_candidate`. *Mutation:* flip the invariant to allow a $0.01 debit → test fails. Also: invariant enforced on *limit prices*, so a test where marks show credit but limits don't must still refuse.
- **T-2 span-on-replacement wired:** earnings inside the roll horizon; candidate expiring after the date is rejected, before is accepted; `unknown` tri-state → whole position skipped `earnings_unknown`; `earnings_calendar=None` with earnings enabled → same skip. *Mutations:* drop `exclude_expiry_on_or_after` from the roller's `find_suitable_calls` call → fails; **substitute an unfiltered chain query for `fallback_candidates` in the STO ladder** (the M-8 call site) → fails.
- **T-3 quote-key regression:** `evaluate_roll_opportunity` against a client stub returning `{'bid': x, 'ask': y}` (stock) and `{'bid','ask','mid_price'}` (option) proceeds past pricing. *Mutation:* reintroduce `last_price`/`ask_price` reads → test fails. Plus: `get_stock_quote` raising → `stock_quote_unavailable` skip event (not a silent None, not a crash).
- **T-4 floor routing:** resolver unresolved/divergent → the two skip events, no order (exists — keep); candidate below floor rejected by `validate_roll`; at-floor candidate accepted. *Mutation:* drop `cost_basis_per_share` from the `min_strike` max() → test fails.
- **T-5 fallback-path floor/credit:** the STO fallback ladder re-validates each candidate and recomputes the invariant against the actual BTC fill. *Mutation:* remove `validate_roll` or the credit recomputation from the fallback path → test fails. (This pins the as-built bypass repair.)
- **T-6 eligibility:** OTM (ratio < 0.98) skipped `not_itm_enough`; ITM at DTE 7 evaluated (no DTE gate — *mutation:* reintroduce a `max_current_dte` gate → test fails); imminence override activates mid-based pricing iff extrinsic ≤ threshold (*mutation:* invert the comparison → fails).
- **T-7 replacement constraints:** Δ 0.45 candidate accepted (entry band would reject — *mutation:* restore entry-band reuse in `validate_roll` → fails); Δ 0.70 rejected; **horizon is expiry-relative:** candidate at old-expiry + 14 accepted, old-expiry + 15 rejected, and — the B-1 pin — a candidate at old-expiry + 14 that is **> 14 DTE from the evaluation date** is accepted (*mutation:* re-anchor the bound to the evaluation date → fails); sub-`min_call_premium` replacement accepted in roll profile (*mutation:* apply premium floor in roll profile → fails).
- **T-7b max-credit selection:** among legal candidates {+$13, +$248}, the +$248 candidate is executed. *Mutation:* revert to first-past-the-post on `return_score` order (with a fixture where score order ≠ credit order) → fails.
- **T-8 entry profile inert:** `find_suitable_calls` with default profile produces byte-identical results to pre-change (fixture-pinned). *Catches:* the roll profile leaking into the scanner path.
- **T-9 leg order:** BTC order placed before any STO (call-order assertion on the client stub). *Mutation:* reorder → fails. STO total failure → `call_roll_naked_exposure` event emitted, no wheel-state calls attempted.
- **T-10 BTC timeout cancel + post-cancel disposition (rev 2):** pins the disposition, not just that cancel was called. Three parameterized cases: (a) cancel confirmed, re-fetch shows zero fill → terminal `call_roll_btc_timeout_canceled`, no STO placed; (b) re-fetch shows **filled** (cancel lost the race) → STO ladder proceeds, terminal is `completed` or `naked_exposure` — never `btc_unfilled`; (c) re-fetch shows partial → remainder canceled, STO for filled qty. *Mutations:* drop the cancel → fails; drop the post-cancel re-fetch (report timeout without checking) → case (b) fails.
- **T-11 terminal-event contract:** a cycle over N positions emits exactly N terminal events (parameterized over skip/execute paths). *Catches:* any reintroduced silent path.
- **T-12 state deletion:** `CallRoller` constructible without `wheel_state`; grep-level test that the roller module no longer references wheel-state methods (import-time assertion or constructor signature test). *Catches:* half-revival of the fiction.
- **T-13 config:** new knobs parse + validate; deleted properties gone; `ROLLER_ENABLED` env override wins over yaml both ways; unparseable value falls back with warning (mirror the `EARNINGS_ENABLED` tests).
- **T-14 endpoint:** `/roll` runs on a Tuesday (Friday guard gone — *mutation:* restore guard → fails); market-closed still guarded; `require_account_match` still applied (pin the decorator).
- **T-15 simulator cadence:** replay calls `run_rolling_cycle` daily; FC-068 tripwire updated: executed replay rolls each satisfy credit ≥ 0 and strike-increase (assert on the replay's roll records). *Mutation:* Friday-only guard restored in simulator → fails.
- **T-16 dry-run:** `ROLLER_DRY_RUN=true` → `call_roll_dry_run` emitted with both would-be limits, `place_option_order` never called.
- **T-17 open-order conflict (rev 2):** a short call with a live open order on its option symbol is skipped with `open_order_conflict`; the same book without the open order proceeds. *Mutation:* drop the open-order guard → fails.
- **T-18 STO ladder cancel-then-verify (rev 2, execution B-1):** across a two-rung ladder, rung 1's order is canceled-and-verified before rung 2 is placed; "cancel failed because filled" returns rung 1 as the success; at most one STO order live at any instant (call-sequence assertion on the client stub). *Mutation:* drop the inter-rung cancel → fails.
- **T-19 partial-fill truth (rev 2, execution H-4):** BTC fills 1 of 2 contracts → BTC remainder canceled, STO placed for qty 1, `call_roll_completed.contracts == 1`, position retains 1 old contract. *Mutation:* pass the requested qty to the STO leg → fails.
- **T-20 cycle budget guard (rev 2):** with remaining budget < 600 s, the next position emits `cycle_budget_exhausted` and no orders are placed; with fresh budget it proceeds. *Mutation:* drop the pre-position budget check → fails.
- **T-21 pre-BTC invariant re-check (rev 2, trader M-3):** STO quote decays between evaluation and execution below the invariant → terminal `credit_gone_at_execution`, zero orders placed. *Mutation:* drop the re-check → fails.
- **T-22 quote-quality guard (rev 2, trader M-5):** one-sided stock quote (bid 0) and spread-insane quote (ask/bid > 1.05, the AAPL 287.82/318.75 shape) both skip `stock_quote_unusable`; one-sided option quote suppresses imminence mode (base pricing used). *Mutation:* restore the "whichever side is positive" fallback → fails.

Hermeticity: all tests run against stubs/fixtures; no live BQ/GCS/Alpaca (conftest guards already block ambient-environment leaks — keep them green).

## Risks & mitigations

- **Risk: fill-quality gap between limits and reality** (STO at bid may not fill on a moving stock). Mitigation: pre-BTC invariant re-check (shrinks staleness to the BTC poll), retry ladder with invariant-floor repricing and cancel-then-verify rung transitions (DD-1, §1). **Accepted residual, stated honestly (rev 2):** worst case is the full BTC debit paid in cash — GOOGL-class ≈ $839, ~$580 of it extrinsic — with the re-cover happening through the entry path at entry-band premiums, adversely selected on down-ticks; near a report the span gate can hold the shares uncovered through the event (DD-1 recovery, composite outcome).
- **Risk: the roller and profit-taker manage the same position on the same day.** Mitigation: schedule order + state disjointness + the **open-order guard** (DD-4 — schedule order alone is insufficient because the monitor's DAY BTC limits outlive 14:55); `strategy_lock` serializes the endpoints. Residual: none identified with the guard in place.
- **Risk: repeated daily re-rolls (churn).** Bounded: strike strictly increases per roll (monotone), each roll nets ≥ 0, Δ ≤ 0.60 rail. **Per-day bound, stated honestly (rev 2, L-9):** one roll per position per *cycle*; deploy-day step 5's manual trigger plus the scheduled run means **up to two rolls on one position in one day is possible** — each independently credit-≥ 0 and strike-increasing, so the composite is still profitable and monotone, but "one per day" is a per-cycle claim, not a hard invariant. A pathological daily-credit staircase is *profitable by construction*; the `min_net_credit_per_contract` knob exists if the operator wants friction.
- **Risk: mid-cycle kill (deadline/crash) between BTC fill and STO placement — the worst seam.** Mitigation: the cycle budget guard refuses to start any position without the full per-position worst case (600 s) of remaining budget (§4); the scheduler attempt-deadline is set explicitly to 1800 s and retries are 0 (§Rollout), so a killed request is never blindly re-entered. Residual: a Cloud Run instance crash mid-ladder still strands a BTC — the naked-exposure alert + DD-1 recovery is the backstop.
- **Risk: span gate blocks the whole candidate set silently.** Mitigation: distinct skip reasons (`earnings_unknown` vs `no_suitable_replacement` vs `no_credit_candidate`) + T-11's event contract; cost-basis skips alert-wired.
- **Risk: expedited timeline compresses review.** Non-negotiable per house rules: two adversarial reviews (fresh contexts, Fable, different personas) + confirmation pass still gate the merge. The expedite compresses idle time, not the gate.
- **Risk: 8/07 expiry passes before deploy.** Accepted: the AMZN/GOOGL 8/07 calls assign at expiry (assignment above basis is the wheel working — +$352/+$315); the roller then serves the *next* cycle. The expedite is opportunity capture, not loss prevention.

## Rollout (deploy-day sequence)

1. **Merge** the single FC-078 PR (two adversarial reviews + confirmation pass complete; verdicts dispositioned).
2. **Pause the Friday job BEFORE deploying** (rev 2, L-10): `gcloud scheduler jobs pause options-wheel-roll-friday`. Otherwise a Friday deploy puts new roller code live under the *old* trigger before alert policies and the daily job exist — the first live roll would happen unwatched. (Equivalently: deploy on a non-Friday; pausing first makes the ordering unconditional.)
3. **Deploy** the Cloud Run revision (standard deploy; env changes — if any — via `--update-env-vars` only). **Verify the service request timeout** covers the cycle budget: `gcloud run services describe options-wheel-strategy --format="value(spec.template.spec.timeoutSeconds)"` — must be ≥ 1800 s; raise it if lower (the cycle budget guard assumes the request survives to the 1800 s attempt-deadline).
4. **Alert policies before first cycle** (rev 2, L-12 runbook): created by the **orchestrator post-merge** (the deploy SA holds `monitoring.alertPolicies.*`) — create `roll_executed_alert_policy.json`, update `cost_basis_alert_policy.json` (`gcloud alpha monitoring policies …`, FC-030 channel; verify with a policies-list). Set `alertStrategy.notificationRateLimit.period: 300s` on the log-match conditions so a **second same-day roll still notifies** (the default suppression window would swallow it); `autoClose: 1800s`.
5. **Scheduler flip:**
   ```
   gcloud scheduler jobs create http options-wheel-roll-daily \
     --location us-central1 --schedule "30 15 * * 1-5" \
     --time-zone "America/New_York" \
     --uri "https://options-wheel-strategy-omnlacz6ia-uc.a.run.app/roll" \
     --http-method POST \
     --attempt-deadline 1800s \
     --oidc-service-account-email 799970961417-compute@developer.gserviceaccount.com
   ```
   (Mirror any headers/auth the Friday job carries — `describe` it first and copy exactly.) **Timing budget, explicit (rev 2, trader H-2 = execution M-5):** the Friday job's live `attemptDeadline` is the 180 s default with `retryCount` unset = **0** (verified live 2026-08-04) — 180 s would kill a real roll mid-ladder, the worst seam. The daily job sets `--attempt-deadline 1800s` (Scheduler's HTTP max is 30 m) against the worst case ≈ 600 s/position with the cycle budget guard (§4) capping total work at 1500 s. **Retry posture: keep retries at 0, deliberately** — a scheduler retry after a deadline kill would re-enter a cycle whose partial order state it cannot see; the daily cadence *is* the retry.
6. **Supervised first cycle:** during market hours, manually `POST /roll` (or `gcloud scheduler jobs run options-wheel-roll-daily`) and watch logs live. Next scheduled cycle is live regardless (operator decision — no gate). Note the L-9 bound: a manual trigger plus the 15:30 scheduled run can produce **two rolls on one position in one day** — each credit-≥ 0 and strike-increasing, accepted (§Risks).
7. **Post-first-roll verification checklist** (run after the first `call_roll_completed`, or after the first full cycle if nothing executed):
   - [ ] Terminal-event contract: N short calls → N terminal events in logs/BQ.
   - [ ] If executed: both fills present in the Alpaca **activities feed** (`trades_from_activities` after the next 15-min ingest), order IDs matching the `call_roll_completed` event; **no residual open orders** on either leg's symbol (cancel-then-verify worked).
   - [ ] Assertions against the executed pair: new strike > old; new strike ≥ Alpaca `avg_entry_price`; replacement expiry ≤ **old expiry + 14 days** and < next earnings date (check `earnings_dates.json`/Finnhub); **fill credit = STO fill − BTC fill ≥ 0**; `call_roll_completed.contracts` matches the filled quantities.
   - [ ] FC-030-channel notification received for `call_roll_completed`.
   - [ ] If AMZN-class position present: its `no_credit_candidate` skip event exists (the credit-only proof).
   - [ ] No `call_roll_naked_exposure`; if one fired, confirm the alert arrived and the next `/run` re-covered.
8. **After first verified week:** delete `options-wheel-roll-friday`; file the decision_events follow-up FC; update FC index + this plan's §Execution.

**Rollback:** instant — `--update-env-vars ROLLER_ENABLED=false`. Full — revert the PR commit (restores the Friday-only guard, making the daily job a no-op — safe orphan), pause/delete `options-wheel-roll-daily`, resume `options-wheel-roll-friday`, roll back the alert-policy edits (delete new policy; remove the two added event types).

## Open questions

- **None blocking.** All eight design decisions are made above; operator decisions of 2026-08-04 are incorporated as binding.
- *Non-blocking, for review/operator taste:* `max_replacement_delta` default (0.60 chosen; 0.50–0.75 all defensible — DD-3); `imminence_extrinsic_threshold` default (0.20 chosen — DD-2); whether the Friday job is deleted after one week or kept paused for a month (cosmetic).
- *Non-blocking follow-ups filed on merge:* decision_events `roll` stage (DD-5); backtest fidelity re-baseline including roller effects (folds into the existing FC-068 staleness item, §9).

## Execution

**Status:** built on branch `fc-078/roller-revival` (base `origin/main @ ec53933`).
**No PR opened; nothing merged.** The two-reviewer gate and the confirmation pass
have not been run.

- **Branch:** `fc-078/roller-revival`
- **Commits:**
  - `93592bd` — FC-078: revive the roller — daily, credit-only defensive rolls
  - `7326df1` — FC-078: pin the execute-time floor on stored ladder candidates
  - `af9c213` — FC-078: prove the terminal-event taxonomy exhaustively
  - `4b9120e` — FC-078: PR-body draft + plan Execution section
  - `1239978` — FC-078: disposition both adversarial reviews (REQUEST_CHANGES ×2)
  - `4e78f3c` — FC-078: pin the shipped cancel-settle bound
- **PR body draft:** `docs/plans/fc-078-pr-body-draft.md` (per-section summary,
  full mutation record, post-merge runbook, DD-8 table restated)
- **Date:** 2026-08-04

### Validation

| Check | Result |
|---|---|
| Full suite at baseline `ec53933` | 1125 passed |
| Full suite on this branch | **1237 passed** (+112) |
| Mutation check | **44 mutations, 44 killed, 0 survived** (31 round-1 + 10 review-fix + 3 re-anchored) |
| Entry-profile byte-identical pin | proven — 400 randomized chains diffed against the `ec53933` implementation; results and published rejection stats identical |
| Deleted-knob grep (`src/ tools/ deploy/ tests/ config/ docs/`) | clean; only deletion-assertion tests, `gates.md`'s explicit deleted-knob list, and historical FC-066 text remain |
| `dashboard/` changes | none |
| `earnings_avoidance_days` (FC-069's) | untouched |
| Terminal taxonomy | 18 scenarios, one per branch, each asserting exactly one terminal event, plus a source-level unclassified-event check |

### Deviations from the plan (detail in the PR body draft)

1. **`find_suitable_calls`'s `[:5]` truncation defeats DD-3.** The slice is
   ordered by `return_score` (annualised yield × OTM preference), which prefers
   far-OTM low-credit strikes — so the max-credit candidate can be dropped
   *before* the roller ranks anything. On GOOGL's chain the top-5 slice is
   plausibly all 8/07 contracts, every one a debit against the 370 8/07 being
   closed, which would have logged `no_credit_candidate` while C375 8/21 sat at
   ≈ +$248. This is DD-3's own rejected failure mode one layer earlier.
   **Resolved:** the `'roll'` profile returns the full legal set; the entry path
   keeps `[:5]`, pinned byte-identical. Deviation from the plan's literal text,
   taken to satisfy its binding intent — **needs reviewer sign-off**.
2. **Two terminal reasons added** to reconcile the per-position try/except with
   the exactly-one-terminal contract: `call_roll_skipped{evaluation_error}` and
   an error-severity `call_roll_execution_error` (the latter deliberately not a
   skip — an exception out of `execute_roll` may have left a filled BTC behind).
3. **`open_orders_unavailable`** — the plan does not say what happens when
   `get_orders` fails. Chosen: fail closed, own reason, mutation-pinned (M31).
4. **`invalid_expiry`** — an unparseable old expiry leaves no horizon to bound
   the replacement; fails closed under its own reason rather than being
   mislabelled `invalid_strike`.
5. **Fallback rungs below the invariant are skipped, not repriced** to the floor,
   matching the plan's "re-passing … the invariant" phrasing. A choice, not a
   derivation.
6. **`call_roll_dry_run` is a sixth terminal** (DD-5's table lists it; the
   exhaustive paragraph omits it). In dry-run mode it is the terminal.
7. **`_coerce_date` moved** to `src/utils/option_symbols.coerce_expiry_date` so
   `validate_roll`'s horizon bound shares the parser; `market_data._coerce_date`
   is kept as an alias, so all existing references resolve unchanged.
8. **`docs/gates.md` updated** (not in the plan's file list) — its roll-path
   table documented gates this FC deletes. Historical documents are not rewritten.

### FC-068 tripwire — flipped, as designed

`test_the_friday_roll_seat_is_occupied_and_still_a_no_op` is now
`test_the_roll_seat_is_occupied_daily_and_every_roll_nets_a_credit`: it asserts
the replay cadence is daily (evaluations exceed the Friday count) and that every
executed replay roll increased the strike and netted a credit ≥ 0. The replay
does now execute rolls end-to-end through the backtest adapter, which doubles as
a live integration check of the two-leg path.

### Two adversarial code reviews — DONE, dispositioned

Both REQUEST_CHANGES (trader w/ live re-derivation; execution lifecycle),
strongly convergent, **no reviewer-vs-reviewer disagreements**. Core money path
verified clean by both; the trader re-derived DD-8 live and put GOOGL C375 8/21
at exactly +$248, **rank 14 of 170** by `return_score` — confirming the `[:5]`
deviation was necessary (sign-off granted). Full table in the PR-body draft.

Two HIGH findings were the same class of defect — the code believed things it
had not verified:

- **Exec H-1 (BLOCKER):** cancel-then-verify did one re-read and dispatched on
  `filled_qty` while ignoring a non-terminal status. Alpaca cancels are
  **queued**; `pending_cancel` is real and such an order can still fill. Probe:
  an STO ladder placed three sells that **all filled** while reporting
  `naked_exposure` "no naked position". **Fixed** — poll to terminal after every
  cancel, both legs; fail safe to an alert-wired `call_roll_unknown_disposition`
  when it will not settle.
- **Exec H-2 = Trader M-1 (HIGH):** STO partial fill produced
  `call_roll_completed` with a mixed-quantity `net_credit` of **−$590** — a
  blended debit labelled a credit — and no naked-exposure event for the
  uncovered remainder, contradicting plan §1's own text. **This was a
  plan-contract violation absent from the deviations list above.** **Fixed** —
  per-replaced-quantity accounting, both legs reported explicitly, remainder
  raises `call_roll_partial_naked_exposure`.

Exec H-3 / Trader M-2 (alert-wire the breadcrumbs) fixed. Exec M-1 (in-process
`strategy_lock` vs. multi-instance Cloud Run) **noted, not code-fixed**:
`--max-instances=1` is now a required rollout step plus an explicit
"manual triggers must not race the 15:30 slot" runbook line. LOW batch all fixed
or noted.

Two mutations survived at some point and both were closed: **M39** (rungs 3+
anchored on `btc_limit` rather than the actual fill — the trader's L-3, invisible
because every prior test filled at the limit) and **M21'** (the shipped
cancel-settle bound, invisible because the autouse test fixture overrides it).

### Remaining before merge

- [ ] **Confirmation pass** (Fable, fresh context, reuse the execution-lifecycle
      persona; the H-1 fix rewrote the two-leg disposition logic, which is where
      a fix-time regression would hide)
- [ ] Open the PR from the draft body; merge
- [ ] Post-merge: alert policies → scheduler flip → supervised first cycle
      (exact commands in the PR body draft)
- [ ] File the `decision_events` `roll`-stage follow-up FC (DD-5)
